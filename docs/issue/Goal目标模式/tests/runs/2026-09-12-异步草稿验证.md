---
title: Goal 异步草稿验证
document_type: test-run
status: reviewing
created: 2026-09-12
updated: 2026-09-13
---

# Goal 异步草稿验证

## 1. 执行信息

- 范围：REQ-122，TC-348～TC-355；现有生成、Goal 宿主和 CLI 回归。
- 环境：macOS，独立 worktree `orca-goal-acceptance-generation`；基线 `fe61ba8b7a6b3f0ed2d1dc4c193e8d1240c82ee0` 加本轮变更。
- 隔离：`ORCA_BACKGROUND_LAUNCH=1`；隐藏 App、独立 profile、Goal Home、模型配置、样例工作区；不修改用户 App、Goal 或 provider 会话。
- 包：安装包 runtime 的隔离副本，覆盖本分支新构建的 main/preload/renderer/goal-driver，核对文件 hash 并单独签名。不将它称为标准发布构建。

## 2. 执行结果

最终隔离包：`1.4.197-async-test.1789228822115`，app.asar SHA-256 `56d9f6589f0320dece74b207fc3147d20a3f5426a57f8a176139bc34ea41e6d9`。1119 个构建输出逐文件核对，隔离副本签名严格检查通过。执行跨 2026-09-12～13。

| Case ID | 实际结果 | 结论 | 证据 |
| --- | --- | --- | --- |
| TC-348 | 真实 Codex 生成中关闭，任务继续；重新打开同一 attempt，未重复启动 | PASS | 最终真实轮 A1～A3 |
| TC-349 | 草稿行实际显示待审阅且出现准确完成通知；重新打开逐字找回 4587 字文档 | PASS | 最终真实轮 A4、A5 |
| TC-350 | 人工稿关闭重开保留；包含首尾空白的人工 Markdown 跨 App 退出/重启逐字恢复 | PASS | 受控轮 B5、B8 |
| TC-351 | 显示真实非零耗时、最近活动及距今时间；结构化工具活动被记录，不显示模型推理 | PASS | 最终真实轮 A3、受控轮 B0 |
| TC-352 | 显式停止发出实际 SIGTERM，显示已停止；同一编辑草稿以新 attempt 重试成功 | PASS | 受控轮 B1、B1b |
| TC-353 | provider 失败原因可见，原输入保留；重试成功；自动化证实连接失败只继续查询，不取消 | PASS | 受控轮 B2、B3；编辑器断线回归 |
| TC-354 | 正常退出后后台仍生成，重启接回；关闭期间 ready，冷启动找回且未再调用 provider | PASS | 受控轮 B7～B10 |
| TC-355 | 修改目标和人工稿后迟到结果不覆盖；只有显式采用才替换，按新上下文核对后才能开始 | PASS | 受控轮 B4～B6；新旧 attempt 防串写回归 |

额外兼容验证：无 Git 文件夹工作区、无执行会话可以起草并在关闭后找回结果，C1/C2 通过。最终 3 轮共 **19/19 个界面检查点通过**：真实 Codex 5 项，受控异常/恢复 12 项，受控普通文件夹 2 项；均未创建执行 Goal。

## 3. 验证证据

证据目录：`.docs/goal-acceptance-generation-ui-validation/2026-09-12/`（本地 ignored）。可读汇总入口为 `evidence/async-final-report.md`。

| 证据 | 结果与范围 |
| --- | --- |
| `evidence/goal-tests-verified.log` | 21 个文件、144 项 Goal 相关回归通过 |
| `evidence/claude-stream-regression.log` | 3 项 Claude 旧参数兼容、流式最终文档/错误解析通过；合计 147 项 Goal 相关自动化 |
| `evidence/cli-tests-verified.log` | 164 项 CLI 回归通过 |
| `evidence/tc-final-all-files.log` | `pnpm tc` 通过，最终退出码 0 |
| `evidence/quality-delivery.log` / `test-lint.log` | 本次改动质量与新增测试 lint 均 0 新问题 |
| `evidence/gate-*.log` | RPC 生成目录、本地化四项检查、fork-features、fork-docs、以 HEAD 为基线的本轮架构检查通过 |
| `evidence/async-real-1789228842467/` | Codex 0.154.0、low，真实生成 99.075 秒、4587 字文档；已核对待审阅行与准确 toast 的截图 |
| `evidence/async-controlled-1789228958054/` | 最终 12 项可控 CLI fixture 场景；不当作真实模型异常表现证据 |
| `evidence/async-controlled-1789229077993/` | 无 Git 文件夹工作区生成与找回 2 项 |
| `evidence/architecture-baseline.json` | 唯一默认全分支架构失败路径与规则均和基线 HEAD 相同，证明不是本轮引入 |

UI 只使用隐藏 Electron renderer 的 Playwright CDP。各测试 App/profile/自有进程已清理；用户 `/Applications/Orca.app`（PID 63088）未退出、替换或升级。

## 4. 缺陷与遗留问题

- 关闭编辑器隐式取消、无持久入口、无真实活动反馈：已修复。
- 点击停止与结果完成同时发生的竞态：原回归捕获，修复后 10 项生成测试通过。
- 快速完成落在两次列表轮询之间时遗漏提醒、旧 attempt 迟到响应污染新 attempt：补充修复，专门回归通过。
- CLI 长验收输出若恰好由单个数据块送达，旧捕获逻辑不设置截断标记：完整 CLI 回归捕获，修复捕获逻辑后 164 项全部通过。
- 默认 `check:architecture-policies` 仍有 1 项基线 `src/main/orcad/orcad-entry.ts` 差异；本轮未改该文件和失败规则。本轮范围检查通过，不扩大其他功能允许范围来掩盖。
- 早一轮 A4 只等待宿主 ready，草稿行尚未刷新：保留旧截图，最终真实轮已补拍真正待审阅与完成通知，不以旧截图证明界面状态。

## 5. 本轮结论

REQ-122 的异步草稿交互已实现，相关自动化及最终隔离包界面验证通过。本轮真实模型为 Codex；Claude 流式兼容做自动化验证。断线/进程不可确认通过自动化模拟验证，App 正常退出恢复通过隔离 App 实测；不宣称 SSH、异常断电或原业务大仓库任务均已实测。

尚未替换用户 App、提交或合并本轮代码。全分支架构基线问题保留在 Journal，进入合并阶段时需处理该集成基线。
