---
title: 顶部名称投影测试
document_type: test-case-list
status: ready
created: 2026-09-09
updated: 2026-09-09
issue: Issues看板与会话
---

# 顶部名称投影测试

此切片覆盖 REQ-025 的已有 Provider 名在 TabGroup → TabBar 中丢失，不代表全候选优先级已实现。

| 用例 | 关联需求 | 输入和操作 | 预期 |
| --- | --- | --- | --- |
| TC-264 分组后的最终标题 | REQ-025 | 两组真实元数据形状：Provider=Continue Orca session，generated=Continue work from the prior Orca；Provider=Read prior Orca session，generated=继续。经过 useTabGroupItemProjections 后再走 TabBar resolver | 顶部与 sidebar 均为各自 Provider 名；身份和来源字段完整保留 |
| TC-265 标题镜像兼容 | REQ-025 | Provider slot 只在 terminal 或 unified；terminal 新身份而 unified 滞后；terminal 明确 null | 字段缺失时兼容镜像；terminal 已有值或明确清除时由 terminal 决定，不复活旧名；两边都缺失才用 generated |

自动回归为 `src/renderer/src/issues/provider-title-tab-projection.test.ts`。UI 使用隐藏 Electron 的正常 TabGroup/TabBar 和 Sidebar，输入是只读核实后脱敏的名称元数据；不调用真实 Provider、不恢复原会话、不改用户 App，不把隔离回放宣称成用户当前 DOM。
