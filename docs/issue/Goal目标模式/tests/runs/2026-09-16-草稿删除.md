# Goal 草稿删除验证

- 需求：REQ-123；用例：TC-362～TC-365。
- 基线：`62b1b2c4f5acbf1824ee568717816ea07fb0d7a3`，分支 `feat/goal-draft-delete`。
- 结果：实现与本地验证通过；提交、CI 和安装状态另行记录。

## 自动化

| 验证 | 结果 |
| --- | --- |
| Goal 固定检查（宿主、渲染、契约、RPC、relay subprocess 等） | 31 文件、224/224 通过 |
| CLI 固定检查 | 166/166 通过 |
| 等价类型收窄后草稿存储/恢复复测 | 13/13 通过 |
| 编辑缓存失效、长时间停止及删除 UI 补充检查 | 8/8 通过 |
| 最终类型检查 | `pnpm tc` 通过 |
| PR 增量质量全部扫描 | 0 新问题，基线 `62b1b2c4f` |
| 架构、fork 功能、fork 文档、RPC 参数目录、行数 | 通过 |
| 本地化 catalog、extraction、coverage、runtime catalog | 通过 |

自动化覆盖删除后重启、迟到保存、并发新修订保护、停止/不可验证/失败重试、迟到启动不调用 provider、同 ID 主机隔离、旧宿主不支持、列表在途响应不复活、缓存失效和有界等待。补充检查是新测试和修改后的定向复测，不合并为一次全量执行计数。

日志位于 `.docs/goal-draft-delete-ui-validation/2026-09-16/evidence/`。第一次直接运行质量脚本使用默认上游基线，对全部历史 fork 改动报告旧 casting/design-system 问题；当前 CI 将这些规则按 PR 增量检查。按现有工作流基准核对通过，未改门禁规则或阈值。初次本地化检查发现不存在的 `common.cancel`，已复用 `goals.editor.cancel` 并重新验证。

## 隐藏 App 与真实 SSH

Run：`run-1789546876662`，Electron 43.7.0 / Chromium 150.0.7871.250，macOS 同机独立 OpenSSH，受控 codex 子进程。窗口始终隐藏、不聚焦。

| 检查点 | 结果 |
| --- | --- |
| 三类草稿常显删除入口 | PASS |
| 取消确认保留记录 | PASS |
| 普通、未命名、待审阅删除；Markdown 保留 | PASS |
| 重载不复活，迟到保存被拒绝 | PASS |
| 本机生成子进程退出后删除 | PASS |
| 不可验证保留，恢复后重试 | PASS |
| 存储错误保留，修复后重试 | PASS |
| 删除 SSH 同 ID 草稿，本机记录保留 | PASS |
| SSH 生成子进程退出后删除，重连不复活 | PASS |

9/9 核心检查点通过，14 张截图；Console/page error 均为 0。生成停止实际观测到独立进程 `started → SIGTERM terminated`，随后宿主 get 返回 null。不可验证和存储错误分别通过私有 owner 缺失及 tombstone 路径 EISDIR 注入，未修改用户数据。

完整图文报告、JSON、构建指纹、截图及清理记录位于 `.docs/goal-draft-delete-ui-validation/2026-09-16/evidence/run-1789546876662/`。验收包包含中文取消文案及 spawn 前停止检查；构建后 save 的等价类型收窄由最终类型检查和 13 项存储测试补证，不将旧截图冒称新的包截图。

前两次测试准备失败分别为验证构建未启用现有 store 暴露开关、辅助 RPC 未携带 authorityExecutionHostId；构建准备还遇到共享 out 目录并发写入竞争，改为串行构建后解决。均保留原始日志，未把工具错误算产品失败。

私有 App、sshd、relay、provider、临时 key/profile 均已清理，残留 PID 为 0。未调用真实模型，未覆盖异机 SSH、Windows/Linux 或发布包安装；用户当前 App 与真实会话未操作。

## PR 首轮 CI 跟进

[PR #28](https://github.com/Fartown/orca/pull/28) 首轮运行 `35073812011` 的类型、静态和打包检查通过，两个单测分片失败：

- 初始化检查发现新删除按钮在 `useRef` 参数中创建客户端；本地复现后改为 nullable ref，仅点击入口时创建并锁定所属主机。修复后初始化检查与删除 UI、客户端、同步、编辑缓存检查共 15/15 通过，`pnpm tc:web` 通过。未修改门禁。
- 未改动的 `AiVaultSessionSubagents.test.tsx` 全部断言通过，但测试环境销毁后的 loading 定时器触发 `window is not defined`。本地单独执行 18/18 通过，未复现该时序错误；原始 CI 日志保留，不将其描述为已修复。

日志见同一证据目录下的 `ci-shard-*-failed.log`、`ci-reproduction.log`、`ci-fix-tests.log` 与 `ci-fix-typecheck.log`。最新源码正在重新构建并执行隐藏 App 验证，最终 CI 与合入状态以 PR 为准。
