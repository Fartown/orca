---
title: 局域网分享自动化与冒烟
document_type: test-run
status: completed
created: 2026-09-17
updated: 2026-09-17
---

# 局域网分享自动化与冒烟

执行人：Claude Code。环境：minizc（macOS arm64）、Node 24.16.0、Vitest 4.1.11、Playwright 1.59.1 的 Chromium 无头模式。
分支：`feat/self-hosted-artifacts-lan-share`，基于 `fork/integration` 的 `e6277cf40`，改动未提交；上游分叉点 `a9232e8db`。

关联需求：REQ-301～REQ-309。用例入口：[功能测试](../cases/自托管产物后端功能测试.md)。

没有启动、退出或替换正在使用的 Orca；两台电脑上的真机用例 TC-01～TC-21 未执行。

| 检查                                   | 结果                        | 说明                                                                                                                                                                  |
| -------------------------------------- | --------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| AT-01～AT-08、AT-10、AT-13 本功能测试 | PASS                        | 本功能目录与接缝测试均通过（`self-hosted-artifacts.checks` 登记的命令）                                                                                               |
| AT-09 上游接缝回归                     | PASS                        | 编辑器头部、工作区文档预览工具栏（47 项）、设置导航元数据、CLI 入口                                                                                                   |
| AT-11 Agent 指南                       | PASS                        | `orca-cli-skill-guidance.test.mjs`；`verify:bundled-skill-guides`、`verify:skill-bundle-manifest`                                                                     |
| AT-12 真实服务与浏览器冒烟             | PASS                        | 亮、暗主题渲染（标题、表格、代码高亮、KaTeX、Mermaid、相对图片）无控制台错误；相对链接跳转；内嵌脚本未执行；敏感路径、目录、错误随机码、越界 404；POST 405；停止分享后 404 |
| 大范围回归                             | 1505 通过、7 跳过、1 失败   | 1513 个测试文件、13133 项；覆盖 CLI、RPC、启动、远程组件、SSH、编辑器、浏览器面板、设置、hooks、Artifacts 与本功能；失败项见下一行                                    |
| `ssh-posix-command-wrapper.test.ts`    | FAIL（环境问题，与本功能无关） | 本机 zsh 启动时打印 `Using Node v24.18.0`，断言 stderr 为空失败；未改动的主仓库 `fork/integration` 上同一测试同样失败                                                |
| 类型检查                               | PASS                        | `pnpm tc`                                                                                                                                                             |
| 本地化门禁                             | PASS                        | `verify:localization-catalog`、`-runtime-catalog`、`-extraction`、`-coverage`                                                                                        |
| 架构与登记门禁                         | PASS                        | `check:architecture-policies`（先 `git fetch origin`）、`check:fork-features`、`check:runtime-electron-ratchet`、`verify:rpc-params-catalog`                           |
| 其他 lint 门禁                         | PASS                        | oxlint（变更文件）、`audit:anti-slop`、`check:dead-classes`、`check:max-lines-ratchet`、`check:reliability-gates`、`check:ts-nocheck-ratchet`、`check:readme-local-links` |
| 变更文件质量门禁                       | 本功能文件无新增问题        | `check:code-quality:changed` 对比上游分叉点，报告的问题都在 fork 既有文件中                                                                                           |
| 两台电脑真机用例 TC-01～TC-21          | 未执行                      | 需要在 minizc、mbp5 安装包含本功能的 Orca，须经用户确认                                                                                                               |

回归中发现并已修复的问题：

- 远程组件引用分享服务时带进 Electron，远程组件在 SSH 主机上启动即崩溃（`src/relay/subprocess.test.ts` 等 3 个文件 37 项失败）；改为拆出服务标识模块，并新增 `artifact-share-relay-bundle.test.ts` 守住。
- runtime 依赖图新增 2 个引用 Electron 的模块（`check:runtime-electron-ratchet`）；改为 RPC 方法只依赖注册表，服务器与远程 Orca 调用由主进程启动时注入。
- 分享按钮在没有确认弹窗上下文的页面里渲染报错（工作区文档预览 47 项失败）；确认弹窗与 store 调用挪进只在打开时挂载的弹层内容。
- 上游测试断言旧文案与旧 RPC 名（`EditorPanelHeader`、设置导航元数据、CLI 入口各 1 项）；按新行为更新并登记为接缝。

证据位于工作区 `.docs/artifact-lan-share-ui-validation/2026-09-17/`，入口是该目录 README。
