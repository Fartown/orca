---
title: 本地默认地址修复验证
document_type: test-run
status: completed
created: 2026-09-10
updated: 2026-09-10
---

# 本地默认地址修复验证

执行人：Codex。环境：macOS arm64、Node 24.18.0、Electron 43.6.0。
分支：`feat/self-hosted-artifacts-http`；基线 `db5e82566cccb13fb0a0f0143fcfa0caf1ab1c99` 加本轮未提交修改。

关联需求：REQ-201、REQ-202、REQ-203、REQ-204、REQ-205。用例入口：[功能测试](../cases/自托管产物后端功能测试.md)。

| 检查                                 | 结果                                    | 说明                                                                                                              |
| ------------------------------------ | --------------------------------------- | ----------------------------------------------------------------------------------------------------------------- |
| TC-401 默认本地 origin、失败不回退云 | PASS                                    | 服务级捕获实际传给 fetch 的 URL；环境中无地址变量；模拟连接失败只发生一次本地请求                                 |
| TC-402～TC-408 既有地址和鉴权回归    | PASS（既有断言）                        | Artifact 与关联 Skills 回归共 77 项；不把源码分类单测当作远端部署证明                                             |
| TC-409 覆盖优先级                    | PASS（显式 origin > 环境 > 本地默认值） | 本轮新测试覆盖实际服务调用；完整 trim 组合仍沿用既有实现边界                                                      |
| TC-410 单端口 HTTP                   | PASS                                    | 独立后端 11 项测试使用临时目录、随机端口；从 LAN IPv4 对同端口创建并读取测试页面                                  |
| 已部署服务                           | PASS（本机发起请求）                    | `0.0.0.0:8787` 是唯一监听；两个 LAN IP 的 API 及原有两份页面均返回 200；三个数据文件哈希不变                      |
| 包内运行时                           | PASS                                    | 清除 API 和 CA 环境变量，用新包 Electron 的 Node 模式执行包内解析器，再请求本地 API，得到 HTTP 200 和两份原有产物 |
| 类型、架构、功能登记、国际化         | PASS                                    | 详见证据目录内各门禁日志                                                                                          |
| macOS 构建                           | PASS                                    | `pnpm build:mac` 完成；最终用于本机的 arm64 App 单独补齐本地签名并校验                                            |
| 当前用户 App UI                      | 未执行                                  | 未退出、替换或启动用户日常 App；不声称现有进程已使用新默认值                                                      |
| 第二台物理设备                       | 未执行                                  | 已验证本机 LAN IP 请求和所有接口监听，未验证另一设备的路由与防火墙                                                |

证据位于工作区 `.docs/artifact-http-lan-ui-validation/2026-09-10/`，入口是该目录 README。

包内 Node 验证执行了解析器和 HTTP 请求，未执行 renderer UI 或真实账号授权流程。后端测试使用隔离数据；生产请求只读取既有产物。
