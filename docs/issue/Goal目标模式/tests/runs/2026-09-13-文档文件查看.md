---
title: Goal 文档文件查看验证
document_type: test-run
status: reviewing
created: 2026-09-13
updated: 2026-09-13
---

# Goal 文档文件查看验证

## 1. 执行信息

- 范围：REQ-122 补充交互、TC-356；生成后的当前稿和迟到候选以文件地址展示，复用已有 Markdown 标签页。
- 工作区：`orca-goal-acceptance-generation`，分支 `feat/goal-acceptance-generation`，基线 `fe61ba8b7a6b` 加未提交变更。
- 隔离：`ORCA_BACKGROUND_LAUNCH=1`、隐藏 Mac App、独立 profile/Goal Home/普通文件夹；不操作用户 App 或已有会话。
- 证据根目录：`.docs/goal-acceptance-generation-ui-validation/2026-09-13/`（ignored）。

## 2. 自动化结果

| 检查 | 结果 | 证据 |
| --- | --- | --- |
| 文件落盘、原文保护、最新保存后查看、失败保留、既有 Markdown tab 本地路由 | 17/17 通过 | `evidence/file-entry-tests.log` |
| Goal 宿主/RPC/renderer/contract 相关目录回归 | 110/110 通过，包含上述 17 项及工作区不可用回归 | `evidence/regression-final.log` |
| `pnpm tc` | 退出码 0 | `evidence/typecheck-final.log` |
| 改动代码质量、type-aware、React Doctor | 0 新问题 | `evidence/checks-final.log` |
| fork-features/fork-docs、以 HEAD 为基线的本轮架构、RPC 目录、本地化四项 | 退出码 0 | `evidence/checks-final.log` |
| Goal driver 与最终 Electron 构建 | 退出码 0 | `evidence/build.log`、`evidence/build-activation-fix.log` |

首次新增自动化发现保存队列收尾时紧接着发生修改，查看可能未等到最新写入。修复 `flush` 等待后继续检查剩余保存，原失败用例通过；旧已打开文档保留原修订，人工新稿通过新文件查看。

首次真实界面验证又发现只在 Goal 下拉框选文件夹时，openFile 创建了文件 tab 但未激活工作区，中央仍停在欢迎页。已复用 activateAndRevealWorkspace 并声明提供文档页面，避免额外创建初始终端；最终包必须从未激活的普通文件夹入口重跑，不能先点侧栏绕过。

## 3. 隔离 App 结果

最终同一隔离包 **9/9 项通过**，运行目录 `evidence/markdown-controlled-1789259316864/`。包版本 `1.4.197-markdown-test.1789259250734`，app.asar SHA-256 `191d80f98a3b8db60a6c80ad897446f72a1060be58d087977a70928e04a3b18d`；1119 个新构建输出逐文件 hash 核对，隔离副本 deep/strict 签名通过。

| 检查点 | 实际结果 |
| --- | --- |
| D1 生成后文件路径 | 文件地址可见、正文框数量 0，文件内容与生成结果逐字一致 |
| D2 点击地址打开 | 未激活的普通 folder 自动选中；既有 Markdown tab 真实渲染标题、列表、代码块；执行终端数仍为 0 |
| D3 重复查看 | 同一文件 tab ID 与数量不变，复用已有标签页 |
| D4 修改后立即查看 | 等待最新保存，打开 acceptance-6.md，首尾空白原文逐字保持 |
| D5 草稿重开 | 人工稿与文件路径恢复 |
| D6 迟到候选 | 原稿与候选有各自真实路径，人工稿未覆盖 |
| D7 查看候选 | 原生标签页可见候选；返回草稿仍未采用 |
| D8 显式采用 | 点击采用后才替换当前稿 |
| D9 查看已采用稿 | acceptance-9.md 原生渲染采用后的文档，未创建执行 Goal |

主流程已复核最终人工稿和候选文件入口截图。`cleanup-result.json` 确认本轮自有 App/profile 已清理、无剩余自有进程、用户 App 未操作。测试包复用安装 runtime 叠加最新输出，修正本地打包脚本对隐藏目录的 asarUnpack glob，并确认 watcher entry 实际存在；不代表标准发布构建。

本轮使用可控 provider fixture 验证文件打开交互，没有调用真实模型。首轮 fixture 末尾换行预期与既有生成解析不一致，以及一次错误要求只读预览提供批注 DOM 属性，均作为测试脚本问题保留失败证据；最终同一包重新完整通过。未激活工作区停在欢迎页是真产品缺陷，已修复并由 D2 从原入口验证。

## 4. 验证边界

- 本轮增加的路径字段为可选，旧宿主无路径时保留文本编辑；后台生成/停止/采用和执行判定规则沿用上一轮。
- 文件预览固定读取本机，自动化覆盖当前选中其他 runtime 时不误路由；未宣称远端生成已支持。
- 原全分支默认架构门禁仍存在上一轮确认的 `orcad-entry.ts` 集成基线差异，本轮未改该文件和失败规则；本轮架构范围检查通过。
- 未提交、合并或替换用户安装的 App。本轮界面证据独立于标准发布构建、真实模型异常和执行 Goal/守卫完整闭环。

## 合码阶段补充（2026-09-13）

上述未提交/基线问题描述是验证时状态；本轮已通过 [PR #13](https://github.com/Fartown/orca/pull/13) 推进交付。合入最新集成基线后类型检查和 110 项相关回归再次通过。`orcad-entry.ts` 与 fork 的上游镜像 `Fartown/main` blob 一致，按 CI 实际使用的该基准运行完整架构规则通过；此前本地默认 `origin/main` 指向未合入的 stablyai 新提交，不能据此认定 fork CI 基线已损坏。没有修改失败规则或该无关模块。最终远端检查与合并状态以 PR 为准。
