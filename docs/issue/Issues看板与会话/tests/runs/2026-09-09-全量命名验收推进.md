---
title: 全量命名验收推进
document_type: test-run
status: draft
created: 2026-09-09
updated: 2026-09-10
issue: Issues看板与会话
---

# 全量命名验收推进

执行人：Codex。当前代码为 feat/claude-owner-window / 9899e7148023 加未提交工作区；不安装用户App，不发布或合入。完整目标仍参照[TC-231～254](../cases/Provider优先命名验收.md)及REQ-025/026/028；按2026-09-10用户最新要求，本轮先跑通真实Provider核心流程，暂停边界矩阵，不以全量边界通过作为核心验证前置。历史单项PASS不自动折算本轮PASS。

## 已执行

### 2026-09-10 用户要求先验核心流程（testing）

- 本轮仅验证真实Claude/Codex在隔离Orca正常PTY中启动、接受自身身份、读取原生名，并在Top/Workspace左侧/普通Dashboard一致显示；通过Provider原生改名验证同步。没有原生名时明确标为回退，不把首prompt冒充Provider name。
- 使用最新独立构建1.4.197；主流程重新计算21,150源码和951产物哈希，全部与01:37:33 UTC构建manifest一致，无需重复编译。证据目录 `.docs/session-name-core-ui-validation/2026-09-10/`；不操作用户日常App或原只读会话。
- Activity第六轮的同状态换身份、生成重新开启/关闭跨入口对照已通过；原生改名reader/显示也通过。原始任务关键词搜索未命中保留FAIL证据，按用户要求deferred，不继续修搜索/历史/分屏等边界，也不据此宣称全量已完成。
- 真实Codex 0.154.0核心失败：第三轮正常新建及原生 `/rename` 后，内部标题任务B替换A，三处显示标题模板。第五轮旧包只读入口探针补齐12条raw/accepted证据：B先发无prompt、transcript_path=null的SessionStart，再发标题模板Prompt；A Stop没有prompt但继承B缓存，B Stop最终再夺身份。原始FAIL分别归档，未以短暂正确的改名断言记通过。
- 修复TC-266：新SID缺transcript的SessionStart不能覆盖已有有transcript的Codex主会话；复用既有严格标题模板判据，在provider normalizer写prompt/model缓存之前识别B，listener私有有界集合阻止B后续无prompt事件。main远端入口同样保护，旧relay缓存回声不污染A；无新DB/RPC/wire字段。先补Prompt/Stop两项RED，再由raw证据补SessionStart两项RED，均保留日志。
- Claude交互式新profile停在Provider onboarding，不能记通过；真实Claude print `--name` / 同测试SID `--resume --name` 已读到原生名，但C1 Dashboard采样时进程已退出显示0 total，该项撤销PASS。新构建复验会用真实工具同步门保留活动行，不伪造Provider事件或名称。
- 最终核心通过（同一02:16:24 UTC构建、两次独立运行）：第六轮Claude print初始原生名/同SID改名共4检查点PASS，Dashboard真实1 total；第七轮仅Codex交互CLI新建/原生改名共4检查点PASS，测试39.2秒。C3从独立读取的本轮原生index确定预期，不固定最早的identity兜底；第六轮这项脚本失败保留，不改写原日志。Claude可执行文件安装切换期一次ENOENT及同路径重试单列为环境恢复，非命名产品缺陷。
- 主流程独立核对：Codex A=`01a0891f-6608-7610-937e-2b4921258ec5`，B=`01a0891f-723a-7dc0-a617-a60ac51232ac`；B的真实Start/Prompt/Stop全部到达，accepted B=0，accepted A Stop保留自身prompt。338次采样中288次含A，错误身份0、顶部标题模板0；不把采样等同逐帧证明。原生index在同A下由`Reply CORE_CODEX_OK`改为`Codex Core Native`，C4D同图Top/左侧/Dashboard一致；主流程已目视该图及Claude C1D/C2T。
- 最终源码：88文件844通过/9跳过；注册9组233/96/114/14/15/103/64/23/187全部通过，分组有交叉不累加。tc、定向type-aware lint、架构/fork/docs/max-lines和4项本地化门禁均通过。主流程对22,268个源码/构建记录重新哈希，changed=[]。本轮未合入或安装，Claude交互onboarding与已暂停边界不记通过；最终证据见`.docs/session-name-core-ui-validation/2026-09-10/`。
- 交付收尾：主流程补齐两次运行的审计合并报告，`final-report.md`与`test-result.json`明确8个检查点来自Claude print/Codex交互两次运行；render/validate均exit0、8张截图可解码。回读PID83101/98055均不存在、两套私有profile与测试repo均已清理；用户App未操作。既有失败目录及原始Codex-only结果不改写为成功。

### 2026-09-10 Activity 名称订阅、历史身份与生成设置（本轮验证中）

- 第三轮隐藏 App 13 PASS 后 A4 真正产品 FAIL：A done→B done 后，HTTP已接受B，Top/reader为B，但Activity等15秒仍A。main与renderer的B `stateStartedAt=1789002853106`仍沿用A，B `updatedAt=1789002854322`；事件按pane/state/time去重，历史A先占位，新B被丢弃。旧完整构建、截图、报告冻结于 `evidence/attempt-03/`，不覆盖为成功。
- 该漏测来自直接builder测试没有传真实timing。补真实HTTP Stop A→B和携旧时间的renderer用例，修前2 FAIL/4 PASS，原始输出 `main-a4-red.log`。主进程现仅在已接受明确身份换代时开始新状态时钟；renderer兼容旧host复用A时钟，并用历史身份限制重复旧时钟回退。修后HTTP+entry+实际IPC到挂载Activity共3文件15项PASS、`pnpm tc`通过；第四轮独立构建UI复验中，不能以此提前标A4 UI通过。
- 首批实际 `useAgentPaneThreads` + app store + public store 回归 4/4 FAIL：无顶部 slot 不读原生名、单独改名不刷新、最新详细 prompt 替代文件首问、历史继承当前 SID。修后该组通过；工具/回复仍独立于会话名。
- 扩展 producer 反例 A done→B done：没有状态变化时旧记录仍使用 A 的时间、却已是 B 的 ID，1/3 FAIL；修为明确 SID 变化也封存旧历史、重置状态时间，不继承 A 实时名。A done→B working、paired history identity-only equality/publication 同组保留断言。
- 按 TC-245 新增 renderer 生成关闭的负例，1/4 FAIL；修为只有开关开启才从当前 Hook/同身份历史派生，Provider 文件已提取的稳定首问证据照常显示。此前 200 项回归没有该负例，不能用该批通过掩盖设置缺口。
- 首次扩大回归的 6 项失败中：1 项调度包装词被派生为名称是产品回归并修复；1 项旧容器别名优先断言按确认的公共排序更新；4 项未读测试因旧 raw prompt 定位与清洗后名称不符，改为明确 aria-label，未弱化未读写入次数/状态断言。派生名称测试夹具显式开启生成，另保留关闭设置的负例。
- 首版新增 split 测试传了不合法 children/sizes 结构，unit 虽通过但 typecheck 拒绝；改为仓库既有 first/second/ratio 后复测。此类数据形状错误不记产品缺陷，也不声称旧夹具证明真实分屏。
- 生产源最终冻结前的 targeted 为 5 文件 31 通过；后续设置修复后的完整结果、门禁和隐藏 App 正在重跑。UI 脚本从正式 Sidebar Activity 入口操作，legacy 整页通过既有导航 action 的 setup 单列。使用私有 Provider 文件和真实 HTTP Hook，未运行真实 Provider CLI、未安装用户 App。
- 设置修复后：Activity + 3 个命名专项共 34 文件 194 通过（其中命名专项 15 项）；注册 9 组回归分别 233/96/109/14/12/102/64/23/187 通过，批次重叠不累加。`pnpm tc`、架构/fork/docs/max-lines 与四项本地化门禁通过。最终 nullable settings 测试夹具改用完整公共 fixture，再跑 2 文件 8 项通过；产品源码未变，无需重新构建该 test-only 修正。
- UI 第一次已证正式 Sidebar 原生名/工具/回复和 Top/Workspace，但 legacy h2 检查失败：现有 selectThread 会 activateAndRevealWorkspace 并回 terminal，属于测试入口预期不符，不是命名失败；legacy详情标未执行，不修导航。第二次 Codex 名称已正确，工具输入缺失：沿用了 Claude 字段，按 Codex parser 校正测试数据中；保留两轮原始记录，不提前给完整UI通过结论。

### 2026-09-10 通知名称与事件身份续修（本轮真实链通过，全量未完成）

- 最终新独立构建完整重跑：1场景34.2秒（总35.0秒），N0/N1/N1C/N3/N2/N2ABA共6检查点、6图PASS。五次真实完成snapshot均含自身SID/path，实际dispatch.sessionTitle与main mobile notification帧同名；raw prompt/回复仍保留。真实Hook/Provider格式文件→reader→cache→renderer IPC→main formatter/replay，不注入最终名；非真实CLI、原生OS、手机或正式安装包验收。
- N2从真实dispatch入口到释放原A结果80ms，N2ABA为118ms，不把前置Hook quiet时间算入读取deadline。ABA原始IPC显示新名结果交付1789000405035、旧A交付1789000405058，本轮实际观察到新先返/旧后返23ms；通知旧A名、当前slot/Top/Sidebar保持新A名。脚本没有主动等待保证这个排列，报告为本轮观察而非通用延迟承诺；本地测试另明确等待新A入slot、通知未发后再释放旧A，16项通过。
- 主流程独立核对最终原生“继续”与ABA截图、事件SID/path、两次真实reader证据和最终slot；21141源码及952产物重新哈希，仅freeze后补强的test文件不同，生产源/产物均无漂移。可读final-report.md已生成并render/validate均exit0，主流程回读完整结果与validator；三轮私有PID96689/81389/88643均exited，profile/home/repo已清理、ownSurvivors0，用户App未操作。
- 最后production改动后的8组注册检查96/94/14/12/102/64/23/187全部PASS，含专用跨层tsc；pnpm tc、定向type-aware、fork/features/docs、架构、max-lines、4本地化及diff check通过。changed-quality相对98b0c329ff39覆盖457分支变更文件，三类0新增；批次有交叉不汇总，457不是本轮改动数。命令摘要及注册回归日志在本目录evidence/main-validation.json和main-fork-checks.log。
- 两轮脚本失败保留：首次误按相同notificationId命中dismiss帧而读不到title；第二次以1500ms等待unread marker，混淆Hook自身quiet window且active pane正常清除了标记。改为过滤type=notification、只读不暂停条件断点采实际dispatch入口及其SID，cold期限从入口起算；均属automation，不是产品FAIL。未通过改formatter、注入名称、增加生产冷读期限或快进main时钟制造通过。

- 通知原链只传 raw terminalTitle/当前任务快照，没有 session 名。首批6项中5FAIL/1PASS，增加独立可选 sessionTitle 并复用公共排序/host读取后38项通过；这不是当时已覆盖全部边界的证明。
- 新边界测试抓到无session ID事件借旧generated、BEL未用自身原生名两处产品遗漏，另有错误 split.children 夹具，应使用实际 first/second。修后45项通过；Provider明确“继续”、旧人工回退、稳定首任务、同ID不同host、inactive split、cold超时只发一次/晚到不重命名覆盖在内。
- 接真实公共store与Gate后，A→B本身不失效A（错误要求A缓存为空已纠正，属test_data）；A→B→A则实证通知错发Codex A。readSnapshot现共享原I/O但保留事件一次性结果，不把旧结果写回已失效cache/Tab；同时验证新代A名称不被旧响应覆盖。snapshot测试7项通过：去重、队列尚未启动即失效、reset、不同path重读和absent/unavailable/cleared语义。
- 首次tc暴露 AgentCompletionStatusSnapshot 不含providerSession；此前测试直接给完整AgentStatusEntry绕过了生产字段投影。新增真实本地IPC applicator、paired store-patch的2条入口反例均FAIL，确认不是只有类型问题。现各入口显式带已接受事件身份；不扩宽shared状态白名单，不转发launchToken；directSSH只关联同Agent已接受状态。修后20文件123项通过，pnpm tc和定向oxlint通过；完整注册8组、changed-quality仍进行中。
- 主进程format/delivery测试验证desktop与mobile同名、状态/工具/回复正文不变、旧producer缺字段兼容、Unicode截断、通知ID保留；inactive folder分别断言ssh与paired host请求，不读取本地同ID替代。9文件98项批次有交叉，不与123累加。
- 生产源码已冻结给隔离后台新构建，证据目录 `.docs/session-name-notification-ui-validation/2026-09-10/`。实际Hook/文件reader/renderer IPC/main formatter/mobile replay即将验证，尚无UI通过结论；可选只读CDP条件断点核对生产snapshot身份，不暂停或替换dispatcher。原生transport禁用，系统横幅/声音/真实手机/headless均NOT RUN。
- 当前命名读取新增最多1500ms冷读等待，未读标记仍同步，超时只发捕获fallback一次；不是完全无时序变化。未安装/重启用户App、未提交合入；Activity和其他消费者、真实ownership/authority与生产包全量验收仍未完成。
- freeze后process-exit补测1FAIL：丢弃unfinished状态/正文时连同有效自身名称一起丢掉。修复只让fresh且与明确Provider相符的旧状态提供identity，不把working状态/回复送进完成通知，正/反3项通过。执行者已重建包含该修改的renderer并核验hash，未拿前一个构建作最终验收。

### 2026-09-10 History 操作与独立刷新续修（本轮UI通过，全量未完成）

- 最终修复后新独立构建完整重跑：1个场景41.3秒、28检查点及28截图全部PASS。H2在History新名出现后立即要求Top于1秒断言上限内同名，随后连续Delete/Continue/C2及最终H2T均新名；9条去重状态中7条History已新名，Top/slot旧名0条。不把TTL时钟快进量或断言上限当实际改名延迟。主流程回读序列并目视C2/H2T，52项冻结源码独立重算一致。
- H5无同SID status/sleep/retained/slot和Conversation，仅正常Refresh，无该步时钟快进或额外名称调用；真实list返回新native后公共exact reader只请求该identity，搜索/行/Delete/Continue一致。Claude原生/正式人工/首任务回退及三种子日志自身名称均通过；Codex仅验证删除禁用、接续及取消。取消前后文件hash/清单、PTY、Conversation IDs不变，未提交删除或启动Provider。
- 最终源码门禁7命令69/14/12/92/64/23/187通过，含专用跨层tsc；tc、type-aware、架构/fork/docs/max-lines、4项本地化通过。完整changed-code-quality相对`98b0c329ff39`覆盖442个分支变更文件，三类0新增；批次不相加，442不是本轮编辑数。新构建952产物hash匹配；五轮隔离App（最终PID90440）均exited，私有home/profile/repo已清理、ownSurvivors0。用户App未操作。
- 最终证据在`.docs/session-name-history-actions-ui-validation/2026-09-10/`，包含原生名阻塞失败的`evidence/attempt-04/`及修后序列。Console/pageerror/requestfailed分别监听均0；输入仍为Provider格式文件和HTTP身份fixture，非真实CLI/生产安装包。通知、Activity、其他消费者与真实ownership/host验收仍未完成，不能据此标全需求完成。
- 可读报告已生成并经validator确认28检查点、28张渲染截图、verdict=PASS，主流程已回读报告及校验日志。H1虽安装list交付屏障，本最终轮实际被扣留响应0个；能证明旧scan对象与新direct名分离，不能声称本轮覆盖了被扣留响应的释放竞态。
- 07:45隐藏App旧构建完整操作有27个检查点通过，但主流程目视C2发现History新名、Top旧名。执行者回读22个稀疏快照，H2D到final跨度14.688秒，同SID/path父Tab slot一直旧名；exact reader早已返回新名，list屏障已释放。补H2T记产品FAIL、整体FAIL，不能用27项局部PASS收尾。前3轮另有展开按钮selector、Codex删除能力预期及多元素断言错误，分别保留automation/test_data归因，不与该产品失败混淆。
- 新增真实cache+Tab store反例，local/ssh两项修前全FAIL；源码`subscribeSessionNames`只调度input-quiet reconcile，已确认native也等用户停止操作，而前一轮只修了manual通知。现Gate提供已有cache快照getter，通知时按当前准入host/identity立即投影，读取/TTL/idle调度不变；ABA旧回流用例接入同一getter。含跨host/换B/unavailable/cleared合同的3文件36项通过，tc及定向type-aware通过；新构建需在连续操作时同时断言Top/History，当前尚未UI复验。
- 本段执行从9月9日23:55跨午夜到10日，代码仍为`9899e7148023`加未提交工作区；覆盖TC-234/243/246，完整需求未结束。
- 新增真实公共cache+History action hook反例：6项修前全FAIL，具体为删除弹窗/接续还取旧`session.title`、共用父SID的子日志借父缓存、子日志seed污染父槽。接入既有`getScannedSessionDisplayName`，限定独立扫描身份可读写公共slot；原操作ID/path/host和旧scanner title不改。修后相关4文件19项通过；删除IPC仅mock捕获参数，未删除真实会话。
- 补真实临时文件→`listClaudeSubagentSessions`→公共显示链，3项修前1FAIL/2PASS：只存旧title的侧车description会被Task prompt顶掉。adapter现发布可选`providerName`：明确transcript native优先，其次child自身`subagent.meta.description`。原title和parentSessionId未改，旧端仍看原投影，不加RPC字段必填要求。含既有child scanner的4文件17项通过。
- 再补“History-only、没有活跃Tab消费者”hook反例，修前2FAIL：新scan已有Renamed native，但cache/搜索/行保留Old且精确reader调用0。`seed`此前只要records.has便退出，所有已有UI改名样本由Tab读者带动，没证明独立History刷新。现发现不同native或补齐首任务时经现有公共reader重新确认，保留在途旧confirmed，沿用host批量/去重，不让旧scan直接覆盖较新direct读。修后History/store/binding 4文件26项通过。
- 隐藏App验收正在`.docs/session-name-history-actions-ui-validation/2026-09-10/`准备/执行，独立新构建需覆盖真实菜单Delete/Continue与取消、原生改名、正式manual与prompt回退、真实文件子日志及History-only刷新；未收到本轮完整报告前不记UI通过。
- 类型检查最初报TS6307：新main目录测试跨层导入renderer。完整断言迁入既有跨层集成测试分类`tests/e2e/issues-session-name-file-chain.unit.test.ts`，新增专用tsconfig和注册check，保留真实parser→public display/cache整链，不扩大生产tsconfig或删断言。该专用tsc、完整`pnpm tc`和定向type-aware lint随后通过。侧车测试3条lint问题已修；文档索引过期由`generate:issue-index`重建，不手改索引。
- 最终注册7命令全部通过：67/14/12/92/64/23/187项（第2组包含专用tsc），批次交叉不相加；架构、fork/features/docs、max-lines、三项本地化和`git diff --check`通过。完整changed-code-quality相对`98b0c329ff39`覆盖441个分支变更文件，code/type-aware/React Doctor三类0新增；不是本轮编辑441文件。可读命令结果摘要在本轮UI目录`evidence/main-validation.json`，该摘要不宣称UI通过。
- 本轮尚未修改通知/Activity；源码确认通知rich标题使用workspace+agent/status，terminalTitle只在fallback正文出现，没有会话名称字段；Activity取名仍有独立task-title路径且agent-session→TerminalTab投影丢原生证据。后续需区分事件时会话标识与任务摘要，保留原有时序、未读、焦点、liveness和移动端分发行为，不能直接把prompt/回复替换成名字。

### 前序执行

- 新增 `src/shared/session-names/session-name-priority.test.ts`，覆盖全候选次序、Provider快照、Claude/Codex标签抢占、低信息prompt与明确原生/人工同名的区别、短有效请求、注入过滤、分屏父标签及普通shell。旧实现18项中13 FAIL / 5 PASS，证明与需求不一致，不是环境问题。
- 新增最终 `SortableTab` 渲染反例：Provider名与customTitle并存，旧组件仍显示customTitle；5项中1 FAIL / 4 PASS，明确命中公共resolver之外的覆盖。
- 公共排序、候选质量、最终组件投影的8文件94项通过；随后扩大到扫描器、传输、恢复和Issues的104文件706项通过。批次有交集，不累加为覆盖率。一次复用导出名拼写错误引起的TypeError已纠正，该次失败不记作产品缺陷或旧代码反例。
- `provider-name-evidence.test.ts`：改前7项中6 FAIL / 1 PASS，改后通过。覆盖Claude原生记录/短明确名、Codex索引优先于旧meta、有效首任务、增量fold、四态合并和paired结果保留。`cleared`仅验证合同/投影，不代表已确认任何Provider版本的真实撤销事件；当前适配器未凭空制造撤销。
- `provider-name-file-chain.test.ts`：2项通过。实际临时JSONL文件经生产parser/cache、file reader、结果validator及共享显示函数；覆盖12次后续“继续”、Claude append改名、Codex仅index变化、不可读取、身份不匹配。没有mock parser/结果；但输入是测试生成的Provider格式文件，不是本轮运行真实Provider CLI，不算UI端到端。
- `provider-name-store-chain.test.ts`：实际状态写入函数、terminal/unified镜像与完整workspace序列化/解析通过。新增测试初稿漏填unified的groupId/worktreeId导致测试行被schema丢弃，补齐合法夹具后通过；不归因产品。来源独立变更不再被旧的“文字相同”判断吞掉。
- 同步层保留Provider证据、旧人工候选和首任务，不再先执行人工名??Provider；不可读/缺名保留同身份已确认缓存。返回时检查host、当前准入身份、pane、PTY；旧测试“身份已经改变仍写旧响应”已改为拒绝迟到。**尚未证明上游准入本身始终正确，也尚未覆盖ABA绑定代次。**
- `dashboard-name-ownership.test.ts`：改前3项2 FAIL / 1 PASS，确认快照卡片未做与侧边栏同等的slot身份复核、合成子会话继承父名；已修。仪表盘/既有身份保护的12文件93项全部通过。旧Hook测试曾明确要求把容器别名分发到两个pane，按已确认的归属规则改为各自实时名；保留所有断言，不把失败删掉。
- 扩大扫描回归曾发现缓存对象在内容不变时失去引用复用，已修后保持原断言通过。WSL停顿模拟继续返回其他请求，并新增核对停顿项为unavailable。真实WSL/SSH机器未在本轮运行。
- Issues完整注册检查46文件186项全部通过；同样不与其他批次相加。
- `pnpm tc`、fork注册、架构、文档与max-lines门禁通过；没有提高行数上限或增加disable。四项本地化检查通过（保留仓库既有缺译/allowlist，不声称全量已翻译）。changed-code-quality首次报告15项新风格问题，已修后复跑通过：代码、type-aware、React Doctor均0新增；其408文件是相对分支基线的检查范围，不是本轮编辑数。

## 完整验收清单

### 22:28 公共读取与消费面续修

- renderer单条/批量写入：已有低信息generated“继续”加custom/quick标签的两条反例先失败，修复后16项通过；容器标签不阻止首个有效任务入槽，后续普通回合不覆盖有效首任务。orchestration显式强制替换仍需独立核对。
- 新增公共renderer只读名称store，复用既有64条批次和分host顺序，合并重叠请求、保留四态与同身份原生缓存、统一订阅/定时器，限制记录数4096；关闭最后订阅释放定时器，reset拒绝旧生命周期响应。旧人工候选仍来自兼容适配，不新增数据库/人工名RPC。
- Issues移除显示层私有请求、useState缓存及人工名阻断查询；按公共host/agent/id key读取。旧title_source及幽灵过滤、人工操作存储未删除；这个key尚未证明覆盖所有profile/WSL/account-home歧义，不能宣称完整authority合同已达成。
- History反例3项中2 FAIL：旧搜索索引返回人工名，实际Row DOM同时显示人工名和“继续”；修复后3项通过。共享扫描证据和显示订阅，只有可见行持续点查；行和拖拽payload用同名，搜索索引随改名更新，不原地改raw session.title或操作ID/path。含既有Row操作回归的4文件18项通过。
- Dashboard新增挂载Hook反例2项中1 FAIL：没有顶部slot的A/B两行都显示“继续”。接入各自identity的公共读取后通过；合成无pane子行不请求、不继承父名。含既有归属回归的5文件29项通过。
- 新store并发path补全反例8项中1 FAIL：旧无path请求未结束时，新transcriptPath被去重吞掉。修复为前请求结束后按新hint重新读取，reset后不再补发。它证明hint不丢失，不证明同ID跨账户天然无歧义。
- 顶部短轮询反例22项中1 FAIL：已有有效任务fallback但providerName=absent时调度300000ms。改为按native证据判断，缺名20秒；已有named维持方案既定5分钟，不承诺实时。另验证共享cache更新可投影到sleeping tab且底层API只读一次，无反馈请求循环。
- 本批扩大回归命令：`ORCA_BACKGROUND_LAUNCH=1 pnpm exec vitest run --config config/vitest.config.ts src/renderer/src/session-names src/renderer/src/lib/ai-vault-tab-title-sync.test.ts src/renderer/src/components/dashboard/use-agent-row-conversation-name.test.ts src/renderer/src/issues src/renderer/src/components/right-sidebar/AiVaultSessionRow.test.tsx src/renderer/src/components/right-sidebar/ai-vault-session-filters.test.ts src/renderer/src/store/slices/terminal-tab-title-batch.test.ts`，22文件151项通过。以上批次相互重叠，不累加。
- `pnpm tc`、fork注册、架构、max-lines ratchet、diff whitespace检查通过。首次typecheck发现新History测试夹具缺字段，补齐合法类型后通过，属于测试数据问题。batcher仅把类型推广为host-owned请求，64上限/同host串行未改；登记seam并从原byte-parity pin移出这一文件，由原批次行为测试继续保护。changed-code-quality仍运行中，不提前写通过。
- 当前生产源码冻结供隔离hidden Electron验证；目录`.docs/shared-session-names-ui-validation/2026-09-09/`。计划使用明确标注的Provider格式fixture走真实parser/API/store/DOM，不等同真实CLI生成或实际用户App验收；报告尚未完成，当前不记UI PASS。没有安装、重启用户App、提交或合入。
- 随后新增公共index兜底反例2项中1 FAIL：把身份兜底预先放进公共名称map，会使Issues提前返回而遮住其live候选。修为公共index只发布Provider/旧人工/有效任务，最终各消费面再合并自己拥有的live/label/身份兜底；明确Provider名恰好等于兜底文字仍保留。扩大回归更新为23文件153项通过。
- 四项本地化门禁复跑通过。changed-code-quality先报curly及watch清理形状2项；curly已修，watch原本返回cleanup且释放定时器的测试通过，但React Doctor只识别watch句柄的unsubscribe/close形式。改成显式`{ unsubscribe }`并由Hook清理，15项公共链测试与定向lint通过；未关闭或抑制门禁，完整quality复跑中。两次构建冻结后的源码小改均通知UI执行者核对hash/重新编译renderer，不能以旧产物验新源。
- 最终本地源码门禁：再次`pnpm tc`通过；`pnpm check:code-quality:changed`相对`98b0c329ff39`检查420个分支变更文件，code/type-aware/React Doctor均0新增；420不表示本轮编辑数。`node config/scripts/run-fork-feature-checks.mjs --feature issues-board`注册6条命令全部通过：公共renderer31、Codex保护12、文件读取/同步90、共享排序64、Claude保护23、Issues187项；批次互有交集，分别报告不汇总覆盖率。UI仍待独立报告。

### 22:42 隐藏App发现的缓存缺陷（复测中）

- 隔离App首轮使用自建Provider格式文件和HTTP身份fixture，非真实CLI。真实parser/API→公共cache→Gate自然写slot，Top/Sidebar/History/Dashboard/Issues的初始名与绑定后人工名对照均有DOM证据；未注入aiVaultTitle。构建输入单独冻结，当前仍待最终报告。
- 第一轮S3：Claude同长度重写custom-title后API仍为旧名，Codex index新名已更新；记录在`.docs/shared-session-names-ui-validation/2026-09-09/evidence/attempt-01/`。该轮未采两次stat，不能补造原现场mtime；保存完整两版fixture。第二轮另测Claude append，五入口均观察到新名，不覆盖第一次失败。
- 主流程新增真实文件反例明确固定两次mtime相差1秒、size完全相同；3项中1 FAIL，file reader仍返回AAAA而不是BBBB，确认是产品缓存缺陷。`parseResumableCandidate.canResume`此前只检查新size>=已消费offset，等长重写被当追加，从EOF读0字节。修为仅文件真实增长时复用incremental；同长或缩短重写全量重解析。原append/unterminated/grown-rewrite guard逻辑保留，不宣称能识别所有“增长且重写前缀”的文件。
- 生产变更仅既有`src/main/ai-vault/session-scanner-parse-cache.ts`的增长条件，已登记seam/allowlist；包含多Provider缓存、persist、file reader、WSL停顿与native evidence的7文件73项通过。原生mtime+size均未变时仍按既定cache合同复用，不伪称内容hash校验。
- 第二轮S4把删字段/空index预期写成manual回退，违反`absent`保留同identity最后named的合同；实际保留named是正确行为，属于测试预期错误，不能记产品失败。执行者被要求保留该轮证据、修正计划，最终单独从未命名的新identity验manual回退。
- 注意S1仅设customTitle属于容器别名，不是会话人工名；S2实际conversations.update后才具备旧Orca会话人工候选。上述分类已要求报告修正。最终缓存修复构建/复测进行中，旧报告不冒充修复后的通过。
- 主流程审阅U1脚本发现customTitle与正式manual使用相同文本，可能即使人工名链失败也由容器fallback假通过；要求改为互不相同的候选值，最后DOM必须为正式manual，同时保留API absent和slot来源核对。这个测试设计问题记录在执行过程，不记为产品失败。

### 22:55 人工候选的更新时序（复测中）

- 严格U1实际暴露Top身份兜底/Sidebar人工名不同步，原图与slot.manualTitle=null留在attempt-04。不能凭超时就声称永久丢名：同构建自然0/5/20/35秒诊断中，0/5秒renderer authority partition还没有新人工值，20秒时partition、slot与Top均已正确。原有15秒authority轮询属于数据到达延迟；稀疏采样不足以精确量出到达后投影延迟。
- 独立单测构造“canonical已经通知、后台扫描idle scheduler尚未释放”的明确反例，23项中1 FAIL，Top仍为identity/manualTitle=null。修复复用现有人工候选投影逻辑，在canonical通知时直接更新两个Tab模型，不等待输入空闲或其他host文件I/O；文件扫描的批量/限频保持原逻辑。不把此独立反例伪称为旧U1全部延迟的已证实根因。
- 修后公共store、实际store与sync共6文件39项通过；`pnpm tc`、定向lint/type-aware、max-lines、fork注册及架构门禁通过。缓存修复后上一轮完整注册6命令通过，其中读取/同步组由90增加至91；本次再加同步反例后的完整套件仍待结束，计数互有交集不求和。
- 最终UI需先观测正式manual进入renderer的既有authority store，再核对Top与Sidebar同步，候选文字必须不同；RPC到renderer的轮询时间仍单独披露。生产源码再冻结、独立重建，不能拿旧构建的成功代替本次修复验收。
- 本次完整注册6命令随后全部通过：公共renderer31、Codex保护12、读取/同步92、共享排序64、Claude保护23、Issues187（交叉批次不相加）。完整changed-code-quality相对`98b0c329ff39`覆盖421个分支变更文件，code/type-aware/React Doctor均0新增；并非本轮编辑421文件。
- 22:56的新构建严格UI轮次37.3秒完成，18个DOM检查点通过、2项NOT RUN。U1这轮RPC后authority数据到达约14760ms，检测到authority后Top/Sidebar/slot全部断言约47ms完成；不是精确渲染latency测量或去除了15秒poll。主流程核对15项生产输入hash无差异，并目视等长改名Top/Sidebar和U1截图。
- 截图复核又发现Dashboard图右侧卡片标题被裁切，虽然sheet DOM全文断言通过，不能声称完整视觉验收；要求执行者保留该轮、确认稳定bounds/动画/overflow后补采，不擅自归因动画或宣称布局bug已证实。最新可读报告须与最终截图对应，当前仍不提前记完整UI验收。
- 22:55只读核实日常App进程49318及安装版本`1.4.197-local.1788947190284.9899e7148023`，仍不是本轮独立e2e构建；未替换、重启、提交或合入。该事实会随用户操作变化，不用历史包指纹推断后续运行状态。

### 稳定截图最终回归

- 同一冻结构建复跑，Playwright共40.4秒、1个场景通过；18个核心检查点PASS、2个可选NOT RUN（无人工名live、真实sleep生命周期）。完整重跑初读、绑定后优先级、同长度重写、append、absent保留及从未named身份人工回退，不用单独修图替代回归。
- Dashboard运行时初始bbox固定，但clip-path为`inset(0px 100% 0px 0px)`且300ms有限CSS动画未结束；稳定采样后clip-path=none、动画完成。补等待稳定与全文Range在sheet/viewport内断言后截图，主流程目视两张完整卡片名与Top/Sidebar一致。裁切是采证抢在动画完成前，不是已确认的持久产品布局缺陷；旧图和报告保留在attempt-06。
- 最终U1自然数据到达约14823ms，检测到authority后两Provider的Top/Sidebar/slot断言约26ms完成（1.5秒断言上限）；仅代表该场景观测，不承诺精确延迟或消除既有轮询。Provider格式fixture、HTTP身份fixture和renderer刷新时钟加速边界不变，仍不是实际CLI、真实子Agent运行或生产安装包验收。
- 报告、18张截图、API/DOM/文件证据、15项关键生产源及完整构建指纹均在`.docs/shared-session-names-ui-validation/2026-09-09/`；全部实现状态仍以以下全量清单为准。
- 最终`final-report.md`生成与validator均exit 0；主流程回读摘要、问题分类、NOT RUN及`delivery-verification.json`的15项hash一致。隔离App PID32464已exited、私有profile/repo已清理、路径匹配残留0，详见`cleanup-result.json`。用户日常App和原Provider会话未被本轮清理。

### 全量状态

23:42 分屏初始化续修：

- 第一轮binding脚本等待单agent情况下不存在的summary按钮，实际Top/Sidebar均A，这是automation失败；保留attempt01。后续B1确认无名B后Top/Sidebar无旧A native/generated；B3同ID同path，新A真实结果早于被扣留旧A约499ms返回，旧A扣留550ms，Top/Sidebar的4条MutationObserver记录均无过期名。五入口另一场景40.2s通过。
- 不能据此结整体PASS：第三轮在两个PTY已登记时，Split后的10条自然采样跨度944ms显示manager/document=empty、store=A、Top=A。正常点回A再点empty会一致，但这是额外绕过动作，故单列B2I为FAIL、整体FAIL（24个通过检查点保留，2项NOT RUN）。该初始化区间无精确时刻截图，相关state证据可用，不能伪称截图完整。
- 新增`session-name-split-initialization.test.tsx`经过实际Hook和Layout setter/Name Gate，修前4项中local/remote形式2 FAIL、2 PASS；源码确认新PTY就绪后原binding writer只补map，保留了先前规范到旧A的activeLeafId。新增owned纯函数在注册PTY时仅同步manager当前同leaf同numericId选择，不focus、不在用户改选后抢回。已登记窄seam和scope，未改协议。
- 修后相关10文件68项通过；首次误用未安装jsdom的worker失败已纠正为既有happy-dom，不算产品失败。产品再次冻结给隔离重建，本次必须Split后无需额外回点即可一致并截图；旧构建FAIL不删除，不提前记新轮通过。
- 最终新构建两个场景64.1秒通过，27检查点中25 PASS、2 NOT RUN（无人工名可信live、真实sleep生命周期）。B2I在两个manager PTY与store map精确对应后，无任何额外回点就自然empty，Top Terminal1、slot null；33条自然样本（含初始化10条）manager/store/document不一致0条。A自身inactive Sidebar行仍保留A，不能把不同实体误要求同名。主流程目视B2I与B3R截图。
- 同ID同path ABA的真实IPC旧返回扣留565ms，新A结果早373ms交付；4条MutationObserver记录中旧名0条，最后Top/Sidebar均“绑定 A 当前名称”。`page.clock.fastForward(321000)`只触发既有TTL刷新，时间序列不冒充实时刷新或Provider网络时延基准；HTTP身份fixture不证明真实子Agent准入。
- 主流程独立重算37项冻结源码hash无差异；源码package版本1.4.197，独立e2e启动器Electron/app.getVersion为43.6.0、Chromium150.0.7871.250，不是生产安装包。两个隔离App PID44486/46252均exited，profile/repo清理、ownSurvivors0；用户App未操作。最终报告在`.docs/session-name-binding-ui-validation/2026-09-09/final-report.md`，历史失败在`evidence/attempt-03/`；render/validate均exit0，27检查点、25截图，主流程已回读结果。
- 源码最终门禁：完整注册6命令55/12/92/64/23/187通过；tc、架构/fork/docs/max-lines、本地化、定向type-aware/React Doctor通过；完整changed-code-quality相对`98b0c329ff39`检查429个分支变更文件，三类0新增。计数不是本轮编辑数，各测试批次仍不相加为覆盖率。

本轮绑定续修（23:28 冻结源码）：

- 先补六条红测：取 tab 主 PTY 而非 own leaf、active shell 借 inactive agent、PTY/generation 未触发同步、ABA 旧响应、真实新 identity 仍有旧 slot。随后又逐项复现旧 generated 残留、其他 Provider 接管、歧义 split、移除 leaf、共享 cache 迟到回流、库存先缺失后明确更换。失败记录来自真实 store/公共 cache；不是只测字符串排序。
- `session-name-binding` 同步记录当前 revision 和最后已知绑定；后者仅处理缺库存后明确替换，不将缺记录判为退出。自己的 leaf PTY、generation、host、path、provider identity 均参与代次；active shell 不选其他 pane。新 identity 清旧 generated 后仍能捕获自己的首个有效任务，同 identity rename 不清除。
- 真实公共 cache ABA 反例特别说明：只挡 sync 旧回调还不够，旧结果先进入 cache，下一轮又显示 `Old A read` 而非 `Current A read`。修复按 identity 取消过时代次未完成读取并清 TTL（保留已有 confirmed 名），发布前逐请求核对仍在位的 PendingRead；失效旧读不能污染新读或另一 host。queued/in-flight、同 host/跨 host、保留 confirmed 名的回归均加入。
- 绑定/同步 3 文件 41 项通过；完整 `issues-board` 注册 6 命令通过：公共 renderer 51、Codex 12、读取/同步 92、共享排序 64、Claude 23、Issues 187。批次交叉不相加。`pnpm tc` 通过；此前新 test fixture 的 settings 不完整造成类型错误，已改用既有 defaults。架构通过，fork import audit 发现 3 个未注册类型引用，补登记并复跑，不关闭门禁。
- 新 UI 目录 `.docs/session-name-binding-ui-validation/2026-09-09/`，计划正常 UI 创建真实分屏，Provider 格式文件→生产解析/API→Gate，HTTP 身份 fixture；迟到场景包装真实 IPC handler 扣留原始返回，明确不是 Provider 网络或真实 CLI。旧五入口场景也要在新构建重跑。当前执行中，不提前记 PASS，上一轮源码指纹不覆盖本轮变更。
- 随后源码门禁：架构、fork注册、fork文档、max-lines、三项本地化均通过；changed-code-quality相对`98b0c329ff39`检查426个分支变更文件，code/type-aware/React Doctor均0新增（不是本轮编辑426文件）。本地化首次把新`*-test-store.ts`中的Shell fixture识别成UI字面量，按既有测试支持文件规范改为`*-test-fixture.ts`，未加allowlist或改门禁；重命名后绑定/cache3文件28项通过。UI构建记录了这一test-only变化，runtime冻结文件未变。

| 要求 | 当前证据/结论 | 下一步 |
| --- | --- | --- |
| TC-231/232/242：Provider第一与逐级回退、普通shell保留 | 公共函数、顶部最终组件、真实格式文件到显示函数已通过；全入口未完成 | 继续History/通知等未接公共候选的消费面 |
| TC-245/251/252/254：有效稳定prompt与低信息区分 | scanner及renderer单条/批量低信息保护已修并通过反例 | orchestration强制替换需逐调用核对 |
| TC-233/234/235/253：原生证据、索引改名、四态、OSC归属 | Claude/Codex可选证据贯穿parser/cache/validator/worker/slot，冷热index已测；部分完成 | Provider真实撤销事件、其他agent适配器、真实SSH/WSL及每个host producer继续验收 |
| TC-236～241/250：旧人工兼容、无Issues可用、组织不改名、旧端合同 | 公共只读候选/旧值兼容及退出Issues独立请求已实施，局部回归通过 | 原有存储/旧端/全scope组合与真实UI继续验收 |
| TC-243/244/254、REQ-028：后台B、合法子会话、真切换、迟到、分屏、窄过滤 | 真切换、ABA/PTY代次、公共cache回流及新split初始化已修，隔离UI的绑定7项通过；既有活动窗口不是全量归属证明 | 真实Provider对照序列、准入身份真假、后台utility纳管与跨端组合仍待完成 |
| TC-246/247：History、搜索、弹窗、Dashboard、Map、Activity、通知、选择器 | History/Dashboard公共读取及History操作新构建28检查点通过；通知公共快照与真实Hook→main replay新构建6检查点通过；全项未完成 | 继续Map、Activity、选择器；原生OS/真实手机未验证，通知比较事件时名称 |
| TC-248/249/250：重启、profile、local/folder/SSH/paired/WSL、Mobile、headless、Chat、新旧端 | 未完成 | 逐host和版本组合验证；不可观测项不算通过 |
| 用户截图default行、上一轮包内Issues45秒缺名 | 未定位/未归因 | 用真实identity与读取结果定位，不能靠改测试文案消除 |
| 新生产包真实端到端、安装与集成 | 未完成 | 完成前述要求再打包验收；需重启用户App时另确认，不影响Provider会话 |

## 通过标准

每项必须有匹配范围的证据；框架/夹具阻塞、真实读取失败和产品断言失败分开。单测数据、metadata注入回放、真实Provider运行与生产包端到端是不同层，不互相冒充。没有观测Console/网络时写未检查，不写0错误。

## 下一批明确源码入口

- `src/renderer/src/store/slices/agent-status-{live,orchestration}-actions.ts`：仍有按dispatch显式强制替换generated的调用；需区分任务label与首任务名，不能以单条/批量setter通过掩盖这条路径。
- `src/renderer/src/components/right-sidebar/ai-vault-session-{delete-action,continuation}.ts`与`AiVaultSessionSubagents.tsx`：9月10日已接公共显示链，源码反例及最终隐藏UI通过；原raw title及操作identity不变，title_source的幽灵过滤/旧人工保护保留。Codex原有删除禁用已核对，适用删除回退用Claude覆盖；未提交真实删除/接续启动。
- `ai-vault-tab-title-requests.ts`已对齐选中leaf的`ptyIdsByLeafId`及generation；ABA须连公共cache一起验。当前反例已修，隐藏UI和真实Provider层仍分别验证，不能只验证响应时字符串刚好相等。
- Dashboard/Activity/通知/选择器/移动端等必须逐消费者接共同结果；本轮未增加独立人工名数据库、顶层存储或新RPC。
- `getActivityThreadTaskTitle`仍先custom/调度label再generated/当前prompt；需在它的调用面区分“会话名”和“任务预览”，不要把任务字段全局改名而破坏状态语义。通知已在2026-09-10新增可选会话名快照并保留raw terminalTitle/agentPrompt，真实链与旧端验证边界见本轮记录；此进展不证明Activity/headless已统一。
- `TerminalTab.generation`与`TerminalLayoutSnapshot.ptyIdsByLeafId`已接入输入比较和绑定代次；仅最终字符串一致仍不足以验收ABA，UI补中间态观察。该机制只处理已准入的renderer identity，不能证明host准入始终正确。
- History子会话不可机械接host/agent/sessionId缓存：子JSONL可携带parent-session。9月10日已限定共用父SID的子日志不得读写父slot，并发布自身侧车description证据；子行通过公共resolver取自身native/sidecar/prompt，原SID/path保留。真实格式文件跨层反例已通过；这一隔离不等于真实Provider后台调用准入已完成。
