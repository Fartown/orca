diff --git a/config/scripts/verify-packaged-daemon-entry.cjs b/config/scripts/verify-packaged-daemon-entry.cjs
index 8f5e8028..b66c35c2 100644
--- a/config/scripts/verify-packaged-daemon-entry.cjs
+++ b/config/scripts/verify-packaged-daemon-entry.cjs
@@ -32,9 +32,16 @@ function assertPackagedDaemonEntryExists(resourcesDir) {
 // <appOutDir>/resources elsewhere). execPath defaults to the packaging Node.
 function verifyPackagedDaemonEntryBoots(resourcesDir, options = {}) {
   const execPath = options.execPath || process.execPath
+  const spawn = options.spawnSync || spawnSync
   const entryPath = assertPackagedDaemonEntryExists(resourcesDir)
 
-  const result = spawnSync(execPath, [entryPath], { encoding: 'utf8', timeout: 10_000 })
+  let result = spawn(execPath, [entryPath], { encoding: 'utf8', timeout: 10_000 })
+  if (result.error?.code === 'ETIMEDOUT') {
+    console.warn(
+      '[verify-packaged-daemon-entry] initial boot timed out; retrying the freshly packaged entry once'
+    )
+    result = spawn(execPath, [entryPath], { encoding: 'utf8', timeout: 10_000 })
+  }
   if (result.error) {
     throw new Error(
       `[verify-packaged-daemon-entry] could not launch daemon-entry.js: ${result.error.message}`
diff --git a/config/scripts/verify-packaged-daemon-entry.test.mjs b/config/scripts/verify-packaged-daemon-entry.test.mjs
index 8705953b..8315e07b 100644
--- a/config/scripts/verify-packaged-daemon-entry.test.mjs
+++ b/config/scripts/verify-packaged-daemon-entry.test.mjs
@@ -43,6 +43,41 @@ describe('verify-packaged-daemon-entry', () => {
     expect(() => verifyPackagedDaemonEntryBoots(resourcesDir)).not.toThrow()
   })
 
+  it('retries one transient timeout without weakening the boot assertion', () => {
+    writePackagedEntry('console.error("Usage: daemon-entry <socket>"); process.exit(1)\n')
+    const attempts = []
+    const spawnSync = (...args) => {
+      attempts.push(args)
+      if (attempts.length === 1) {
+        const error = new Error('spawnSync node ETIMEDOUT')
+        error.code = 'ETIMEDOUT'
+        return { error, stderr: '', stdout: '' }
+      }
+      return { status: 1, stderr: 'Usage: daemon-entry <socket>\n', stdout: '' }
+    }
+
+    expect(() => verifyPackagedDaemonEntryBoots(resourcesDir, { spawnSync })).not.toThrow()
+    expect(attempts).toHaveLength(2)
+    expect(attempts[0][2]).toEqual({ encoding: 'utf8', timeout: 10_000 })
+    expect(attempts[1][2]).toEqual({ encoding: 'utf8', timeout: 10_000 })
+  })
+
+  it('fails after a second timeout', () => {
+    writePackagedEntry('console.error("Usage: daemon-entry <socket>"); process.exit(1)\n')
+    let attempts = 0
+    const spawnSync = () => {
+      attempts += 1
+      const error = new Error('spawnSync node ETIMEDOUT')
+      error.code = 'ETIMEDOUT'
+      return { error, stderr: '', stdout: '' }
+    }
+
+    expect(() => verifyPackagedDaemonEntryBoots(resourcesDir, { spawnSync })).toThrow(
+      /could not launch daemon-entry\.js.*ETIMEDOUT/
+    )
+    expect(attempts).toBe(2)
+  })
+
   it('fails when the packaged entry cannot resolve its module graph', () => {
     writePackagedEntry('require("orca-module-that-does-not-exist")\n')
     expect(() => verifyPackagedDaemonEntryBoots(resourcesDir)).toThrow(
diff --git a/mobile/app/h/[hostId]/index.tsx b/mobile/app/h/[hostId]/index.tsx
index 9cf10c94..62a77162 100644
--- a/mobile/app/h/[hostId]/index.tsx
+++ b/mobile/app/h/[hostId]/index.tsx
@@ -18,7 +18,8 @@ import {
   Check,
   UserCircle,
   PanelLeftClose,
-  SquareTerminal
+  SquareTerminal,
+  Bell
 } from 'lucide-react-native'
 import type { RpcClient } from '../../../src/transport/rpc-client'
 import { loadHosts, updateLastConnected } from '../../../src/transport/host-store'
@@ -98,6 +99,7 @@ import {
 import type { RepoSummary } from '../../../src/worktree/host-worktree-rpc-types'
 import type { WorkspaceStatusDefinition } from '../../../../src/shared/worktree/types'
 import { DEFAULT_MOBILE_WORKSPACE_STATUSES } from '../../../src/worktree/mobile-workspace-statuses'
+import { supportsMobileIssues } from '../../../src/issues/mobile-issue-screen-model'
 
 function isErrorVerdict(v: ConnectionVerdict): boolean {
   return v.kind === 'warning' || v.kind === 'unreachable' || v.kind === 'auth-failed'
@@ -184,6 +186,7 @@ export function HostScreen({
   const [showFilterModal, setShowFilterModal] = useState(false)
   const [actionTarget, setActionTarget] = useState<Worktree | null>(null)
   const { hostCapabilities, floatingWorkspaceEnabled } = useHostProtocolGates()
+  const mobileIssuesEnabled = supportsMobileIssues(hostCapabilities)
   const [confirmDelete, setConfirmDelete] = useState<Worktree | null>(null)
   const [confirmRemoveHost, setConfirmRemoveHost] = useState(false)
   const [routeActionState, setRouteActionState] = useState(() =>
@@ -973,6 +976,24 @@ export function HostScreen({
                 />
               </Pressable>
 
+              {mobileIssuesEnabled ? (
+                <Pressable
+                  style={[
+                    styles.embeddedToolbarIconButton,
+                    connState !== 'connected' && styles.toolbarIconDisabled
+                  ]}
+                  onPress={() => navigateFromHostList(`/h/${hostId}/issues`)}
+                  disabled={connState !== 'connected'}
+                  accessibilityRole="button"
+                  accessibilityLabel="Needs your input"
+                >
+                  <Bell
+                    size={16}
+                    color={connState === 'connected' ? colors.textSecondary : colors.textMuted}
+                  />
+                </Pressable>
+              ) : null}
+
               {floatingWorkspaceEnabled ? (
                 <Pressable
                   style={[
@@ -1085,6 +1106,21 @@ export function HostScreen({
               />
             </Pressable>
 
+            {mobileIssuesEnabled ? (
+              <Pressable
+                style={styles.searchToggle}
+                onPress={() => navigateFromHostList(`/h/${hostId}/issues`)}
+                disabled={connState !== 'connected'}
+                accessibilityRole="button"
+                accessibilityLabel="Needs your input"
+              >
+                <Bell
+                  size={16}
+                  color={connState === 'connected' ? colors.textSecondary : colors.textMuted}
+                />
+              </Pressable>
+            ) : null}
+
             <Pressable style={styles.searchToggle} onPress={() => setShowSearch((s) => !s)}>
               {showSearch ? (
                 <X size={16} color={colors.textSecondary} />
diff --git a/mobile/app/h/_layout.tsx b/mobile/app/h/_layout.tsx
index 7f77c33b..090de888 100644
--- a/mobile/app/h/_layout.tsx
+++ b/mobile/app/h/_layout.tsx
@@ -43,6 +43,8 @@ function HostStack({ animation }: { animation: 'none' | 'default' }) {
       <Stack.Screen name="[hostId]/edit" options={{ title: 'Edit host' }} />
       <Stack.Screen name="[hostId]/accounts" options={{ title: 'Accounts' }} />
       <Stack.Screen name="[hostId]/tasks" options={{ title: 'Tasks' }} />
+      <Stack.Screen name="[hostId]/issues" options={{ title: 'Needs your input' }} />
+      <Stack.Screen name="[hostId]/issues/[conversationId]" options={{ title: 'Issue context' }} />
       <Stack.Screen name="[hostId]/session/[worktreeId]" options={{ title: 'Terminal' }} />
       <Stack.Screen
         name="[hostId]/source-control/[worktreeId]"
diff --git a/mobile/src/notifications/local-notification-scheduling.ts b/mobile/src/notifications/local-notification-scheduling.ts
index f5113462..ae67de8f 100644
--- a/mobile/src/notifications/local-notification-scheduling.ts
+++ b/mobile/src/notifications/local-notification-scheduling.ts
@@ -10,6 +10,10 @@ export type NotificationEvent = {
   title: string
   body: string
   worktreeId?: string
+  executionHostId?: import('../../../src/shared/execution-host').ExecutionHostId
+  conversationId?: string
+  issueId?: string
+  roundRecordId?: string
   notificationId?: string
   // Desktop-assigned seq for reconnect catch-up (#8129); optional since older runtimes may omit it.
   notificationSeq?: number
diff --git a/mobile/src/notifications/notification-routing.test.ts b/mobile/src/notifications/notification-routing.test.ts
index 779aa242..b8b4e21a 100644
--- a/mobile/src/notifications/notification-routing.test.ts
+++ b/mobile/src/notifications/notification-routing.test.ts
@@ -10,17 +10,25 @@ describe('notification routing', () => {
     expect(
       buildLocalNotificationData(
         {
-          source: 'agent-task-complete',
+          source: 'agent-needs-input',
           worktreeId: 'repo::/Users/me/orca/workspaces/feature',
-          notificationId: 'agent:one'
+          notificationId: 'agent:one',
+          executionHostId: 'ssh:docs',
+          conversationId: '33333333-3333-4333-8333-333333333333',
+          issueId: '22222222-2222-4222-8222-222222222222',
+          roundRecordId: '44444444-4444-4444-8444-444444444444'
         },
         'host-1'
       )
     ).toEqual({
-      source: 'agent-task-complete',
+      source: 'agent-needs-input',
       hostId: 'host-1',
       worktreeId: 'repo::/Users/me/orca/workspaces/feature',
-      notificationId: 'agent:one'
+      notificationId: 'agent:one',
+      executionHostId: 'ssh:docs',
+      conversationId: '33333333-3333-4333-8333-333333333333',
+      issueId: '22222222-2222-4222-8222-222222222222',
+      roundRecordId: '44444444-4444-4444-8444-444444444444'
     })
   })
 
@@ -40,6 +48,52 @@ describe('notification routing', () => {
     })
   })
 
+  it('routes an Issue notification to its exact Round before falling back to Conversation or Workspace', () => {
+    expect(
+      getNotificationNavigationTarget({
+        hostId: 'host-1',
+        worktreeId: 'repo::/Users/me/orca/workspaces/feature',
+        executionHostId: 'ssh:docs',
+        conversationId: '33333333-3333-4333-8333-333333333333',
+        issueId: '22222222-2222-4222-8222-222222222222',
+        roundRecordId: '44444444-4444-4444-8444-444444444444'
+      })
+    ).toEqual({
+      hostId: 'host-1',
+      sessionTarget: {
+        name: '[hostId]/issues/[conversationId]',
+        params: {
+          hostId: 'host-1',
+          executionHostId: 'ssh:docs',
+          conversationId: '33333333-3333-4333-8333-333333333333',
+          issueId: '22222222-2222-4222-8222-222222222222',
+          roundRecordId: '44444444-4444-4444-8444-444444444444',
+          worktreeId: 'repo::/Users/me/orca/workspaces/feature'
+        }
+      }
+    })
+
+    expect(
+      getNotificationNavigationTarget({
+        hostId: 'host-1',
+        worktreeId: 'repo::/Users/me/orca/workspaces/feature',
+        executionHostId: 'local',
+        conversationId: '33333333-3333-4333-8333-333333333333'
+      })
+    ).toEqual({
+      hostId: 'host-1',
+      sessionTarget: {
+        name: '[hostId]/issues/[conversationId]',
+        params: {
+          hostId: 'host-1',
+          executionHostId: 'local',
+          conversationId: '33333333-3333-4333-8333-333333333333',
+          worktreeId: 'repo::/Users/me/orca/workspaces/feature'
+        }
+      }
+    })
+  })
+
   it('falls back to the host screen when the payload has no worktree id', () => {
     expect(getNotificationNavigationTarget({ hostId: 'host-1' })).toEqual({
       hostId: 'host-1',
diff --git a/mobile/src/notifications/notification-routing.ts b/mobile/src/notifications/notification-routing.ts
index 5f81fb35..329fb44c 100644
--- a/mobile/src/notifications/notification-routing.ts
+++ b/mobile/src/notifications/notification-routing.ts
@@ -1,13 +1,23 @@
 import type { HostStackRouteTarget } from '../navigation/host-stack-navigation'
+import { mobileIssueRouteTarget } from '../issues/mobile-issue-route'
 import { mobileSessionRouteTarget } from '../session/mobile-session-route'
 import type { HostCredentialStatus } from '../transport/types'
+import type { ExecutionHostId } from '../../../src/shared/execution-host'
 
-export type DesktopNotificationSource = 'agent-task-complete' | 'terminal-bell' | 'test'
+export type DesktopNotificationSource =
+  | 'agent-task-complete'
+  | 'agent-needs-input'
+  | 'terminal-bell'
+  | 'test'
 
 export type DesktopNotificationEvent = {
   source: DesktopNotificationSource
   worktreeId?: string
   notificationId?: string
+  executionHostId?: ExecutionHostId
+  conversationId?: string
+  issueId?: string
+  roundRecordId?: string
 }
 
 export type LocalNotificationData = {
@@ -15,6 +25,10 @@ export type LocalNotificationData = {
   hostId: string
   worktreeId?: string
   notificationId?: string
+  executionHostId?: ExecutionHostId
+  conversationId?: string
+  issueId?: string
+  roundRecordId?: string
 }
 
 export type NotificationNavigationOptions = {
@@ -40,6 +54,18 @@ export function buildLocalNotificationData(
   if (event.notificationId) {
     data.notificationId = event.notificationId
   }
+  if (event.executionHostId) {
+    data.executionHostId = event.executionHostId
+  }
+  if (event.conversationId) {
+    data.conversationId = event.conversationId
+  }
+  if (event.issueId) {
+    data.issueId = event.issueId
+  }
+  if (event.roundRecordId) {
+    data.roundRecordId = event.roundRecordId
+  }
   return data
 }
 
@@ -78,10 +104,19 @@ export function getNotificationNavigationTarget(
   }
 
   const worktreeId = readNonEmptyString(record.worktreeId)
+  const issueTarget = mobileIssueRouteTarget({
+    hostId,
+    executionHostId: record.executionHostId,
+    conversationId: record.conversationId,
+    issueId: record.issueId,
+    roundRecordId: record.roundRecordId,
+    worktreeId
+  })
   const credentialStatus = options.credentialStatusByHostId?.get(hostId)
   return {
     hostId,
-    sessionTarget: worktreeId ? mobileSessionRouteTarget({ hostId, worktreeId }) : null,
+    sessionTarget:
+      issueTarget ?? (worktreeId ? mobileSessionRouteTarget({ hostId, worktreeId }) : null),
     ...(credentialStatus === 'missing'
       ? { credentialRecovery: 're-pair' as const }
       : credentialStatus === 'temporarily-unavailable'
diff --git a/src/main/agent-hooks/server.ts b/src/main/agent-hooks/server.ts
index f819414a..0f6952a3 100644
--- a/src/main/agent-hooks/server.ts
+++ b/src/main/agent-hooks/server.ts
@@ -313,6 +313,10 @@ function sanitizeHydratedEntry(
   const source = isAgentHookSource(record.source) ? record.source : undefined
   const providerPromptId =
     source === 'claude' ? normalizeClaudePromptId(record.providerPromptId) : undefined
+  const providerTurnId =
+    source === 'codex' && typeof record.providerTurnId === 'string'
+      ? record.providerTurnId.trim() || undefined
+      : undefined
   const compactTrigger =
     source === 'claude' && (record.compactTrigger === 'manual' || record.compactTrigger === 'auto')
       ? record.compactTrigger
@@ -326,6 +330,7 @@ function sanitizeHydratedEntry(
     hasExplicitPrompt: record.hasExplicitPrompt === true ? true : undefined,
     hookEventName: typeof record.hookEventName === 'string' ? record.hookEventName : undefined,
     providerPromptId,
+    providerTurnId,
     compactTrigger,
     toolUseId: typeof record.toolUseId === 'string' ? record.toolUseId : undefined,
     toolAgentId: typeof record.toolAgentId === 'string' ? record.toolAgentId : undefined,
@@ -1881,6 +1886,7 @@ export class AgentHookServer {
       hookEventName?: string
       source?: unknown
       providerPromptId?: unknown
+      providerTurnId?: unknown
       compactTrigger?: unknown
       toolUseId?: string
       toolAgentId?: string
@@ -1946,6 +1952,12 @@ export class AgentHookServer {
     const source = isAgentHookSource(envelope.source) ? envelope.source : undefined
     const providerPromptId =
       source === 'claude' ? normalizeClaudePromptId(envelope.providerPromptId) : undefined
+    const providerTurnId =
+      source === 'codex' &&
+      typeof envelope.providerTurnId === 'string' &&
+      envelope.providerTurnId.trim().length > 0
+        ? envelope.providerTurnId.trim()
+        : undefined
     const compactTrigger =
       source === 'claude' &&
       (envelope.compactTrigger === 'manual' || envelope.compactTrigger === 'auto')
@@ -2058,6 +2070,7 @@ export class AgentHookServer {
       promptInteractionKey,
       hookEventName,
       providerPromptId,
+      providerTurnId,
       compactTrigger,
       toolUseId,
       toolAgentId,
diff --git a/src/main/artifacts/artifact-cloud-config.test.ts b/src/main/artifacts/artifact-cloud-config.test.ts
index 1d0d5162..bd7b506c 100644
--- a/src/main/artifacts/artifact-cloud-config.test.ts
+++ b/src/main/artifacts/artifact-cloud-config.test.ts
@@ -6,27 +6,68 @@ import {
 
 describe('resolveArtifactCloudApiUrl', () => {
   it('uses the first-party production origin by default', () => {
-    expect(resolveArtifactCloudApiUrl(undefined, {}, true)).toBe('https://share.onorca.dev')
+    expect(resolveArtifactCloudApiUrl(undefined, {})).toBe('https://share.onorca.dev')
   })
 
-  it('allows loopback HTTP only in development', () => {
+  it('allows loopback HTTP so a self-hosted backend needs no certificate', () => {
     expect(
-      resolveArtifactCloudApiUrl(
-        undefined,
-        { ORCA_ARTIFACTS_API_URL: 'http://127.0.0.1:45961' },
-        false
-      )
-    ).toBe('http://127.0.0.1:45961')
-    expect(() => resolveArtifactCloudApiUrl('http://127.0.0.1:45961', {}, true)).toThrow(/HTTPS/)
+      resolveArtifactCloudApiUrl(undefined, { ORCA_ARTIFACTS_API_URL: 'http://127.0.0.1:8787' })
+    ).toBe('http://127.0.0.1:8787')
+    expect(resolveArtifactCloudApiUrl('http://localhost:8787', {})).toBe('http://localhost:8787')
+    expect(resolveArtifactCloudApiUrl('http://[::1]:8787', {})).toBe('http://[::1]:8787')
+  })
+
+  it('allows a private LAN backend over either scheme', () => {
+    for (const origin of [
+      'https://10.95.173.189:8787',
+      'http://10.95.173.189:8787',
+      'https://192.168.1.5:8787',
+      'http://192.168.1.5:8787',
+      'http://172.16.0.1:8787',
+      'http://172.31.255.254:8787',
+      'http://nas.local:8787'
+    ]) {
+      expect(resolveArtifactCloudApiUrl(origin, {})).toBe(origin)
+    }
+  })
+
+  it('still refuses plaintext to a public host', () => {
+    expect(() => resolveArtifactCloudApiUrl('http://share.onorca.dev', {})).toThrow(/HTTPS/)
+    expect(() => resolveArtifactCloudApiUrl('http://example.com', {})).toThrow(/HTTPS/)
   })
 
   it('rejects origins that could receive an Orca access token', () => {
-    expect(() => resolveArtifactCloudApiUrl('https://example.com', {}, false)).toThrow(
-      /onorca\.dev/
-    )
-    expect(() => resolveArtifactCloudApiUrl('https://share.onorca.dev/path', {}, false)).toThrow(
-      /origin/
-    )
+    expect(() => resolveArtifactCloudApiUrl('https://example.com', {})).toThrow(/onorca\.dev/)
+    expect(() => resolveArtifactCloudApiUrl('https://share.onorca.dev/path', {})).toThrow(/origin/)
+  })
+
+  it('does not accept a public host that merely looks private', () => {
+    // A prefix check would admit every one of these.
+    for (const origin of [
+      'https://10.0.0.1.evil.com',
+      'https://192.168.1.5.evil.com',
+      'https://172.16.0.1.attacker.net',
+      'https://127.0.0.1.evil.com',
+      'https://notlocal'
+    ]) {
+      expect(() => resolveArtifactCloudApiUrl(origin, {})).toThrow(/onorca\.dev/)
+    }
+  })
+
+  it('does not treat public IPv4 just outside the RFC1918 blocks as private', () => {
+    // 172.15 and 172.32 bracket the 172.16–172.31 block.
+    for (const origin of [
+      'https://172.15.0.1',
+      'https://172.32.0.1',
+      'https://11.0.0.1',
+      'https://192.169.1.1'
+    ]) {
+      expect(() => resolveArtifactCloudApiUrl(origin, {})).toThrow(/onorca\.dev/)
+    }
+  })
+
+  it('rejects a malformed octet before the host is ever classified', () => {
+    expect(() => resolveArtifactCloudApiUrl('https://999.168.1.1', {})).toThrow(/Invalid URL/)
   })
 
   it('allows auth token overrides only in non-production development builds', () => {
diff --git a/src/main/artifacts/artifact-cloud-config.ts b/src/main/artifacts/artifact-cloud-config.ts
index 6ff5af72..18593636 100644
--- a/src/main/artifacts/artifact-cloud-config.ts
+++ b/src/main/artifacts/artifact-cloud-config.ts
@@ -2,6 +2,8 @@ import { app } from 'electron'
 
 const PRODUCTION_ARTIFACTS_API_URL = 'https://share.onorca.dev'
 
+const LOOPBACK_HOSTNAMES = ['127.0.0.1', 'localhost', '[::1]']
+
 function isPackaged(): boolean {
   try {
     return app?.isPackaged === true
@@ -10,20 +12,44 @@ function isPackaged(): boolean {
   }
 }
 
+/** Parsed rather than prefix-matched: `10.0.0.1.evil.com` starts with a private range. */
+function isPrivateIpv4(hostname: string): boolean {
+  const parts = hostname.split('.')
+  if (parts.length !== 4 || !parts.every((part) => /^\d{1,3}$/.test(part))) {
+    return false
+  }
+  const [a, b] = parts.map(Number)
+  if (parts.some((part) => Number(part) > 255)) {
+    return false
+  }
+  return a === 10 || (a === 192 && b === 168) || (a === 172 && b >= 16 && b <= 31)
+}
+
+/** Hosts that cannot be reached from outside the local link, so a self-hosted backend may live there. */
+function isPrivateHost(hostname: string): boolean {
+  return (
+    LOOPBACK_HOSTNAMES.includes(hostname) ||
+    isPrivateIpv4(hostname) ||
+    // `.local` is reserved for mDNS (RFC 6762) and never resolves off-link.
+    hostname.endsWith('.local')
+  )
+}
+
 export function resolveArtifactCloudApiUrl(
   override?: string,
-  env: NodeJS.ProcessEnv = process.env,
-  packaged = isPackaged()
+  env: NodeJS.ProcessEnv = process.env
 ): string {
   const candidate = override?.trim() || env.ORCA_ARTIFACTS_API_URL?.trim()
   const url = new URL(candidate || PRODUCTION_ARTIFACTS_API_URL)
-  const loopback = ['127.0.0.1', 'localhost', '[::1]'].includes(url.hostname)
+  const privateHost = isPrivateHost(url.hostname)
   const firstParty = url.hostname === 'onorca.dev' || url.hostname.endsWith('.onorca.dev')
-  if (url.protocol !== 'https:' && !(url.protocol === 'http:' && loopback && !packaged)) {
-    throw new Error('Artifact API URLs must use HTTPS; local development may use loopback HTTP.')
+  // A self-hosted backend may use plaintext so it needs no certificate. The bearer sent to it is
+  // an Orca account credential, so this is limited to hosts that cannot be reached off-link.
+  if (url.protocol !== 'https:' && !(url.protocol === 'http:' && privateHost)) {
+    throw new Error('Artifact API URLs must use HTTPS; only a private LAN backend may use HTTP.')
   }
-  if (!firstParty && !loopback) {
-    throw new Error('Artifact API URLs must use an onorca.dev or loopback host.')
+  if (!firstParty && !privateHost) {
+    throw new Error('Artifact API URLs must use an onorca.dev host or a private LAN address.')
   }
   if (url.username || url.password || url.search || url.hash || url.pathname !== '/') {
     throw new Error('Artifact API URL must be an origin without credentials, paths, or parameters.')
diff --git a/src/main/index.ts b/src/main/index.ts
index 2e816ba4..18252ad7 100644
--- a/src/main/index.ts
+++ b/src/main/index.ts
@@ -48,6 +48,7 @@ import {
 import { closeAllWatchers } from './ipc/filesystem-watcher'
 import { disposeWorktreeBaseDirectoryWatchers } from './ipc/worktree-base-directory-watcher'
 import { registerCoreHandlers } from './ipc/register-core-handlers'
+import { registerIssueClientDataHandlers } from './ipc/issues'
 import { initObservability, shutdownObservability } from './observability'
 import { registerMobileHandlers } from './ipc/mobile'
 import { initTelemetry, shutdownTelemetry, trackAppOpenedOnce, track } from './telemetry/client'
@@ -69,6 +70,17 @@ import { initOnboardingCohortClassifier } from './telemetry/onboarding-cohort-cl
 import { resolveConsent } from './telemetry/consent'
 import { triggerStartupNotificationRegistration } from './ipc/notifications'
 import { OrcaRuntimeService, type RuntimeWorktreeLifecycleEvent } from './runtime/orca-runtime'
+import {
+  ActiveProfileIssueRepository,
+  ConversationHookIdentityIngestor,
+  createProductionIssueDigestGenerationExecutor,
+  createIssueRuntimeService,
+  IssueClientDataService,
+  RoundRecordIngestor,
+  RoundRecordReconciler
+} from './issues'
+import { getSshFilesystemProvider } from './providers/ssh-filesystem-dispatch'
+import { getLocalWorktreeCanonicalPathAccess } from './local-worktree-filesystem'
 import { ArtifactCloudService } from './artifacts/artifact-cloud-service'
 import { isArtifactSharingEnabled } from '../shared/artifact-sharing-gate'
 import { loadAgentSessionClaimSigner } from './runtime/agent-session-claim-identity'
@@ -284,7 +296,7 @@ import { normalizeComputerAwakeMode } from '../shared/computer-awake-mode'
 import { registerSystemResumeBroadcast } from './system-resume-broadcast'
 import { settleTeardownWithinDeadline } from './quit-teardown-deadline'
 import { quitTeardownStartGate } from './quit-teardown-start-gate'
-import { beginSshShutdown } from './ipc/ssh'
+import { beginSshShutdown, listRegisteredSshTargets } from './ipc/ssh'
 import { PluginService } from './plugins/plugin-service'
 import { PluginKillListService } from './plugins/plugin-kill-list-service'
 import { getPluginsDataDir } from './plugins/plugin-discovery'
@@ -295,6 +307,7 @@ import { resolveBundledPluginRoot } from './plugins/plugin-bundled-bootstrap'
 import { resolvePluginHostEntryPath } from './plugins/plugin-host-process'
 import { applyPluginConsent, applyPluginEnablement } from './plugins/plugin-enablement'
 import { setPluginServiceForRpc } from './runtime/rpc/methods/plugins'
+import { setIssueRuntimeServiceForRpc } from './runtime/rpc/methods/issues'
 import {
   normalizePluginConsents,
   normalizePluginIdList
@@ -358,6 +371,8 @@ let codexSessionMigration: ReturnType<typeof createCodexSessionMigrationSchedule
 let claudeAccounts: ClaudeAccountService | null = null
 let claudeRuntimeAuth: ClaudeRuntimeAuthService | null = null
 let runtime: OrcaRuntimeService | null = null
+let activeProfileIssueRepository: ActiveProfileIssueRepository | null = null
+let stopIssueFactsBroadcast: (() => void) | null = null
 let rateLimits: RateLimitService | null = null
 let runtimeRpc: OrcaRuntimeRpcServer | null = null
 const serveReadinessPublisher = new ServeReadinessPublisher()
@@ -2171,6 +2186,35 @@ void app.whenReady().then(async () => {
 
   const activeOrcaProfile = ensureActiveOrcaProfile()
   store = new Store({ dataFile: activeOrcaProfile.dataFile })
+  activeProfileIssueRepository = ActiveProfileIssueRepository.open({
+    profileId: activeOrcaProfile.profile.id,
+    userDataPath: app.getPath('userData')
+  })
+  const issueRuntimeService = createIssueRuntimeService({
+    repository: activeProfileIssueRepository.repository,
+    store,
+    isManagedSshTarget: (targetId) =>
+      listRegisteredSshTargets().some((target) => target.id === targetId),
+    digestExecutor: createProductionIssueDigestGenerationExecutor({
+      store,
+      getAgentEnvironmentResolvers: () =>
+        codexRuntimeHome && claudeRuntimeAuth
+          ? {
+              prepareForCodexLaunch: prepareCodexRuntimeHomeForLaunch,
+              prepareForClaudeLaunch: (target) =>
+                claudeRuntimeAuth!.prepareForClaudeLaunch(target)
+            }
+          : undefined
+    })
+  })
+  stopIssueFactsBroadcast = registerIssueClientDataHandlers(
+    new IssueClientDataService(
+      activeOrcaProfile.profile.id,
+      app.getPath('userData')
+    ),
+    issueRuntimeService
+  )
+  setIssueRuntimeServiceForRpc(issueRuntimeService)
   // Why: must precede PTY handler registration and run in headless serve too, which returns before openMainWindow.
   neutralizeLegacyTerminalShimDir(app.getPath('userData'))
   const windowsShellPathHydration = createWindowsShellPathHydration()
@@ -2299,6 +2343,82 @@ void app.whenReady().then(async () => {
       publishProviderSessionChanges(sessions)
     }
   )
+  const conversationIdentityIngestor = new ConversationHookIdentityIngestor(
+    activeProfileIssueRepository.repository,
+    {
+      resolveContext: (paneKey, expectedWorktreeId) =>
+        runtime?.resolveConversationHookIdentityContext(paneKey, expectedWorktreeId) ??
+        Promise.resolve(null),
+      resolvePathAccess: (context) => {
+        if (context.connectionId) {
+          const provider = getSshFilesystemProvider(context.connectionId)
+          return provider
+            ? {
+                platform: context.hostPlatform,
+                realpath: (path: string) => provider.realpath(path),
+                stat: (path: string) => provider.stat(path)
+              }
+            : null
+        }
+        return context.wslDistro
+          ? getLocalWorktreeCanonicalPathAccess({ wslDistro: context.wslDistro })
+          : null
+      },
+      onDiagnostic: (result, event) => {
+        if (result.disposition === 'ignored' && result.reason !== 'identity-missing') {
+          console.warn('[issues] Conversation identity event was not persisted', {
+            paneKey: event.paneKey,
+            reason: result.reason
+          })
+        }
+      }
+    }
+  )
+  const roundRecordReconciler = new RoundRecordReconciler(
+    activeProfileIssueRepository.repository,
+    {
+      onDiagnostic: (result) => {
+        if (
+          result.disposition === 'skipped' &&
+          result.reason !== 'transcript-not-found' &&
+          result.reason !== 'identity-missing'
+        ) {
+          console.warn('[issues] Round Record reconciliation was skipped', {
+            conversationId: result.conversationId,
+            reason: result.reason,
+            detail: result.detail
+          })
+        }
+      }
+    }
+  )
+  const roundRecordIngestor = new RoundRecordIngestor(
+    activeProfileIssueRepository.repository,
+    conversationIdentityIngestor,
+    {
+      onDiagnostic: (result, event) => {
+        if (result.disposition === 'ignored' && result.reason === 'conversation-unresolved') {
+          console.warn('[issues] Round Record event had no stable Conversation', {
+            paneKey: event.paneKey,
+            state: event.payload.state
+          })
+        }
+      },
+      scheduleReconciliation: (conversationId) => {
+        void roundRecordReconciler.schedule(conversationId).catch((error) => {
+          console.warn('[issues] Failed to reconcile Round Records', {
+            conversationId,
+            error
+          })
+        })
+      }
+    }
+  )
+  const unsubscribeRoundRecordIngest = agentHookServer.subscribeEnrichedStatus((enriched) => {
+    void roundRecordIngestor.ingest(enriched).catch((error) => {
+      console.warn('[issues] Failed to ingest Conversation or Round Record', error)
+    })
+  })
   // Why: hook rows are the only carrier of live agent state on a headless host, and
   // nothing else republishes `session.tabs` when one changes — so a paired client
   // would keep the pane's last projection until an unrelated PTY touch came along.
@@ -2323,6 +2443,7 @@ void app.whenReady().then(async () => {
   unsubscribeAgentAwakeStatusChanges = () => {
     unsubscribeStatusChanges()
     unsubscribeProviderSessionChanges()
+    unsubscribeRoundRecordIngest()
     unsubscribeHookStatusSessionTabs()
     unsubscribeHookStatusClear()
   }
@@ -2508,6 +2629,7 @@ void app.whenReady().then(async () => {
       )
   }
   const runtimeService = new OrcaRuntimeService(store, stats, {
+    conversationAllocator: activeProfileIssueRepository.conversationAllocator,
     agentSessionClaimSigner: loadAgentSessionClaimSigner(
       getProfileUserDataPath(),
       getProfileUserDataPath()
@@ -3234,6 +3356,11 @@ app.on('will-quit', (e) => {
   pluginService = null
   setUnreadDockBadgeCount(0)
   agentHookServer.stop()
+  stopIssueFactsBroadcast?.()
+  stopIssueFactsBroadcast = null
+  setIssueRuntimeServiceForRpc(null)
+  activeProfileIssueRepository?.close()
+  activeProfileIssueRepository = null
   // Why: cancels relay restart/reinstall timers and kills wsl.exe children deterministically, not via stdio-pipe teardown.
   wslHookRelayManager.disposeAll()
   const statsFlush = stats?.flushAsync() ?? Promise.resolve()
diff --git a/src/main/ipc/dashboard-payload-validation.test.ts b/src/main/ipc/dashboard-payload-validation.test.ts
index 83970cfa..9c422a15 100644
--- a/src/main/ipc/dashboard-payload-validation.test.ts
+++ b/src/main/ipc/dashboard-payload-validation.test.ts
@@ -18,6 +18,8 @@ vi.mock('../../shared/repo-icon', async (importOriginal) => {
 
 import {
   admitDashboardSnapshot,
+  isDashboardAckAgentArgs,
+  isDashboardOpenIssueArgs,
   isDashboardRevealAgentArgs,
   isDashboardSpawnAgentArgs,
   isDashboardSnapshot
@@ -287,6 +289,96 @@ describe('dashboard payload validation', () => {
     ).toBe(false)
   })
 
+  it('accepts a bounded optional Issue projection and rejects malformed variants', () => {
+    const issueContext = {
+      status: 'mapped' as const,
+      authority: {
+        authorityId: '11111111-1111-4111-8111-111111111111',
+        hostPartitionKey: 'local',
+        routeExecutionHostId: 'local'
+      },
+      conversationId: '22222222-2222-4222-8222-222222222222',
+      issue: {
+        id: '33333333-3333-4333-8333-333333333333',
+        title: 'Dashboard integration',
+        typeLabel: null,
+        state: 'active' as const
+      },
+      latestRound: {
+        id: '44444444-4444-4444-8444-444444444444',
+        readAt: null
+      }
+    }
+    expect(
+      isDashboardSnapshot({
+        ...SNAPSHOT,
+        cards: [{ ...SNAPSHOT.cards[0], issueContext }]
+      })
+    ).toBe(true)
+    expect(
+      isDashboardSnapshot({
+        ...SNAPSHOT,
+        cards: [
+          {
+            ...SNAPSHOT.cards[0],
+            issueContext: {
+              ...issueContext,
+              issue: { ...issueContext.issue, title: 'x'.repeat(1_025) }
+            }
+          }
+        ]
+      })
+    ).toBe(false)
+    expect(
+      isDashboardSnapshot({
+        ...SNAPSHOT,
+        cards: [
+          {
+            ...SNAPSHOT.cards[0],
+            issueContext: { status: 'unsupported', extra: true }
+          }
+        ]
+      })
+    ).toBe(false)
+  })
+
+  it('validates optional-host acknowledgements and Issue navigation payloads', () => {
+    expect(isDashboardAckAgentArgs({ paneKey: 'pane-1' })).toBe(true)
+    expect(
+      isDashboardAckAgentArgs({
+        paneKey: 'pane-1',
+        executionHostId: 'runtime:env-1'
+      })
+    ).toBe(true)
+    expect(
+      isDashboardAckAgentArgs({
+        paneKey: 'pane-1',
+        executionHostId: 'env-1'
+      })
+    ).toBe(false)
+
+    expect(
+      isDashboardOpenIssueArgs({
+        authority: {
+          authorityId: '11111111-1111-4111-8111-111111111111',
+          hostPartitionKey: 'local',
+          routeExecutionHostId: 'local'
+        },
+        issueId: '22222222-2222-4222-8222-222222222222'
+      })
+    ).toBe(true)
+    expect(
+      isDashboardOpenIssueArgs({
+        authority: {
+          authorityId: '',
+          hostPartitionKey: 'local',
+          routeExecutionHostId: 'local'
+        },
+        issueId: '22222222-2222-4222-8222-222222222222'
+      })
+    ).toBe(false)
+  })
+
   it('validates the preview terminal input profile', () => {
     const terminalInput = {
       hostPlatform: 'win32',
diff --git a/src/main/ipc/dashboard-payload-validation.ts b/src/main/ipc/dashboard-payload-validation.ts
index 6186e638..86ebd0d7 100644
--- a/src/main/ipc/dashboard-payload-validation.ts
+++ b/src/main/ipc/dashboard-payload-validation.ts
@@ -1,5 +1,7 @@
 import {
   DASHBOARD_MAX_LABEL_LENGTH,
+  type DashboardAckAgentArgs,
+  type DashboardOpenIssueArgs,
   type DashboardRevealAgentArgs,
   type DashboardSleepWorkspaceArgs,
   type DashboardSnapshot
@@ -66,6 +68,49 @@ function isFiniteNumber(value: unknown): value is number {
   return typeof value === 'number' && Number.isFinite(value)
 }
 
+function isExecutionHostId(value: unknown): boolean {
+  return (
+    isBoundedString(value, MAX_ID_LENGTH) &&
+    normalizeExecutionHostId(value) !== null
+  )
+}
+
+export function isDashboardAckAgentArgs(
+  value: unknown
+): value is DashboardAckAgentArgs {
+  if (!value || typeof value !== 'object' || Array.isArray(value)) {
+    return false
+  }
+  const args = value as Record<string, unknown>
+  return (
+    isDashboardPaneKey(args.paneKey) &&
+    (args.executionHostId === undefined || isExecutionHostId(args.executionHostId))
+  )
+}
+
+export function isDashboardOpenIssueArgs(
+  value: unknown
+): value is DashboardOpenIssueArgs {
+  if (!value || typeof value !== 'object' || Array.isArray(value)) {
+    return false
+  }
+  const args = value as Record<string, unknown>
+  if (
+    !isBoundedString(args.issueId, MAX_ID_LENGTH) ||
+    !args.authority ||
+    typeof args.authority !== 'object' ||
+    Array.isArray(args.authority)
+  ) {
+    return false
+  }
+  const authority = args.authority as Record<string, unknown>
+  return (
+    isBoundedString(authority.authorityId, MAX_ID_LENGTH) &&
+    isBoundedString(authority.hostPartitionKey, MAX_ID_LENGTH) &&
+    isExecutionHostId(authority.routeExecutionHostId)
+  )
+}
+
 export function isDashboardRevealAgentArgs(value: unknown): value is DashboardRevealAgentArgs {
   if (!value || typeof value !== 'object' || Array.isArray(value)) {
     return false
@@ -74,9 +119,7 @@ export function isDashboardRevealAgentArgs(value: unknown): value is DashboardRe
   return (
     isBoundedString(args.repoId, MAX_ID_LENGTH) &&
     isBoundedString(args.worktreeId, MAX_ID_LENGTH) &&
-    (args.executionHostId === undefined ||
-      (isBoundedString(args.executionHostId, MAX_ID_LENGTH) &&
-        normalizeExecutionHostId(args.executionHostId) !== null)) &&
+    (args.executionHostId === undefined || isExecutionHostId(args.executionHostId)) &&
     isBoundedString(args.tabId, MAX_ID_LENGTH) &&
     (args.leafId === null || isBoundedString(args.leafId, MAX_ID_LENGTH))
   )
@@ -269,9 +312,7 @@ function isDashboardCard(value: unknown): boolean {
     isBoundedString(card.worktreeName, MAX_LABEL_LENGTH, true) &&
     (card.hostKind === undefined ||
       (typeof card.hostKind === 'string' && DASHBOARD_HOST_KINDS.has(card.hostKind))) &&
-    (card.executionHostId === undefined ||
-      (isBoundedString(card.executionHostId, MAX_ID_LENGTH) &&
-        normalizeExecutionHostId(card.executionHostId) !== null)) &&
+    (card.executionHostId === undefined || isExecutionHostId(card.executionHostId)) &&
     isOptionalBoundedString(card.hostLabel, MAX_LABEL_LENGTH) &&
     (card.workspaceKind === undefined ||
       (typeof card.workspaceKind === 'string' &&
@@ -287,12 +328,80 @@ function isDashboardCard(value: unknown): boolean {
     isFiniteNumber(card.stateChangedAt) &&
     (card.statusUpdatedAt === undefined || isFiniteNumber(card.statusUpdatedAt)) &&
     typeof card.unseen === 'boolean' &&
+    isDashboardIssueContext(card.issueContext) &&
     isOptionalBoundedString(card.askSummary, AGENT_STATUS_INTERACTIVE_PROMPT_MAX_LENGTH) &&
     isOptionalBoundedString(card.conversationName, MAX_LABEL_LENGTH) &&
     isDashboardTerminalInput(card.terminalInput)
   )
 }
 
+function isDashboardIssueContext(value: unknown): boolean {
+  if (value === undefined) {
+    return true
+  }
+  if (!value || typeof value !== 'object' || Array.isArray(value)) {
+    return false
+  }
+  const context = value as Record<string, unknown>
+  if (
+    context.status === 'unmapped' ||
+    context.status === 'unsupported' ||
+    context.status === 'unavailable'
+  ) {
+    return Object.keys(context).length === 1
+  }
+  if (
+    context.status !== 'mapped' ||
+    !context.authority ||
+    typeof context.authority !== 'object' ||
+    Array.isArray(context.authority) ||
+    !isBoundedString(context.conversationId, MAX_ID_LENGTH)
+  ) {
+    return false
+  }
+  const authority = context.authority as Record<string, unknown>
+  if (
+    !isBoundedString(authority.authorityId, MAX_ID_LENGTH) ||
+    !isBoundedString(authority.hostPartitionKey, MAX_ID_LENGTH) ||
+    !isExecutionHostId(authority.routeExecutionHostId)
+  ) {
+    return false
+  }
+  if (
+    context.issue !== null &&
+    (!context.issue ||
+      typeof context.issue !== 'object' ||
+      Array.isArray(context.issue) ||
+      !isDashboardIssue(context.issue as Record<string, unknown>))
+  ) {
+    return false
+  }
+  return (
+    context.latestRound === null ||
+    (Boolean(context.latestRound) &&
+      typeof context.latestRound === 'object' &&
+      !Array.isArray(context.latestRound) &&
+      isBoundedString(
+        (context.latestRound as Record<string, unknown>).id,
+        MAX_ID_LENGTH
+      ) &&
+      ((context.latestRound as Record<string, unknown>).readAt === null ||
+        isFiniteNumber(
+          (context.latestRound as Record<string, unknown>).readAt
+        )))
+  )
+}
+
+function isDashboardIssue(issue: Record<string, unknown>): boolean {
+  return (
+    isBoundedString(issue.id, MAX_ID_LENGTH) &&
+    isBoundedString(issue.title, MAX_LABEL_LENGTH, true) &&
+    (issue.typeLabel === null ||
+      isBoundedString(issue.typeLabel, MAX_LABEL_LENGTH, true)) &&
+    (issue.state === 'active' || issue.state === 'archived')
+  )
+}
+
 function isDashboardTerminalInput(value: unknown): boolean {
   if (value === undefined) {
     return true
diff --git a/src/main/ipc/dashboard-popout.test.ts b/src/main/ipc/dashboard-popout.test.ts
index ce10b355..ef1cb054 100644
--- a/src/main/ipc/dashboard-popout.test.ts
+++ b/src/main/ipc/dashboard-popout.test.ts
@@ -247,6 +247,49 @@ describe('registerDashboardPopoutHandlers', () => {
       paneKey: 'tab1:leaf1'
     })
     expect(sendToTrustedMock).toHaveBeenCalledWith('ui:ackDashboardAgent', 'tab1:leaf1')
+
+    handlers.get('dashboardPopout:ackAgent')!({ sender: popoutSender } as never, {
+      paneKey: 'tab2:leaf2',
+      executionHostId: 'runtime:env-1'
+    })
+    expect(sendToTrustedMock).toHaveBeenCalledWith('ui:ackDashboardAgent', {
+      paneKey: 'tab2:leaf2',
+      executionHostId: 'runtime:env-1'
+    })
+  })
+
+  it('routes a valid Issue chip click only from the popout', () => {
+    getTrustedWindowMock.mockReturnValue(makeWindow(mainSender))
+    const args = {
+      authority: {
+        authorityId: '11111111-1111-4111-8111-111111111111',
+        hostPartitionKey: 'local',
+        routeExecutionHostId: 'local'
+      },
+      issueId: '22222222-2222-4222-8222-222222222222'
+    }
+
+    handlers.get('dashboardPopout:openIssue')!(
+      { sender: untrustedSender } as never,
+      args
+    )
+    handlers.get('dashboardPopout:openIssue')!(
+      { sender: popoutSender } as never,
+      { ...args, issueId: '' }
+    )
+    expect(mainSender.send).not.toHaveBeenCalledWith(
+      'ui:openDashboardIssue',
+      expect.anything()
+    )
+
+    handlers.get('dashboardPopout:openIssue')!(
+      { sender: popoutSender } as never,
+      args
+    )
+    expect(mainSender.send).toHaveBeenCalledWith(
+      'ui:openDashboardIssue',
+      args
+    )
   })
 
   it('relays only valid agent launches from the popout', () => {
diff --git a/src/main/ipc/dashboard-popout.ts b/src/main/ipc/dashboard-popout.ts
index 5b4781ec..5d0f9cdd 100644
--- a/src/main/ipc/dashboard-popout.ts
+++ b/src/main/ipc/dashboard-popout.ts
@@ -13,7 +13,8 @@ import { safelyRevealWindow } from '../window/focus-existing-window'
 import { getTrustedUIRendererWindow, isTrustedUIRenderer, sendToTrustedUIRenderer } from './ui'
 import {
   admitDashboardSnapshot,
-  isDashboardPaneKey,
+  isDashboardAckAgentArgs,
+  isDashboardOpenIssueArgs,
   isDashboardRevealAgentArgs,
   isDashboardSleepWorkspaceArgs,
   isDashboardSpawnAgentArgs
@@ -38,6 +39,7 @@ export function registerDashboardPopoutHandlers(
   ipcMain.removeHandler('dashboard:getPopoutOpen')
   ipcMain.removeHandler('dashboardPopout:revealAgent')
   ipcMain.removeHandler('dashboardPopout:ackAgent')
+  ipcMain.removeHandler('dashboardPopout:openIssue')
   ipcMain.removeHandler('dashboardPopout:spawnAgent')
   ipcMain.removeHandler('dashboardPopout:sleepWorkspace')
 
@@ -118,13 +120,35 @@ export function registerDashboardPopoutHandlers(
     if (
       !isDashboardPopoutRenderer(event.sender) ||
       !isDashboardEnabled(store) ||
-      !args ||
-      typeof args !== 'object' ||
-      !isDashboardPaneKey((args as { paneKey?: unknown }).paneKey)
+      !isDashboardAckAgentArgs(args)
     ) {
       return
     }
-    sendToTrustedUIRenderer('ui:ackDashboardAgent', (args as { paneKey: string }).paneKey)
+    sendToTrustedUIRenderer(
+      'ui:ackDashboardAgent',
+      args.executionHostId ? args : args.paneKey
+    )
+  })
+
+  ipcMain.handle('dashboardPopout:openIssue', (event, args: unknown): void => {
+    if (
+      !isDashboardPopoutRenderer(event.sender) ||
+      !isDashboardEnabled(store) ||
+      !isDashboardOpenIssueArgs(args)
+    ) {
+      return
+    }
+    const mainWindow = getTrustedUIRendererWindow()
+    if (!mainWindow) {
+      return
+    }
+    safelyRevealWindow(mainWindow)
+    mainWindow.webContents.send('ui:openDashboardIssue', args)
+    try {
+      app.focus({ steal: true })
+    } catch {
+      // Best-effort; the per-window focus above may still bring it forward.
+    }
   })
 
   // Click-to-focus: raise the main window and route it to the agent's pane.
diff --git a/src/main/ipc/notification-options.ts b/src/main/ipc/notification-options.ts
index a2553f05..d5aa6cf1 100644
--- a/src/main/ipc/notification-options.ts
+++ b/src/main/ipc/notification-options.ts
@@ -128,6 +128,14 @@ function buildAgentTaskCompleteFallbackNotificationOptions(args: NotificationDis
   title: string
   body: string
 } {
+  if (args.source === 'agent-needs-input') {
+    return {
+      title: `Input needed in ${args.worktreeLabel ?? 'workspace'}`,
+      body: args.repoLabel
+        ? `${args.repoLabel}${args.terminalTitle ? ` · ${args.terminalTitle}` : ''}`
+        : (args.terminalTitle ?? 'A coding agent needs your input.')
+    }
+  }
   return {
     title: `Task complete in ${args.worktreeLabel ?? 'workspace'}`,
     body: buildAgentTaskCompleteFallbackBody(args)
diff --git a/src/main/ipc/notifications.test.ts b/src/main/ipc/notifications.test.ts
index b7cb9dd5..91776a55 100644
--- a/src/main/ipc/notifications.test.ts
+++ b/src/main/ipc/notifications.test.ts
@@ -318,6 +318,116 @@ describe('registerNotificationHandlers', () => {
     expect(notificationCtorMock).not.toHaveBeenCalled()
   })
 
+  it('does not suppress a hidden pane just because its worktree is active', async () => {
+    getAllWindowsMock.mockReturnValue([
+      {
+        isDestroyed: () => false,
+        isFocused: () => true
+      } as never
+    ])
+
+    registerNotificationHandlers({
+      getSettings: () => ({
+        notifications: {
+          enabled: true,
+          agentTaskComplete: true,
+          terminalBell: true,
+          suppressWhenFocused: true
+        }
+      })
+    } as never)
+
+    const handler = getDispatchHandler()
+    expect(
+      await handler(
+        {},
+        {
+          source: 'agent-task-complete',
+          isActiveWorktree: true,
+          isVisibleForegroundPane: false,
+          isVisibleRoundRecord: false
+        }
+      )
+    ).toEqual({ delivered: true })
+    expect(notificationCtorMock).toHaveBeenCalledTimes(1)
+  })
+
+  it.each([
+    [{ isVisibleForegroundPane: true, isVisibleRoundRecord: false }, 'pane'],
+    [{ isVisibleForegroundPane: false, isVisibleRoundRecord: true }, 'round']
+  ] as const)(
+    'suppresses a focused notification when its exact %s is visible',
+    async (exact, _target) => {
+    getAllWindowsMock.mockReturnValue([
+      {
+        isDestroyed: () => false,
+        isFocused: () => true
+      } as never
+    ])
+
+    registerNotificationHandlers({
+      getSettings: () => ({
+        notifications: {
+          enabled: true,
+          agentTaskComplete: true,
+          terminalBell: true,
+          suppressWhenFocused: true
+        }
+      })
+    } as never)
+
+    const handler = getDispatchHandler()
+    expect(
+      await handler({}, { source: 'agent-task-complete', isActiveWorktree: false, ...exact })
+    ).toEqual({
+      delivered: false,
+      reason: 'suppressed-focus'
+    })
+    expect(notificationCtorMock).not.toHaveBeenCalled()
+    }
+  )
+
+  it('forwards Issue notification locators to mobile delivery', async () => {
+    const dispatchMobileNotification = vi.fn()
+    registerNotificationHandlers(
+      {
+        getSettings: () => ({
+          notifications: {
+            enabled: true,
+            agentTaskComplete: true,
+            terminalBell: true,
+            suppressWhenFocused: false
+          }
+        })
+      } as never,
+      { dispatchMobileNotification } as never
+    )
+
+    const handler = getDispatchHandler()
+    await handler(
+      {},
+      {
+        source: 'agent-needs-input',
+        worktreeId: 'repo::wt1',
+        executionHostId: 'ssh:docs',
+        conversationId: '33333333-3333-4333-8333-333333333333',
+        issueId: '22222222-2222-4222-8222-222222222222',
+        roundRecordId: '44444444-4444-4444-8444-444444444444'
+      }
+    )
+
+    expect(dispatchMobileNotification).toHaveBeenCalledWith(
+      expect.objectContaining({
+        source: 'agent-needs-input',
+        worktreeId: 'repo::wt1',
+        executionHostId: 'ssh:docs',
+        conversationId: '33333333-3333-4333-8333-333333333333',
+        issueId: '22222222-2222-4222-8222-222222222222',
+        roundRecordId: '44444444-4444-4444-8444-444444444444'
+      })
+    )
+  })
+
   describe('minimized tray attention dot', () => {
     function registerEnabledNotifications(): void {
       registerNotificationHandlers({
@@ -529,6 +639,64 @@ describe('registerNotificationHandlers', () => {
     })
   })
 
+  it('routes an Issue notification click through the renderer locator without racing legacy focus', async () => {
+    const webContentsSend = vi.fn()
+    const mainWindow = {
+      isDestroyed: () => false,
+      isFocused: () => false,
+      isMinimized: () => false,
+      restore: vi.fn(),
+      show: vi.fn(),
+      focus: vi.fn(),
+      webContents: { send: webContentsSend }
+    }
+    getAllWindowsMock.mockReturnValue([mainWindow] as never)
+    getTrustedUIRendererWindowMock.mockReturnValue(mainWindow)
+    registerNotificationHandlers({
+      getSettings: () => ({
+        notifications: {
+          enabled: true,
+          agentTaskComplete: true,
+          terminalBell: true,
+          suppressWhenFocused: false
+        }
+      })
+    } as never)
+
+    const handler = getDispatchHandler()
+    await handler(
+      {},
+      {
+        source: 'agent-needs-input',
+        worktreeId: 'repo::wt1',
+        paneKey: 'tab-1:11111111-1111-4111-8111-111111111111',
+        executionHostId: 'ssh:docs',
+        conversationId: '33333333-3333-4333-8333-333333333333',
+        issueId: '22222222-2222-4222-8222-222222222222',
+        roundRecordId: '44444444-4444-4444-8444-444444444444'
+      }
+    )
+
+    getNotificationEventHandler('click')()
+
+    expect(webContentsSend).toHaveBeenCalledWith('ui:openIssueNotification', {
+      executionHostId: 'ssh:docs',
+      conversationId: '33333333-3333-4333-8333-333333333333',
+      issueId: '22222222-2222-4222-8222-222222222222',
+      roundRecordId: '44444444-4444-4444-8444-444444444444',
+      worktreeId: 'repo::wt1',
+      paneKey: 'tab-1:11111111-1111-4111-8111-111111111111'
+    })
+    expect(webContentsSend).not.toHaveBeenCalledWith(
+      'ui:activateWorktree',
+      expect.anything()
+    )
+    expect(webContentsSend).not.toHaveBeenCalledWith(
+      'ui:focusTerminal',
+      expect.anything()
+    )
+  })
+
   it('clears the retained notification fallback timer when the native notification closes', async () => {
     registerNotificationHandlers({
       getSettings: () => ({
diff --git a/src/main/ipc/notifications.ts b/src/main/ipc/notifications.ts
index 26d87f8f..88bc9711 100644
--- a/src/main/ipc/notifications.ts
+++ b/src/main/ipc/notifications.ts
@@ -29,6 +29,7 @@ import { parsePaneKey } from '../../shared/stable-pane-id'
 import { setTrayAttention } from '../tray/system-tray'
 import { isMainWindowVisible } from '../window/main-window-visibility'
 import { getTrustedUIRendererWindow } from './ui'
+import { issueNotificationClickTarget } from '../issues/issue-notification-click-target'
 
 const NOTIFICATION_COOLDOWN_MS = 5000
 const MAX_RECENT_NOTIFICATION_KEYS = 50
@@ -395,7 +396,11 @@ export function registerNotificationHandlers(store: Store, runtime?: OrcaRuntime
       args: NotificationDispatchRequest
     ): NotificationDispatchResult | Promise<NotificationDispatchResult> => {
       // Why: light the tray attention dot before the cooldown/focus/enabled gates so they can't hold it back (clears on window show/restore; see index.ts).
-      if (args.source === 'agent-task-complete' || args.source === 'terminal-bell') {
+      if (
+        args.source === 'agent-task-complete' ||
+        args.source === 'agent-needs-input' ||
+        args.source === 'terminal-bell'
+      ) {
         const activeWindow = BrowserWindow.getAllWindows().find((win) => !win.isDestroyed()) ?? null
         if (!isMainWindowVisible(activeWindow)) {
           setTrayAttention(true)
@@ -408,7 +413,8 @@ export function registerNotificationHandlers(store: Store, runtime?: OrcaRuntime
       }
 
       if (
-        (args.source === 'agent-task-complete' && !settings.agentTaskComplete) ||
+        ((args.source === 'agent-task-complete' || args.source === 'agent-needs-input') &&
+          !settings.agentTaskComplete) ||
         (args.source === 'terminal-bell' && !settings.terminalBell)
       ) {
         return { delivered: false, reason: 'source-disabled' }
@@ -426,6 +432,10 @@ export function registerNotificationHandlers(store: Store, runtime?: OrcaRuntime
             title: notificationOptions.title,
             body: notificationOptions.body,
             worktreeId: args.worktreeId,
+            ...(args.executionHostId ? { executionHostId: args.executionHostId } : {}),
+            ...(args.conversationId ? { conversationId: args.conversationId } : {}),
+            ...(args.issueId ? { issueId: args.issueId } : {}),
+            ...(args.roundRecordId ? { roundRecordId: args.roundRecordId } : {}),
             ...(args.notificationId ? { notificationId: args.notificationId } : {})
           })
         }
@@ -433,11 +443,16 @@ export function registerNotificationHandlers(store: Store, runtime?: OrcaRuntime
 
       const browserWindow =
         BrowserWindow.getAllWindows().find((window) => !window.isDestroyed()) ?? null
+      const hasExactVisibility =
+        args.isVisibleForegroundPane !== undefined ||
+        args.isVisibleRoundRecord !== undefined
+      const isExactTargetVisible =
+        args.isVisibleForegroundPane === true || args.isVisibleRoundRecord === true
+      const isLegacyTargetVisible = !hasExactVisibility && args.isActiveWorktree === true
       if (
         settings.suppressWhenFocused &&
-        args.isActiveWorktree &&
-        browserWindow &&
-        browserWindow.isFocused()
+        (isExactTargetVisible || isLegacyTargetVisible) &&
+        browserWindow?.isFocused()
       ) {
         return { delivered: false, reason: 'suppressed-focus' }
       }
@@ -508,9 +523,10 @@ export function registerNotificationHandlers(store: Store, runtime?: OrcaRuntime
         }
         notification.on('failed', failedHandler)
 
-        // Why: worktreeId is formatted "repoId::worktreePath"; without the separator we can't extract a repoId, so skip the click-to-navigate binding.
-        if (args.worktreeId && args.worktreeId.includes('::')) {
-          const repoId = getRepoIdFromWorktreeId(args.worktreeId)
+        const issueClickTarget = issueNotificationClickTarget(args)
+        const legacyWorktreeId =
+          args.worktreeId?.includes('::') ? args.worktreeId : null
+        if (issueClickTarget || legacyWorktreeId) {
           clickHandler = () => {
             release()
             const win = getTrustedUIRendererWindow()
@@ -525,16 +541,24 @@ export function registerNotificationHandlers(store: Store, runtime?: OrcaRuntime
             }
             win.show()
             win.focus()
+            if (issueClickTarget) {
+              win.webContents.send(
+                'ui:openIssueNotification',
+                issueClickTarget
+              )
+              return
+            }
+            const repoId = getRepoIdFromWorktreeId(legacyWorktreeId!)
             win.webContents.send('ui:activateWorktree', {
               repoId,
-              worktreeId: args.worktreeId
+              worktreeId: legacyWorktreeId
             })
             // Why: focusTerminal targets the pane by stable leafId so split-pane notifications land on the exact pane.
             const paneTarget = args.paneKey ? parsePaneKey(args.paneKey) : null
             if (paneTarget) {
               win.webContents.send('ui:focusTerminal', {
                 tabId: paneTarget.tabId,
-                worktreeId: args.worktreeId,
+                worktreeId: legacyWorktreeId,
                 leafId: paneTarget.leafId,
                 ackPaneKeyOnSuccess: args.paneKey,
                 flashFocusedPane: true,
diff --git a/src/main/ipc/pty.test.ts b/src/main/ipc/pty.test.ts
index 5c151667..4c0281a2 100644
--- a/src/main/ipc/pty.test.ts
+++ b/src/main/ipc/pty.test.ts
@@ -16,6 +16,8 @@ import type { AgentSessionOwnerBinding } from '../../shared/agent-session-host-a
 import { AGENT_SESSION_CLAIM_DIGEST_VERSION } from '../../shared/agent-session-host-authority'
 import { PtyWriteUnavailableError } from '../providers/pty-write-unavailable-error'
 import { TerminalSessionOwnerUnverifiedError } from '../daemon/daemon-errors'
+import { IssueDatabase } from '../issues/issue-database'
+import { IssueRepository } from '../issues/issue-repository'
 import type * as Wsl from '../wsl'
 
 const isWindowsHost = process.platform === 'win32'
@@ -786,6 +788,38 @@ describe('registerPtyHandlers', () => {
     }
   }
 
+  function createRendererConversationProvider(
+    spawn: () => Promise<{ id: string; incarnationId?: string; isReattach?: boolean }>
+  ) {
+    return {
+      ...createAgentClaimProvider({ spawn: vi.fn(spawn), authoritativeOwnerListings: false }),
+      routesFreshSpawnsToLocalProvider: true
+    }
+  }
+
+  function createRendererConversationRuntime(repository: IssueRepository) {
+    const runtime = new OrcaRuntimeService(null, undefined, {
+      conversationAllocator: repository.conversationAllocator
+    })
+    const internal = runtime as unknown as {
+      resolveTerminalWorkspaceLaunchScope: ReturnType<typeof vi.fn>
+    }
+    internal.resolveTerminalWorkspaceLaunchScope = vi.fn(async () => ({
+      id: 'worktree-renderer',
+      path: '/tmp/worktree-renderer',
+      connectionId: null,
+      repo: null,
+      folderWorkspace: null,
+      managedWorktree: {
+        id: 'worktree-renderer',
+        displayName: 'Renderer worktree',
+        path: '/tmp/worktree-renderer',
+        hostId: 'local'
+      }
+    }))
+    return runtime
+  }
+
   const recoveredAgentClaim = {
     digestVersion: 1 as const,
     keyId: 'claim-key',
@@ -8281,6 +8315,194 @@ describe('registerPtyHandlers', () => {
     )
   })
 
+  it('persists an Issue-bound Conversation before a renderer agent spawn', async () => {
+    const repository = new IssueRepository(
+      IssueDatabase.openTransient({ logicalPath: 'pty-renderer-conversation-fresh' })
+    )
+    try {
+      const issue = repository.issues.create({
+        identity: { callerFingerprint: 'pty-test', mutationId: 'issue-1' },
+        input: { kind: 'local', executionHostId: 'local', title: 'Renderer launch' }
+      })
+      const tabId = 'tab-renderer-conversation'
+      const leafId = '12121212-1212-4212-8212-121212121212'
+      const paneKey = makePaneKey(tabId, leafId)
+      const launchToken = 'renderer-conversation-token'
+      const runtime = createRendererConversationRuntime(repository)
+      const provider = createRendererConversationProvider(async () => {
+        expect(repository.conversations.list()).toEqual([
+          expect.objectContaining({
+            issueId: issue.id,
+            workspaceRef: { type: 'worktree', worktreeId: 'worktree-renderer' },
+            state: 'active'
+          })
+        ])
+        return {
+          id: 'pty-renderer-conversation',
+          incarnationId: 'incarnation-renderer-conversation'
+        }
+      })
+      setLocalPtyProvider(provider as never)
+      registerPtyHandlers(mainWindow as never, runtime as never)
+
+      const result = (await handlers.get('pty:spawn')!(mainWindowIpcEvent, {
+        cols: 80,
+        rows: 24,
+        cwd: '/tmp/worktree-renderer',
+        command: 'codex',
+        worktreeId: 'worktree-renderer',
+        tabId,
+        leafId,
+        env: {
+          ORCA_PANE_KEY: paneKey,
+          ORCA_TAB_ID: tabId,
+          ORCA_WORKTREE_ID: 'worktree-renderer',
+          ORCA_AGENT_LAUNCH_TOKEN: launchToken
+        },
+        launchConfig: { agentCommand: 'codex', agentArgs: '', agentEnv: {} },
+        launchAgent: 'codex',
+        launchToken,
+        issueId: issue.id
+      })) as { id: string; conversationId?: string }
+
+      expect(result).toMatchObject({
+        id: 'pty-renderer-conversation',
+        conversationId: repository.conversations.list()[0]?.id
+      })
+      expect(
+        repository.database
+          .prepare('SELECT COUNT(*) AS count FROM conversation_launch_claims')
+          .get()
+      ).toEqual({ count: 1 })
+    } finally {
+      repository.close()
+    }
+  })
+
+  it('reuses a provider identity for renderer resume without changing its Issue', async () => {
+    const repository = new IssueRepository(
+      IssueDatabase.openTransient({ logicalPath: 'pty-renderer-conversation-resume' })
+    )
+    try {
+      const issue = repository.issues.create({
+        identity: { callerFingerprint: 'pty-test', mutationId: 'issue-1' },
+        input: { kind: 'local', executionHostId: 'local', title: 'Resume renderer' }
+      })
+      const seeded = repository.conversationAllocator.allocateFreshLaunch({
+        executionHostId: 'local',
+        workspaceRef: { type: 'worktree', worktreeId: 'worktree-renderer' },
+        workspaceSnapshot: { name: 'Renderer worktree', path: '/tmp/worktree-renderer' },
+        agent: 'codex',
+        issueId: issue.id,
+        launchToken: 'seed-resume-token'
+      })
+      const providerSession = { key: 'session_id' as const, id: 'provider-resume-1' }
+      repository.conversationAllocator.attachProviderIdentity({
+        executionHostId: 'local',
+        agent: 'codex',
+        providerSession,
+        launchToken: 'seed-resume-token'
+      })
+      const tabId = 'tab-renderer-resume'
+      const leafId = '13131313-1313-4313-8313-131313131313'
+      const paneKey = makePaneKey(tabId, leafId)
+      const launchToken = 'renderer-resume-token'
+      const runtime = createRendererConversationRuntime(repository)
+      const provider = createRendererConversationProvider(async () => {
+        expect(repository.conversations.list()).toEqual([
+          expect.objectContaining({ id: seeded.conversation.id, issueId: issue.id })
+        ])
+        expect(
+          repository.database
+            .prepare('SELECT COUNT(*) AS count FROM conversation_launch_claims')
+            .get()
+        ).toEqual({ count: 2 })
+        return {
+          id: 'pty-renderer-resume',
+          incarnationId: 'incarnation-renderer-resume'
+        }
+      })
+      setLocalPtyProvider(provider as never)
+      registerPtyHandlers(mainWindow as never, runtime as never)
+
+      const result = (await handlers.get('pty:spawn')!(mainWindowIpcEvent, {
+        cols: 80,
+        rows: 24,
+        cwd: '/tmp/worktree-renderer',
+        command: 'codex resume provider-resume-1',
+        worktreeId: 'worktree-renderer',
+        tabId,
+        leafId,
+        env: {
+          ORCA_PANE_KEY: paneKey,
+          ORCA_TAB_ID: tabId,
+          ORCA_WORKTREE_ID: 'worktree-renderer',
+          ORCA_AGENT_LAUNCH_TOKEN: launchToken
+        },
+        launchConfig: { agentCommand: 'codex', agentArgs: '', agentEnv: {} },
+        launchAgent: 'codex',
+        launchToken,
+        resumeProviderSession: providerSession
+      })) as { conversationId?: string }
+
+      expect(result.conversationId).toBe(seeded.conversation.id)
+      expect(repository.conversations.list()).toHaveLength(1)
+    } finally {
+      repository.close()
+    }
+  })
+
+  it('closes a newly allocated renderer Conversation when provider spawn fails', async () => {
+    const repository = new IssueRepository(
+      IssueDatabase.openTransient({ logicalPath: 'pty-renderer-conversation-failure' })
+    )
+    try {
+      const tabId = 'tab-renderer-failure'
+      const leafId = '14141414-1414-4414-8414-141414141414'
+      const paneKey = makePaneKey(tabId, leafId)
+      const launchToken = 'renderer-failure-token'
+      const runtime = createRendererConversationRuntime(repository)
+      const provider = createRendererConversationProvider(async () => {
+        expect(repository.conversations.list()).toHaveLength(1)
+        throw new Error('spawn ENOENT')
+      })
+      setLocalPtyProvider(provider as never)
+      registerPtyHandlers(mainWindow as never, runtime as never)
+
+      await expect(
+        handlers.get('pty:spawn')!(mainWindowIpcEvent, {
+          cols: 80,
+          rows: 24,
+          cwd: '/tmp/worktree-renderer',
+          command: 'codex',
+          worktreeId: 'worktree-renderer',
+          tabId,
+          leafId,
+          env: {
+            ORCA_PANE_KEY: paneKey,
+            ORCA_TAB_ID: tabId,
+            ORCA_WORKTREE_ID: 'worktree-renderer',
+            ORCA_AGENT_LAUNCH_TOKEN: launchToken
+          },
+          launchConfig: { agentCommand: 'codex', agentArgs: '', agentEnv: {} },
+          launchAgent: 'codex',
+          launchToken
+        })
+      ).rejects.toThrow('spawn ENOENT')
+
+      expect(repository.conversations.list()).toEqual([
+        expect.objectContaining({ state: 'closed', launchFailure: 'spawn ENOENT' })
+      ])
+      expect(
+        repository.database
+          .prepare('SELECT consumed_at IS NOT NULL AS retired FROM conversation_launch_claims')
+          .get()
+      ).toEqual({ retired: 1 })
+    } finally {
+      repository.close()
+    }
+  })
+
   it.each([
     {
       label: 'matching proof',
diff --git a/src/main/ipc/pty.ts b/src/main/ipc/pty.ts
index 6c38d513..79ff4549 100644
--- a/src/main/ipc/pty.ts
+++ b/src/main/ipc/pty.ts
@@ -12,7 +12,10 @@ import {
   powerMonitor
 } from 'electron'
 export { getBashShellReadyRcfileContent } from '../providers/local-pty-shell-ready'
-import type { OrcaRuntimeService } from '../runtime/orca-runtime'
+import type {
+  OrcaRuntimeService,
+  RendererConversationLaunchAllocation
+} from '../runtime/orca-runtime'
 import type { PtyBindingSourceExpectation, Store } from '../persistence'
 import { retireTerminalSurfaceFromPersistence } from '../runtime/mobile-session-terminal-persistence-retirement'
 import type { GlobalSettings } from '../../shared/global-settings-types'
@@ -5882,6 +5885,7 @@ export function registerPtyHandlers(
         resumeProviderSession?: AgentProviderSessionMetadata
         launchToken?: unknown
         launchAgent?: TuiAgent
+        issueId?: string
         startupCommandDelivery?: StartupCommandDelivery
         connectionId?: string | null
         worktreeId?: string
@@ -5999,6 +6003,8 @@ export function registerPtyHandlers(
       // have scanned from those bytes live.
       let snapshotKittyFlagsCoverReconciledSeq = true
       let preparedProvisionalExecutionContext = false
+        let conversationAllocation: RendererConversationLaunchAllocation | null = null
+        let providerSpawnCommitted = false
       let releaseWorktreeSpawn: (() => void) | undefined
       try {
         if (!earlyStablePaneOwner) {
@@ -6562,6 +6568,49 @@ export function registerPtyHandlers(
             args.worktreeId,
             args.connectionId
           )
+          const managedWorktreeId =
+            typeof args.worktreeId === 'string' ? args.worktreeId : null
+          const managedLaunchToken =
+            typeof args.launchToken === 'string' &&
+            args.launchToken.length > 0 &&
+            args.launchToken.length <= AGENT_LAUNCH_TOKEN_MAX_LENGTH
+              ? args.launchToken
+              : null
+          const managedLaunchAgent = isTuiAgent(args.launchAgent)
+            ? args.launchAgent
+            : null
+          if (
+            !preAdoptedStablePane &&
+            !stablePaneOwnerCandidate &&
+            managedWorktreeId &&
+            managedLaunchToken &&
+            managedLaunchAgent &&
+            effectiveLaunchConfig !== undefined &&
+            spawnEnv?.ORCA_AGENT_LAUNCH_TOKEN === managedLaunchToken
+          ) {
+            conversationAllocation =
+              (await runtime?.allocateRendererConversationLaunch?.({
+                worktreeId: managedWorktreeId,
+                connectionId: args.connectionId,
+                paneKey: reservationPaneKey,
+                launchToken: managedLaunchToken,
+                agent: managedLaunchAgent,
+                ...(typeof args.issueId === 'string' && args.issueId.length > 0
+                  ? { issueId: args.issueId }
+                  : {}),
+                ...(args.resumeProviderSession
+                  ? {
+                      resumeProviderSession: args.resumeProviderSession,
+                      resumeLocator: args.resumeProviderSession.transcriptPath ?? null
+                    }
+                  : {})
+              })) ?? null
+          }
+          if (conversationAllocation) {
+            spawnOptions.onPtySpawnCommitted = () => {
+              providerSpawnCommitted = true
+            }
+          }
           const expectedPtyId =
             stablePaneOwnerCandidate?.ptyId ?? effectiveSessionAppId ?? effectiveSessionId
           if (expectedPtyId) {
@@ -6713,6 +6762,21 @@ export function registerPtyHandlers(
               ...getCohortAtEmit()
             })
           }
+          if (
+            conversationAllocation &&
+            !providerSpawnCommitted &&
+            !(
+              typeof spawnError === 'object' &&
+              spawnError !== null &&
+              'agentSessionOperationOutcome' in spawnError &&
+              spawnError.agentSessionOperationOutcome === 'unknown'
+            )
+          ) {
+            runtime?.recordRendererConversationLaunchFailure?.(
+              conversationAllocation,
+              spawnError
+            )
+          }
           throw spawnError
         } finally {
           if (preAllocatedHandle) {
@@ -7022,6 +7086,9 @@ export function registerPtyHandlers(
           // renderer can say so, and a reattach never ran this launch command.
           ...(codexResumeLaunch.notifyResumeUnavailable && !result.isReattach
             ? { agentResumeUnavailable: true as const }
+            : {}),
+          ...(conversationAllocation
+            ? { conversationId: conversationAllocation.conversationId }
             : {})
         }
         // Why: renderer tab state cannot reliably infer background and reattached PTYs in the daemon inventory.
diff --git a/src/main/local-worktree-filesystem.ts b/src/main/local-worktree-filesystem.ts
index 6cf50ed1..fe6b60ad 100644
--- a/src/main/local-worktree-filesystem.ts
+++ b/src/main/local-worktree-filesystem.ts
@@ -1,5 +1,5 @@
 import { execFile } from 'node:child_process'
-import { lstat, readFile } from 'node:fs/promises'
+import { lstat, readFile, realpath, stat } from 'node:fs/promises'
 import {
   buildWslLoginShellCommand,
   escapeWslShCommandForWindows,
@@ -20,6 +20,12 @@ type LocalWorktreePathAccess = {
   readPath: ReadPath
 }
 
+export type LocalWorktreeCanonicalPathAccess = {
+  platform: NodeJS.Platform
+  realpath(path: string): Promise<string>
+  stat(path: string): Promise<{ type: string }>
+}
+
 type ExecFileTextResult = {
   stdout: string
   stderr: string
@@ -123,6 +129,55 @@ export function getLocalWorktreePathAccess(
   }
 }
 
+export function getLocalWorktreeCanonicalPathAccess(
+  options: LocalWorktreeFilesystemOptions = {}
+): LocalWorktreeCanonicalPathAccess {
+  const distro = options.wslDistro?.trim()
+  if (!shouldUseWslFilesystem(options) || !distro) {
+    return {
+      platform: process.platform,
+      realpath,
+      stat: async (path) => {
+        const metadata = await stat(path)
+        return {
+          type: metadata.isFile()
+            ? 'file'
+            : metadata.isDirectory()
+              ? 'directory'
+              : metadata.isSymbolicLink()
+                ? 'symlink'
+                : 'other'
+        }
+      }
+    }
+  }
+  return {
+    platform: 'linux',
+    realpath: async (path) => {
+      const { stdout } = await runWslLoginShellCommand(
+        distro,
+        `realpath -- ${quotePosixShell(toLinuxPath(path))}`
+      )
+      const canonicalPath = stdout.trim()
+      if (!canonicalPath) {
+        throw new Error(`missing ${path}`)
+      }
+      return canonicalPath
+    },
+    stat: async (path) => {
+      const metadata = await getLocalWorktreePathAccess(options).statPath(path)
+      const type =
+        metadata &&
+        typeof metadata === 'object' &&
+        'type' in metadata &&
+        typeof metadata.type === 'string'
+          ? metadata.type
+          : 'other'
+      return { type }
+    }
+  }
+}
+
 export async function removeLocalWorktreePath(
   targetPath: string,
   options: LocalWorktreeFilesystemOptions = {}
diff --git a/src/main/native-chat/transcript-line-decoders-codex.ts b/src/main/native-chat/transcript-line-decoders-codex.ts
index c4b0ddbf..b6454c0a 100644
--- a/src/main/native-chat/transcript-line-decoders-codex.ts
+++ b/src/main/native-chat/transcript-line-decoders-codex.ts
@@ -123,7 +123,12 @@ function codexEventMessage(
     }
   }
   if (payload.type === 'item_completed') {
-    return codexCompletedTurnItem(payload, id, timestamp)
+    return codexCompletedTurnItem(
+      payload,
+      id,
+      timestamp,
+      extractString(payload.turn_id) ?? extractString(payload.turnId) ?? undefined
+    )
   }
   if (payload.type === 'user_message') {
     const text = extractString(payload.message)
@@ -143,7 +148,8 @@ function codexEventMessage(
 function codexCompletedTurnItem(
   payload: Record<string, unknown>,
   fallbackId: string,
-  timestamp: number | null
+  timestamp: number | null,
+  turnId: string | undefined
 ): NativeChatMessage | null {
   const item = asRecord(payload.item)
   if (!item) {
@@ -155,10 +161,24 @@ function codexCompletedTurnItem(
     return null
   }
   if (item.type === 'UserMessage' || item.type === 'user_message') {
-    return { id, role: 'user', blocks, timestamp, source: 'transcript' }
+    return {
+      id,
+      role: 'user',
+      blocks,
+      timestamp,
+      source: 'transcript',
+      ...(turnId ? { turnId } : {})
+    }
   }
   if (item.type === 'AgentMessage' || item.type === 'agent_message') {
-    return { id, role: 'assistant', blocks, timestamp, source: 'transcript' }
+    return {
+      id,
+      role: 'assistant',
+      blocks,
+      timestamp,
+      source: 'transcript',
+      ...(turnId ? { turnId } : {})
+    }
   }
   return null
 }
diff --git a/src/main/native-chat/transcript-reader-codex-history-mode.test.ts b/src/main/native-chat/transcript-reader-codex-history-mode.test.ts
index 5b0f3dc0..0a54457f 100644
--- a/src/main/native-chat/transcript-reader-codex-history-mode.test.ts
+++ b/src/main/native-chat/transcript-reader-codex-history-mode.test.ts
@@ -165,6 +165,7 @@ describe('Codex transcript history modes', () => {
         {
           id: 'user-1',
           role: 'user',
+          turnId: 'turn-1',
           blocks: [
             { type: 'text', text: 'Visible prompt' },
             { type: 'image-ref', url: 'data:image/png;base64,abc' },
@@ -174,6 +175,7 @@ describe('Codex transcript history modes', () => {
         {
           id: 'assistant-1',
           role: 'assistant',
+          turnId: 'turn-1',
           blocks: [{ type: 'text', text: 'Visible response' }]
         }
       ]
diff --git a/src/main/runtime/agent-session-claim-identity.test.ts b/src/main/runtime/agent-session-claim-identity.test.ts
index c8f566dc..dd95508c 100644
--- a/src/main/runtime/agent-session-claim-identity.test.ts
+++ b/src/main/runtime/agent-session-claim-identity.test.ts
@@ -4,6 +4,7 @@ import { tmpdir } from 'node:os'
 import { join } from 'node:path'
 import {
   canonicalizeAgentSessionIdentity,
+  canonicalizeAgentSessionIdentityWithPathAccess,
   createEphemeralAgentSessionClaimSigner
 } from './agent-session-claim-identity'
 
@@ -68,4 +69,56 @@ describe('agent session claim identity', () => {
       rmSync(dir, { recursive: true, force: true })
     }
   })
+
+  it('canonicalizes a remote Prime transcript on the execution owner filesystem', async () => {
+    const rawTranscriptPath = '/remote/link/session.jsonl'
+
+    await expect(
+      canonicalizeAgentSessionIdentityWithPathAccess(
+        'prime-agent',
+        {
+          key: 'session_id',
+          id: 'prime-session-remote',
+          transcriptPath: rawTranscriptPath
+        },
+        {
+          platform: 'linux',
+          realpath: async (path) =>
+            path === rawTranscriptPath ? '/remote/canonical/session.jsonl' : path,
+          stat: async (path) => ({
+            type: path === '/remote/canonical/session.jsonl' ? 'file' : 'directory'
+          })
+        }
+      )
+    ).resolves.toEqual({
+      agent: 'prime-agent',
+      providerSession: {
+        key: 'session_id',
+        id: 'prime-session-remote',
+        transcriptPath: '/remote/canonical/session.jsonl'
+      }
+    })
+  })
+
+  it('uses Windows path rules when the execution owner is Windows', async () => {
+    await expect(
+      canonicalizeAgentSessionIdentityWithPathAccess(
+        'pi',
+        {
+          key: 'session_id',
+          id: 'pi-session-remote',
+          transcriptPath: 'C:\\Users\\alice\\sessions\\..\\session.jsonl'
+        },
+        {
+          platform: 'win32',
+          realpath: async () => 'C:\\Users\\Alice\\session.jsonl',
+          stat: async () => ({ type: 'file' })
+        }
+      )
+    ).resolves.toMatchObject({
+      providerSession: {
+        transcriptPath: 'c:\\users\\alice\\session.jsonl'
+      }
+    })
+  })
 })
diff --git a/src/main/runtime/agent-session-claim-identity.ts b/src/main/runtime/agent-session-claim-identity.ts
index bb172066..5703a2bb 100644
--- a/src/main/runtime/agent-session-claim-identity.ts
+++ b/src/main/runtime/agent-session-claim-identity.ts
@@ -8,7 +8,7 @@ import {
   statSync,
   writeFileSync
 } from 'node:fs'
-import { dirname, isAbsolute, join, normalize } from 'node:path'
+import { dirname, join, posix, win32 } from 'node:path'
 import {
   AGENT_SESSION_CLAIM_DIGEST_VERSION,
   type AgentSessionExecutionClaim
@@ -37,6 +37,12 @@ export type CanonicalAgentSessionIdentity = {
   providerSession: AgentProviderSessionMetadata
 }
 
+export type AgentSessionIdentityPathAccess = {
+  platform: NodeJS.Platform
+  realpath(path: string): Promise<string>
+  stat(path: string): Promise<{ type: string } | { isFile(): boolean }>
+}
+
 function encodeFields(fields: readonly string[]): Buffer {
   const chunks: Buffer[] = []
   for (const field of fields) {
@@ -48,9 +54,34 @@ function encodeFields(fields: readonly string[]): Buffer {
   return Buffer.concat(chunks)
 }
 
-function canonicalPathForPlatform(value: string): string {
-  const canonical = normalize(realpathSync(value))
-  return process.platform === 'win32' ? canonical.toLocaleLowerCase('en-US') : canonical
+function pathApiForPlatform(platform: NodeJS.Platform): typeof posix | typeof win32 {
+  return platform === 'win32' ? win32 : posix
+}
+
+function normalizeCanonicalPath(value: string, platform: NodeJS.Platform): string {
+  const canonical = pathApiForPlatform(platform).normalize(value)
+  return platform === 'win32' ? canonical.toLocaleLowerCase('en-US') : canonical
+}
+
+function requireCanonicalizableTranscriptPath(
+  agent: ResumableTuiAgent,
+  providerSession: AgentProviderSessionMetadata,
+  platform: NodeJS.Platform
+): string {
+  const transcriptPath = providerSession.transcriptPath
+  if (
+    (agent !== 'pi' && agent !== 'prime-agent') ||
+    !transcriptPath ||
+    !pathApiForPlatform(platform).isAbsolute(transcriptPath) ||
+    Buffer.byteLength(transcriptPath, 'utf8') > TRANSCRIPT_PATH_MAX_BYTES
+  ) {
+    throw new Error('agent_session_identity_required')
+  }
+  return transcriptPath
+}
+
+function isFileStat(value: { type: string } | { isFile(): boolean }): boolean {
+  return 'isFile' in value ? value.isFile() : value.type === 'file'
 }
 
 export function canonicalizeAgentSessionIdentity(
@@ -67,16 +98,52 @@ export function canonicalizeAgentSessionIdentity(
   if (agent !== 'pi' && agent !== 'prime-agent') {
     return { agent, providerSession }
   }
-  const transcriptPath = providerSession.transcriptPath
-  if (
-    !transcriptPath ||
-    !isAbsolute(transcriptPath) ||
-    Buffer.byteLength(transcriptPath, 'utf8') > TRANSCRIPT_PATH_MAX_BYTES
-  ) {
+  const transcriptPath = requireCanonicalizableTranscriptPath(
+    agent,
+    providerSession,
+    process.platform
+  )
+  const canonicalTranscriptPath = normalizeCanonicalPath(
+    realpathSync(transcriptPath),
+    process.platform
+  )
+  if (!statSync(canonicalTranscriptPath).isFile()) {
     throw new Error('agent_session_identity_required')
   }
-  const canonicalTranscriptPath = canonicalPathForPlatform(transcriptPath)
-  if (!statSync(canonicalTranscriptPath).isFile()) {
+  return {
+    agent,
+    providerSession: { ...providerSession, transcriptPath: canonicalTranscriptPath }
+  }
+}
+
+export async function canonicalizeAgentSessionIdentityWithPathAccess(
+  agent: unknown,
+  rawProviderSession: unknown,
+  pathAccess?: AgentSessionIdentityPathAccess
+): Promise<CanonicalAgentSessionIdentity> {
+  if (!pathAccess) {
+    return canonicalizeAgentSessionIdentity(agent, rawProviderSession)
+  }
+  if (!isResumableTuiAgent(agent)) {
+    throw new Error('agent_session_identity_required')
+  }
+  const providerSession = normalizeAgentProviderSession(rawProviderSession)
+  if (!providerSession || !getAgentResumeArgv(agent, providerSession)) {
+    throw new Error('agent_session_identity_required')
+  }
+  if (agent !== 'pi' && agent !== 'prime-agent') {
+    return { agent, providerSession }
+  }
+  const transcriptPath = requireCanonicalizableTranscriptPath(
+    agent,
+    providerSession,
+    pathAccess.platform
+  )
+  const canonicalTranscriptPath = normalizeCanonicalPath(
+    await pathAccess.realpath(transcriptPath),
+    pathAccess.platform
+  )
+  if (!isFileStat(await pathAccess.stat(canonicalTranscriptPath))) {
     throw new Error('agent_session_identity_required')
   }
   return {
diff --git a/src/main/runtime/mobile-notification-replay.test.ts b/src/main/runtime/mobile-notification-replay.test.ts
index a13c0f75..3353357c 100644
--- a/src/main/runtime/mobile-notification-replay.test.ts
+++ b/src/main/runtime/mobile-notification-replay.test.ts
@@ -44,6 +44,25 @@ describe('MobileNotificationReplayBuffer', () => {
     expect((a as Record<string, unknown>).seq).toBeUndefined()
   })
 
+  it('preserves optional Issue locators through replay', () => {
+    const buffer = new MobileNotificationReplayBuffer()
+    const event = dispatch(buffer, {
+      source: 'agent-needs-input',
+      executionHostId: 'ssh:docs',
+      conversationId: '33333333-3333-4333-8333-333333333333',
+      issueId: '22222222-2222-4222-8222-222222222222',
+      roundRecordId: '44444444-4444-4444-8444-444444444444'
+    })
+
+    expect(event).toMatchObject({
+      source: 'agent-needs-input',
+      executionHostId: 'ssh:docs',
+      conversationId: '33333333-3333-4333-8333-333333333333',
+      issueId: '22222222-2222-4222-8222-222222222222',
+      roundRecordId: '44444444-4444-4444-8444-444444444444'
+    })
+  })
+
   it('returns missed notifications that were dispatched after the watermark', () => {
     const buffer = new MobileNotificationReplayBuffer()
     dispatch(buffer, { notificationId: 'agent:one' })
diff --git a/src/main/runtime/mobile-rpc-allowlist.test.ts b/src/main/runtime/mobile-rpc-allowlist.test.ts
index 63957f87..d8783e9e 100644
--- a/src/main/runtime/mobile-rpc-allowlist.test.ts
+++ b/src/main/runtime/mobile-rpc-allowlist.test.ts
@@ -141,4 +141,24 @@ describe('mobile RPC allowlist', () => {
       )
     ).toEqual([])
   })
+
+  it('allows the read-only Issue projection without granting Issue mutations', () => {
+    const allowed = mobileRpcAllowlist()
+    expect(allowed.has('issues.pendingForMobile')).toBe(true)
+    expect(allowed.has('conversations.get')).toBe(true)
+    expect(
+      [
+        'issues.create',
+        'issues.update',
+        'issues.archive',
+        'issues.reopen',
+        'issues.reparent',
+        'issues.delete',
+        'conversations.bindIssue',
+        'issues.generateDigest',
+        'issues.updateDigestDraft',
+        'issues.confirmDigest'
+      ].filter((method) => allowed.has(method))
+    ).toEqual([])
+  })
 })
diff --git a/src/main/runtime/orca-runtime.ts b/src/main/runtime/orca-runtime.ts
index a0707815..84623632 100644
--- a/src/main/runtime/orca-runtime.ts
+++ b/src/main/runtime/orca-runtime.ts
@@ -147,6 +147,8 @@ import type {
   ArtifactWriteRequest
 } from '../../shared/artifacts'
 import type { ArtifactCloudService } from '../artifacts/artifact-cloud-service'
+import type { ConversationAllocator } from '../issues/conversation-allocator'
+import type { ConversationHookIdentityContext } from '../issues/conversation-hook-identity-ingestor'
 import { ORCHESTRATION_MESSAGE_WAIT_DEFAULT_TIMEOUT_MS } from '../../shared/orchestration-message-wait-timeout'
 import { shouldForwardHeadlessTerminalQueryReply } from './headless-terminal-query-reply-policy'
 import type { TerminalRevealIdentity } from '../../shared/terminal-reveal-identity'
@@ -178,7 +180,12 @@ import type {
   AutomationWorkspaceMode
 } from '../../shared/automations-types'
 import type { DirEntry, FilesystemPathFlavor } from '../../shared/filesystem-entry-types'
-import type { FolderWorkspace, WorkspaceKey } from '../../shared/folder-workspace-types'
+import type {
+  FolderWorkspace,
+  WorkspaceKey,
+  WorkspaceScope
+} from '../../shared/folder-workspace-types'
+import type { WorkspaceSnapshot } from '../../shared/issues/types'
 import type {
   GitHubPRReviewCommentInput,
   GitHubReactionContent
@@ -1403,6 +1410,7 @@ type TerminalCreateOptions = {
   resumeProviderSession?: AgentProviderSessionMetadata
   launchToken?: string
   launchAgent?: TuiAgent
+  issueId?: string
   // Why: agent ids are not shell commands (`cursor` is the Cursor desktop app; its
   // CLI is `cursor-agent`). Callers that know the agent name it here instead of
   // guessing a command, and the runtime builds the configured launch.
@@ -1431,6 +1439,7 @@ type TerminalCreateOptions = {
   // identity; opaque terminal.create commands remain ordinary shells.
   agentSessionClaim?: AgentSessionExecutionClaim
   agentSessionCreateOperationId?: string
+  conversationAllocation?: RendererConversationLaunchAllocation
   signal?: AbortSignal
   // Why: idempotent create operations must retain their fence after the PTY
   // exists, even if later runtime publication fails.
@@ -1464,6 +1473,11 @@ function isAgentSessionOperationOutcomeUnknown(error: unknown): boolean {
   )
 }
 
+function describeConversationLaunchFailure(error: unknown): string {
+  const message = error instanceof Error ? error.message : String(error)
+  return message.slice(0, 4096)
+}
+
 const AGENT_SESSION_OPERATION_PER_CLIENT_LIMIT = 512
 const AGENT_SESSION_OPERATION_GLOBAL_LIMIT = 4_096
 
@@ -2522,6 +2536,31 @@ type TerminalWorkspaceLaunchScope = {
   connectionId: string | null
   repo: Repo | null
   folderWorkspace: FolderWorkspace | null
+  managedWorktree: ResolvedWorktree | null
+}
+
+type ConversationWorkspaceAllocation = {
+  executionHostId: ExecutionHostId
+  workspaceRef: WorkspaceScope
+  workspaceSnapshot: WorkspaceSnapshot
+}
+
+export type RendererConversationLaunchAllocation = {
+  conversationId: string
+  claimId: string
+  closeConversationOnFailure: boolean
+}
+
+export type RendererConversationLaunchInput = {
+  worktreeId: string
+  connectionId?: string | null
+  paneKey?: string | null
+  processIncarnation?: string | null
+  launchToken: string
+  agent: TuiAgent
+  issueId?: string
+  resumeProviderSession?: AgentProviderSessionMetadata
+  resumeLocator?: string | null
 }
 
 type ResolvedTerminalWorkspaceLaunchTarget = {
@@ -2657,10 +2696,14 @@ type ResolvedWorktreeInFlight = {
 // events after it — idempotent, no duplicate local pushes.
 export type MobileNotificationDispatchEvent = {
   type: 'notification'
-  source: 'agent-task-complete' | 'terminal-bell' | 'test' | 'plugin'
+  source: 'agent-task-complete' | 'agent-needs-input' | 'terminal-bell' | 'test' | 'plugin'
   title: string
   body: string
   worktreeId?: string
+  executionHostId?: ExecutionHostId
+  conversationId?: string
+  issueId?: string
+  roundRecordId?: string
   notificationId?: string
   notificationSeq?: number
   notificationEpoch?: string
@@ -3340,6 +3383,7 @@ export class OrcaRuntimeService {
     | ((args: AiVaultPrepareSessionResumeArgs) => Promise<AiVaultPrepareSessionResumeResult>)
     | null
   private readonly agentSessionClaimSigner: AgentSessionClaimSigner
+  private readonly conversationAllocator: ConversationAllocator | null
   private readonly agentSessionCreateOperations = new Map<string, AgentSessionCreateOperation>()
   private readonly orchestrationCompatibilitySshAttachments = new Map<
     string,
@@ -3419,6 +3463,7 @@ export class OrcaRuntimeService {
       buildAgentHookPtyEnv?: () => Record<string, string>
       getDesktopWindowStatus?: () => RuntimeDesktopWindowStatus
       agentSessionClaimSigner?: AgentSessionClaimSigner
+      conversationAllocator?: ConversationAllocator
       orchestrationEnvironmentTransport?: OrchestrationEnvironmentTransport
     }
   ) {
@@ -3468,6 +3513,7 @@ export class OrcaRuntimeService {
     this.prepareAiVaultSessionResumeFn = deps?.prepareAiVaultSessionResume ?? null
     this.agentSessionClaimSigner =
       deps?.agentSessionClaimSigner ?? createEphemeralAgentSessionClaimSigner(this.runtimeId)
+    this.conversationAllocator = deps?.conversationAllocator ?? null
     this.onTerminalSideEffects = deps?.onTerminalSideEffects ?? null
     // Why: the ConPTY spawn mark can land after daemon stream data already
     // created this PTY's emulator; the mark retrofits the DA1 override here
@@ -3488,6 +3534,65 @@ export class OrcaRuntimeService {
     return this.getLocalProviderFn ? this.getLocalProviderFn() : null
   }
 
+  async allocateRendererConversationLaunch(
+    input: RendererConversationLaunchInput
+  ): Promise<RendererConversationLaunchAllocation | null> {
+    if (!this.conversationAllocator) {
+      return null
+    }
+    const workspace = await this.resolveTerminalWorkspaceLaunchScope(`id:${input.worktreeId}`)
+    if ((workspace.connectionId ?? null) !== (input.connectionId ?? null)) {
+      throw new Error('conversation_workspace_host_mismatch')
+    }
+    return this.allocateConversationLaunch(workspace, input)
+  }
+
+  private allocateConversationLaunch(
+    workspace: TerminalWorkspaceLaunchScope,
+    input: Omit<RendererConversationLaunchInput, 'worktreeId' | 'connectionId'>
+  ): RendererConversationLaunchAllocation | null {
+    if (!this.conversationAllocator) {
+      return null
+    }
+    const base = {
+      ...this.getConversationWorkspaceAllocation(workspace),
+      agent: input.agent,
+      launchToken: input.launchToken,
+      paneKey: input.paneKey,
+      processIncarnation: input.processIncarnation,
+      connectionId: workspace.connectionId
+    }
+    const allocation = input.resumeProviderSession
+      ? this.conversationAllocator.resolveResumeOrAllocate({
+          ...base,
+          issueId: null,
+          providerSession: input.resumeProviderSession,
+          resumeLocator:
+            input.resumeLocator ?? input.resumeProviderSession.transcriptPath ?? null
+        })
+      : this.conversationAllocator.allocateFreshLaunch({
+          ...base,
+          issueId: input.issueId ?? null
+        })
+    return {
+      conversationId: allocation.conversation.id,
+      claimId: allocation.claim.claimId,
+      closeConversationOnFailure: allocation.disposition === 'created'
+    }
+  }
+
+  recordRendererConversationLaunchFailure(
+    allocation: RendererConversationLaunchAllocation,
+    error: unknown
+  ): void {
+    this.conversationAllocator?.recordLaunchFailure({
+      conversationId: allocation.conversationId,
+      claimId: allocation.claimId,
+      failure: describeConversationLaunchFailure(error),
+      closeConversation: allocation.closeConversationOnFailure
+    })
+  }
+
   private async stopPtysForDestructiveWorktreeRemoval(
     worktreeId: string,
     options: { connectionId?: string; allowUnverifiedStop?: boolean } = {}
@@ -16578,6 +16683,45 @@ export class OrcaRuntimeService {
     return leaf?.worktreeId ?? this.getPtyRecordForPaneKey(paneKey)?.worktreeId ?? null
   }
 
+  async resolveConversationHookIdentityContext(
+    paneKey: string,
+    expectedWorktreeId?: string
+  ): Promise<ConversationHookIdentityContext | null> {
+    let pane: RuntimeTerminalResolvePane
+    try {
+      pane = this.resolveTerminalPane(paneKey, expectedWorktreeId)
+    } catch {
+      return null
+    }
+    if (!pane.worktreeId) {
+      return null
+    }
+    const pty = this.getPtyRecordForPaneKey(paneKey)
+    if (!pty) {
+      return null
+    }
+    let workspace: TerminalWorkspaceLaunchScope
+    try {
+      workspace = await this.resolveTerminalWorkspaceLaunchScope(`id:${pane.worktreeId}`)
+    } catch {
+      return null
+    }
+    if ((workspace.connectionId ?? null) !== (pty.connectionId ?? null)) {
+      return null
+    }
+    const allocation = this.getConversationWorkspaceAllocation(workspace)
+    if (pane.executionHostId && pane.executionHostId !== allocation.executionHostId) {
+      return null
+    }
+    return {
+      ...allocation,
+      processIncarnation: this.getTerminalProcessIncarnation(pane.handle),
+      connectionId: workspace.connectionId,
+      hostPlatform: pane.hostPlatform ?? process.platform,
+      ...(pty.wslDistro ? { wslDistro: pty.wslDistro } : {})
+    }
+  }
+
   /** Read-only context of the worktree the user is focused on, for plugin
    *  panels (workspace.readContext). Prefers the persisted session focus and
    *  falls back to the last-focused pane's worktree; null when neither
@@ -25597,21 +25741,63 @@ export class OrcaRuntimeService {
     if (_caller.signal?.aborted) {
       throw new Error('client_disconnected')
     }
-    const terminal = await this.createTerminal(`id:${workspace.id}`, {
-      command: startup.launchCommand,
-      env: startup.env,
-      launchConfig: startup.launchConfig,
-      launchAgent: request.agent,
-      presentation: request.presentation ?? 'background',
-      tabId: request.placement?.tabId,
-      leafId: request.placement?.leafId,
-      persistHostSessionBinding: true,
-      agentSessionClaim: claim,
-      signal: _caller.signal
-    })
+    const launchToken = this.conversationAllocator ? randomUUID() : undefined
+    const allocation =
+      this.conversationAllocator && launchToken
+        ? this.conversationAllocator.resolveResumeOrAllocate({
+            ...this.getConversationWorkspaceAllocation(workspace),
+            agent: request.agent,
+            issueId: null,
+            launchToken,
+            connectionId: workspace.connectionId,
+            providerSession: identity.providerSession,
+            resumeLocator:
+              identity.providerSession.transcriptPath ?? request.ompResumeFilePath ?? null
+          })
+        : null
+    let spawnCommitted = false
+    let terminal: RuntimeTerminalCreate
+    try {
+      terminal = await this.createTerminal(`id:${workspace.id}`, {
+        command: startup.launchCommand,
+        env: startup.env,
+        launchConfig: startup.launchConfig,
+        launchAgent: request.agent,
+        ...(launchToken ? { launchToken } : {}),
+        presentation: request.presentation ?? 'background',
+        tabId: request.placement?.tabId,
+        leafId: request.placement?.leafId,
+        persistHostSessionBinding: true,
+        agentSessionClaim: claim,
+        ...(allocation
+          ? {
+              conversationAllocation: {
+                conversationId: allocation.conversation.id,
+                claimId: allocation.claim.claimId,
+                closeConversationOnFailure: allocation.disposition === 'created'
+              }
+            }
+          : {}),
+        signal: _caller.signal,
+        onPtySpawnCommitted: () => {
+          spawnCommitted = true
+        }
+      })
+    } catch (error) {
+      if (allocation && !spawnCommitted && !isAgentSessionOperationOutcomeUnknown(error)) {
+        this.conversationAllocator?.recordLaunchFailure({
+          conversationId: allocation.conversation.id,
+          claimId: allocation.claim.claimId,
+          failure: describeConversationLaunchFailure(error),
+          closeConversation: allocation.disposition === 'created'
+        })
+      }
+      throw error
+    }
     return {
       terminal,
-      disposition: terminal.agentSessionDisposition ?? 'created'
+      disposition: terminal.agentSessionDisposition ?? 'created',
+      ...(allocation ? { conversationId: allocation.conversation.id } : {})
     }
   }
 
@@ -25637,6 +25823,7 @@ export class OrcaRuntimeService {
         JSON.stringify([
           request.worktree,
           request.agent,
+          request.issueId ?? null,
           request.prompt ?? null,
           request.promptDelivery ?? null,
           request.agentArgs ?? null,
@@ -25703,6 +25890,7 @@ export class OrcaRuntimeService {
           JSON.stringify([
             workspace.id,
             request.agent,
+            request.issueId ?? null,
             request.prompt ?? null,
             request.promptDelivery ?? null,
             request.agentArgs ?? null,
@@ -25780,12 +25968,25 @@ export class OrcaRuntimeService {
       const operationLeafId =
         request.placement?.leafId ?? deterministicAgentSessionUuid(`${executionOperationId}:leaf`)
       const operationHandle = `term_${deterministicAgentSessionUuid(`${executionOperationId}:handle`)}`
+      const launchToken = this.conversationAllocator ? randomUUID() : undefined
+      const allocation =
+        this.conversationAllocator && launchToken
+          ? this.conversationAllocator.allocateFreshLaunch({
+              ...this.getConversationWorkspaceAllocation(workspace),
+              agent: request.agent,
+              issueId: request.issueId ?? null,
+              launchToken,
+              paneKey: makePaneKey(operationTabId, operationLeafId),
+              connectionId: workspace.connectionId
+            })
+          : null
       try {
         terminal = await this.createTerminal(`id:${workspace.id}`, {
           command: startup.launchCommand,
           env: startup.env,
           launchConfig: startup.launchConfig,
           launchAgent: request.agent,
+          ...(launchToken ? { launchToken } : {}),
           startupCommandDelivery: startup.startupCommandDelivery,
           cwd: startupCwd,
           presentation: request.presentation ?? 'background',
@@ -25795,6 +25996,15 @@ export class OrcaRuntimeService {
           viewMode: request.viewMode,
           persistHostSessionBinding: true,
           agentSessionCreateOperationId: executionOperationId,
+          ...(allocation
+            ? {
+                conversationAllocation: {
+                  conversationId: allocation.conversation.id,
+                  claimId: allocation.claim.claimId,
+                  closeConversationOnFailure: allocation.disposition === 'created'
+                }
+              }
+            : {}),
           signal: caller.signal,
           onPtySpawnCommitted: () => {
             retainReplayFence = true
@@ -25804,9 +26014,20 @@ export class OrcaRuntimeService {
         if (isAgentSessionOperationOutcomeUnknown(error)) {
           retainReplayFence = true
         }
+        if (allocation && !retainReplayFence) {
+          this.conversationAllocator?.recordLaunchFailure({
+            conversationId: allocation.conversation.id,
+            claimId: allocation.claim.claimId,
+            failure: describeConversationLaunchFailure(error)
+          })
+        }
         throw error
       }
-      return { terminal, disposition: 'created' }
+      return {
+        terminal,
+        disposition: 'created',
+        ...(allocation ? { conversationId: allocation.conversation.id } : {})
+      }
     })()
     this.agentSessionCreateOperations.set(operationKey, {
       fingerprint: requestFingerprint,
@@ -25875,6 +26096,8 @@ export class OrcaRuntimeService {
       }
       const workspace = await this.resolveTerminalWorkspaceLaunchScope(worktreeSelector)
       const launchOpts = await this.resolveAgentTerminalCreateOptions(workspace, opts)
+      let conversationAllocation = launchOpts.conversationAllocation ?? null
+      const ownsConversationAllocation = conversationAllocation === null
       let ptySpawnCommitReported = false
       const reportPtySpawnCommitted = (): void => {
         if (ptySpawnCommitReported) {
@@ -26015,6 +26238,22 @@ export class OrcaRuntimeService {
         if (launchOpts.signal?.aborted) {
           throw new Error('client_disconnected')
         }
+        if (
+          !adoptedBeforeLaunch &&
+          !conversationAllocation &&
+          launchToken &&
+          launchOpts.launchAgent &&
+          launchOpts.launchConfig
+        ) {
+          conversationAllocation = this.allocateConversationLaunch(workspace, {
+            paneKey,
+            launchToken,
+            agent: launchOpts.launchAgent,
+            issueId: launchOpts.issueId,
+            resumeProviderSession: launchOpts.resumeProviderSession,
+            resumeLocator: launchOpts.resumeProviderSession?.transcriptPath ?? null
+          })
+        }
         const persistHostSessionBinding =
           launchOpts.persistHostSessionBinding ||
           launchOpts.surfaceOwner === false ||
@@ -26074,6 +26313,16 @@ export class OrcaRuntimeService {
             // awaits above if the authoritative window is destroyed mid-spawn.
             ...(persistHostSessionBinding ? { persistHostSessionBinding: true } : {})
           })
+        } catch (error) {
+          if (
+            ownsConversationAllocation &&
+            conversationAllocation &&
+            !ptySpawnCommitReported &&
+            !isAgentSessionOperationOutcomeUnknown(error)
+          ) {
+            this.recordRendererConversationLaunchFailure(conversationAllocation, error)
+          }
+          throw error
         } finally {
           releaseStablePaneCreate?.()
         }
@@ -26181,6 +26430,9 @@ export class OrcaRuntimeService {
         }
         return {
           handle,
+          ...(conversationAllocation
+            ? { conversationId: conversationAllocation.conversationId }
+            : {}),
           tabId,
           paneKey,
           ptyId: result.id,
@@ -28888,7 +29140,8 @@ export class OrcaRuntimeService {
       path: workspace.folderPath,
       connectionId: this.resolveFolderWorkspaceConnectionId(workspace),
       repo: null,
-      folderWorkspace: workspace
+      folderWorkspace: workspace,
+      managedWorktree: null
     }
   }
 
@@ -28946,6 +29199,40 @@ export class OrcaRuntimeService {
     return resolveTerminalStartupCwd(workspace.path, requestedCwd)
   }
 
+  private getConversationWorkspaceAllocation(
+    workspace: TerminalWorkspaceLaunchScope
+  ): ConversationWorkspaceAllocation {
+    if (workspace.folderWorkspace) {
+      const folderWorkspace = workspace.folderWorkspace
+      const executionHostId = getWorktreeExecutionHostId(
+        workspace.managedWorktree ?? this.folderWorkspaceToResolvedWorktree(folderWorkspace),
+        undefined
+      )
+      return {
+        executionHostId,
+        workspaceRef: { type: 'folder', folderWorkspaceId: folderWorkspace.id },
+        workspaceSnapshot: {
+          name: folderWorkspace.name,
+          path: folderWorkspace.folderPath
+        }
+      }
+    }
+    if (!workspace.managedWorktree) {
+      throw new Error('conversation_workspace_required')
+    }
+    return {
+      executionHostId: getWorktreeExecutionHostId(
+        workspace.managedWorktree,
+        workspace.repo ?? undefined
+      ),
+      workspaceRef: { type: 'worktree', worktreeId: workspace.managedWorktree.id },
+      workspaceSnapshot: {
+        name: workspace.managedWorktree.displayName,
+        path: workspace.managedWorktree.path
+      }
+    }
+  }
+
   private async resolveTerminalWorkspaceLaunchScope(
     selector: string
   ): Promise<TerminalWorkspaceLaunchScope> {
@@ -28966,7 +29253,8 @@ export class OrcaRuntimeService {
           path: homedir(),
           connectionId: null,
           repo: null,
-          folderWorkspace: null
+          folderWorkspace: null,
+          managedWorktree: null
         },
         managedWorktree: null
       }
@@ -28974,9 +29262,12 @@ export class OrcaRuntimeService {
 
     const folderScope = await this.resolveFolderWorkspaceLaunchScope(selector)
     if (folderScope) {
+      const managedWorktree = this.folderWorkspaceToResolvedWorktree(
+        folderScope.folderWorkspace
+      )
       return {
-        scope: folderScope,
-        managedWorktree: this.folderWorkspaceToResolvedWorktree(folderScope.folderWorkspace)
+        scope: { ...folderScope, managedWorktree },
+        managedWorktree
       }
     }
 
@@ -28991,7 +29282,8 @@ export class OrcaRuntimeService {
         path: worktree.path,
         connectionId: repo?.connectionId ?? null,
         repo,
-        folderWorkspace: null
+        folderWorkspace: null,
+        managedWorktree: worktree
       },
       managedWorktree: worktree
     }
diff --git a/src/main/runtime/rpc/methods/agent-session.ts b/src/main/runtime/rpc/methods/agent-session.ts
index 08aaa910..82228110 100644
--- a/src/main/runtime/rpc/methods/agent-session.ts
+++ b/src/main/runtime/rpc/methods/agent-session.ts
@@ -25,6 +25,7 @@ const MAX_TRANSCRIPT_PATH_BYTES = 16 * 1024
 const MAX_PROMPT_BYTES = 256 * 1024
 const MAX_AGENT_ARGS_BYTES = 16 * 1024
 const MAX_LAUNCH_PREFERENCE_LENGTH = 512
+const MAX_ISSUE_ID_LENGTH = 512
 
 const StrictNonEmptyString = (max: number, message: string) =>
   z
@@ -166,6 +167,7 @@ export const CreateAgentSessionParams: z.ZodType<RuntimeCreateAgentSessionReques
       ),
     worktree: WorktreeSelector,
     agent: z.string().refine(isTuiAgent, 'Unknown agent preset'),
+    issueId: StrictNonEmptyString(MAX_ISSUE_ID_LENGTH, 'Invalid Issue ID').optional(),
     prompt: z
       .string()
       .refine(
diff --git a/src/main/runtime/rpc/methods/client-events.test.ts b/src/main/runtime/rpc/methods/client-events.test.ts
index bf5026c8..b794207b 100644
--- a/src/main/runtime/rpc/methods/client-events.test.ts
+++ b/src/main/runtime/rpc/methods/client-events.test.ts
@@ -1,9 +1,12 @@
-import { describe, expect, it, vi } from 'vitest'
+import { afterEach, describe, expect, it, vi } from 'vitest'
+import { ORCA_ISSUES_RUNTIME_CAPABILITY } from '../../../../shared/protocol-version'
 import type { RuntimeClientEvent } from '../../../../shared/runtime-client-events'
+import type { IssueRuntimeService } from '../../../issues'
 import type { OrcaRuntimeService } from '../../orca-runtime'
 import { isStreamingMethod, type RpcContext, type RpcStreamingMethod } from '../core'
 // Why: importing client-events directly trips its module-init cycle through ipc/ssh; the index resolves it.
 import { ALL_RPC_METHODS } from './index'
+import { setIssueRuntimeServiceForRpc } from './issues'
 
 const subscribeMethod = ALL_RPC_METHODS.find(
   (method) => method.name === 'runtime.clientEvents.subscribe' && isStreamingMethod(method)
@@ -31,6 +34,8 @@ function makeRuntime(): {
   return { runtime, onClientEvent, cleanups }
 }
 
+afterEach(() => setIssueRuntimeServiceForRpc(null))
+
 describe('runtime.clientEvents.subscribe', () => {
   it('registers mobile subscriptions as non-consumers of terminal side effects', async () => {
     const { runtime, onClientEvent, cleanups } = makeRuntime()
@@ -63,4 +68,65 @@ describe('runtime.clientEvents.subscribe', () => {
     cleanups.forEach((cleanup) => cleanup())
     await done
   })
+
+  it('emits Issue invalidations only to clients that negotiated the capability', async () => {
+    const issueSubscription: {
+      listener?: (event: RuntimeClientEvent) => void
+    } = {}
+    setIssueRuntimeServiceForRpc({
+      onFactsChanged: (listener: (event: RuntimeClientEvent) => void) => {
+        issueSubscription.listener = listener
+        return () => {
+          delete issueSubscription.listener
+        }
+      }
+    } as unknown as IssueRuntimeService)
+    const supported = makeRuntime()
+    const supportedEvents: RuntimeClientEvent[] = []
+    const supportedDone = subscribeMethod.handler(
+      undefined,
+      {
+        runtime: supported.runtime,
+        connectionId: 'supported',
+        clientCapabilities: [ORCA_ISSUES_RUNTIME_CAPABILITY]
+      } as RpcContext,
+      (event) => {
+        const clientEvent = event as RuntimeClientEvent
+        if (clientEvent.type === 'issueFactsChanged') {
+          supportedEvents.push(clientEvent)
+        }
+      }
+    )
+    const issueEvent = {
+      type: 'issueFactsChanged' as const,
+      authorityId: 'authority',
+      hostPartitionKey: 'local' as const,
+      factsRevision: 2,
+      treeRevision: 1
+    }
+    issueSubscription.listener?.(issueEvent)
+
+    expect(supportedEvents).toEqual([issueEvent])
+    supported.cleanups.forEach((cleanup) => cleanup())
+    await supportedDone
+    expect(issueSubscription.listener).toBeUndefined()
+
+    const unsupported = makeRuntime()
+    const unsupportedEvents: RuntimeClientEvent[] = []
+    const unsupportedDone = subscribeMethod.handler(
+      undefined,
+      { runtime: unsupported.runtime, connectionId: 'unsupported' } as RpcContext,
+      (event) => {
+        const clientEvent = event as RuntimeClientEvent
+        if (clientEvent.type === 'issueFactsChanged') {
+          unsupportedEvents.push(clientEvent)
+        }
+      }
+    )
+
+    expect(unsupportedEvents).toEqual([])
+    expect(issueSubscription.listener).toBeUndefined()
+    unsupported.cleanups.forEach((cleanup) => cleanup())
+    await unsupportedDone
+  })
 })
diff --git a/src/main/runtime/rpc/methods/client-events.ts b/src/main/runtime/rpc/methods/client-events.ts
index 81ee32d1..826c3e15 100644
--- a/src/main/runtime/rpc/methods/client-events.ts
+++ b/src/main/runtime/rpc/methods/client-events.ts
@@ -1,7 +1,9 @@
 import { z } from 'zod'
 import { getRegisteredSshState, listRegisteredSshTargets } from '../../../ipc/ssh'
 import { getPublicSshState } from '../../public-ssh-state'
+import { ORCA_ISSUES_RUNTIME_CAPABILITY } from '../../../../shared/protocol-version'
 import { defineMethod, defineStreamingMethod, type RpcAnyMethod } from '../core'
+import { getIssueRuntimeServiceForRpc } from './issues'
 
 let clientEventSubscriptionSeq = 0
 
@@ -16,7 +18,11 @@ export const CLIENT_EVENT_METHODS: readonly RpcAnyMethod[] = [
   defineStreamingMethod({
     name: 'runtime.clientEvents.subscribe',
     params: null,
-    handler: async (_params, { runtime, connectionId, clientKind }, emit) => {
+    handler: async (
+      _params,
+      { runtime, connectionId, clientKind, clientCapabilities },
+      emit
+    ) => {
       await new Promise<void>((resolve) => {
         // Why: mobile discards terminalSideEffects; excluding it stops the
         // per-OSC batch frames from crossing the relay.
@@ -26,6 +32,10 @@ export const CLIENT_EVENT_METHODS: readonly RpcAnyMethod[] = [
           },
           { consumesTerminalSideEffects: clientKind !== 'mobile' }
         )
+        const unsubscribeIssueFacts =
+          clientCapabilities?.includes(ORCA_ISSUES_RUNTIME_CAPABILITY) === true
+            ? getIssueRuntimeServiceForRpc()?.onFactsChanged(emit)
+            : undefined
 
         const seq = ++clientEventSubscriptionSeq
         const subscriptionId = `runtime-client-events-${connectionId ?? 'inproc'}-${seq}`
@@ -33,6 +43,7 @@ export const CLIENT_EVENT_METHODS: readonly RpcAnyMethod[] = [
           subscriptionId,
           () => {
             unsubscribe()
+            unsubscribeIssueFacts?.()
             emit({ type: 'end' })
             resolve()
           },
diff --git a/src/main/runtime/rpc/methods/client-ui-schemas.ts b/src/main/runtime/rpc/methods/client-ui-schemas.ts
index 8a333894..8ccaf85c 100644
--- a/src/main/runtime/rpc/methods/client-ui-schemas.ts
+++ b/src/main/runtime/rpc/methods/client-ui-schemas.ts
@@ -169,6 +169,7 @@ const TopLevelViewSchema = z.enum([
   'terminal',
   'settings',
   'tasks',
+  'issues',
   'activity',
   'automations',
   'space',
diff --git a/src/main/runtime/rpc/methods/index.ts b/src/main/runtime/rpc/methods/index.ts
index 26c9a5a5..39bccb1a 100644
--- a/src/main/runtime/rpc/methods/index.ts
+++ b/src/main/runtime/rpc/methods/index.ts
@@ -40,6 +40,7 @@ import { PAIRING_METHODS } from './pairing'
 import { UPDATER_METHODS } from './updater'
 import { AGENT_SESSION_METHODS } from './agent-session'
 import { ARTIFACT_METHODS } from './artifacts'
+import { ISSUE_METHODS } from './issues'
 
 // Why: a flat manifest keeps registration order explicit and provides one
 // grep-point for "what methods does the RPC server expose?" — useful when
@@ -48,6 +49,7 @@ export const ALL_RPC_METHODS: readonly RpcAnyMethod[] = [
   ...STATUS_METHODS,
   ...AI_VAULT_METHODS,
   ...ARTIFACT_METHODS,
+  ...ISSUE_METHODS,
   ...AUTOMATION_METHODS,
   ...REPO_METHODS,
   ...WORKTREE_METHODS,
diff --git a/src/main/runtime/runtime-rpc.ts b/src/main/runtime/runtime-rpc.ts
index 326801c6..bec91a16 100644
--- a/src/main/runtime/runtime-rpc.ts
+++ b/src/main/runtime/runtime-rpc.ts
@@ -319,6 +319,8 @@ const MOBILE_RPC_METHOD_ALLOWLIST = new Set([
   'hostedReview.createStacked',
   'hostedReview.forBranch',
   'hostedReview.getCreationEligibility',
+  'conversations.get',
+  'issues.pendingForMobile',
   'linear.getCustomView',
   'linear.getIssue',
   'linear.getProject',
diff --git a/src/main/ssh/ssh-relay-session.ts b/src/main/ssh/ssh-relay-session.ts
index 820eb7e1..d0dff597 100644
--- a/src/main/ssh/ssh-relay-session.ts
+++ b/src/main/ssh/ssh-relay-session.ts
@@ -1499,6 +1499,7 @@ export class SshRelaySession {
         hookEventName?: unknown
         source?: unknown
         providerPromptId?: unknown
+        providerTurnId?: unknown
         compactTrigger?: unknown
         toolUseId?: unknown
         toolAgentId?: unknown
@@ -1531,6 +1532,7 @@ export class SshRelaySession {
             typeof envelope.hookEventName === 'string' ? envelope.hookEventName : undefined,
           source: envelope.source,
           providerPromptId: envelope.providerPromptId,
+          providerTurnId: envelope.providerTurnId,
           compactTrigger: envelope.compactTrigger,
           toolUseId: typeof envelope.toolUseId === 'string' ? envelope.toolUseId : undefined,
           toolAgentId: typeof envelope.toolAgentId === 'string' ? envelope.toolAgentId : undefined,
diff --git a/src/main/text-generation/commit-message-text-generation.ts b/src/main/text-generation/commit-message-text-generation.ts
index 655c6de0..78ccc980 100644
--- a/src/main/text-generation/commit-message-text-generation.ts
+++ b/src/main/text-generation/commit-message-text-generation.ts
@@ -103,7 +103,11 @@ export type RemoteCommitMessageExecResult = {
   spawnError?: string
 }
 
-export type TextGenerationOperation = 'commit-message' | 'pull-request-fields' | 'branch-name'
+export type TextGenerationOperation =
+  | 'commit-message'
+  | 'pull-request-fields'
+  | 'branch-name'
+  | 'issue-digest'
 
 export type CommitMessageGenerationTarget =
   | { kind: 'local'; cwd: string; env?: NodeJS.ProcessEnv; wslDistro?: string }
@@ -116,6 +120,7 @@ export type CommitMessageGenerationTarget =
         timeoutMs: number,
         operation: TextGenerationOperation
       ) => Promise<RemoteCommitMessageExecResult>
+      cancel?: (cwd: string, operation: TextGenerationOperation) => Promise<void>
       missingBinaryLocation: string
     }
 
@@ -134,6 +139,13 @@ type InternalTextGenerationResult =
       failureOutput?: AgentGenerationFailureOutput
     }
 
+export type TextGenerationExecutionResult = InternalTextGenerationResult
+
+export type TextGenerationExecutionOptions = {
+  emptyResultName: string
+  operation: TextGenerationOperation
+}
+
 type LocalProcessExecution<T> = {
   result: Promise<T>
   processClosed: Promise<void>
@@ -609,6 +621,17 @@ export function cancelGenerateCommitMessageLocal(cwd: string): void {
   cancelTokensByLane.get(localLaneKey('commit-message', cwd))?.()
 }
 
+export async function cancelTextGeneration(
+  target: CommitMessageGenerationTarget,
+  operation: TextGenerationOperation
+): Promise<void> {
+  if (target.kind === 'local') {
+    cancelTokensByLane.get(localLaneKey(operation, target.cwd))?.()
+    return
+  }
+  await target.cancel?.(target.cwd, operation)
+}
+
 function buildWslLauncherEnv(explicitEnv: NodeJS.ProcessEnv | undefined): NodeJS.ProcessEnv {
   const env: NodeJS.ProcessEnv = {}
   for (const key of WSL_LAUNCHER_ENV_KEYS) {
@@ -1064,6 +1087,23 @@ async function runRemotePlan(
   })
 }
 
+export async function executeTextGenerationPlan(
+  agentId: string,
+  plan: CommitMessagePlan,
+  target: CommitMessageGenerationTarget,
+  options: TextGenerationExecutionOptions
+): Promise<TextGenerationExecutionResult> {
+  return target.kind === 'remote'
+    ? runRemotePlan(plan, target, options.emptyResultName, options.operation)
+    : runLocalPlanForAgent(
+        agentId,
+        plan,
+        target,
+        options.emptyResultName,
+        options.operation
+      )
+}
+
 function formatCommitMessageGenerationResult(
   result: InternalTextGenerationResult
 ): GenerateCommitMessageResult {
diff --git a/src/preload/api-types.ts b/src/preload/api-types.ts
index d81d8c59..7992c49e 100644
--- a/src/preload/api-types.ts
+++ b/src/preload/api-types.ts
@@ -32,6 +32,7 @@ import type { GithubWorkItemApi } from './api/github-work-item-api'
 import type { GitLabApi } from './api/gitlab-api'
 import type { BitbucketApi, HostedReviewApi } from './api/hosted-review-api'
 import type { JiraApi } from './api/jira-api'
+import type { IssueClientDataApi } from './api/issue-client-data-api'
 import type { LinearApi } from './api/linear-api'
 import type { MobileApi } from './api/mobile-api'
 import type { NativeChatApi } from './api/native-chat-api'
@@ -89,6 +90,7 @@ export type PreloadApi = {
   bitbucket: BitbucketApi
   linear: LinearApi
   jira: JiraApi
+  issueClientData: IssueClientDataApi
   starNag: StarNagApi
   telemetryTrack: TelemetryApi['telemetryTrack']
   telemetrySetOptIn: TelemetryApi['telemetrySetOptIn']
diff --git a/src/preload/api/dashboard-api.ts b/src/preload/api/dashboard-api.ts
index aa8d6dab..a17f7429 100644
--- a/src/preload/api/dashboard-api.ts
+++ b/src/preload/api/dashboard-api.ts
@@ -1,9 +1,11 @@
 import type {
+  DashboardOpenIssueArgs,
   DashboardRevealAgentArgs,
   DashboardSleepWorkspaceArgs,
   DashboardSnapshot,
   DashboardSpawnAgentArgs
 } from '../../shared/dashboard-snapshot'
+import type { ExecutionHostId } from '../../shared/execution-host'
 import type {
   TerminalPreviewConnectResult,
   TerminalPreviewDataPayload
@@ -16,14 +18,21 @@ export type DashboardApi = {
   onPopoutOpenChanged: (callback: (open: boolean) => void) => () => void
   onSnapshotRequested: (callback: () => void) => () => void
   onRevealAgent: (callback: (args: DashboardRevealAgentArgs) => void) => () => void
-  onAckAgent: (callback: (paneKey: string) => void) => () => void
+  onAckAgent: (
+    callback: (paneKey: string, executionHostId?: ExecutionHostId) => void
+  ) => () => void
+  onOpenIssue: (callback: (args: DashboardOpenIssueArgs) => void) => () => void
   onSpawnAgent: (callback: (args: DashboardSpawnAgentArgs) => void) => () => void
   onSleepWorkspace: (callback: (args: DashboardSleepWorkspaceArgs) => void) => () => void
   requestSnapshot: () => Promise<void>
   onSnapshot: (callback: (snapshot: DashboardSnapshot) => void) => () => void
   onViewRequested: (callback: (view: 'board' | 'map') => void) => () => void
   revealAgent: (args: DashboardRevealAgentArgs) => Promise<void>
-  ackAgent: (paneKey: string) => Promise<void>
+  ackAgent: (
+    paneKey: string,
+    executionHostId?: ExecutionHostId
+  ) => Promise<void>
+  openIssue: (args: DashboardOpenIssueArgs) => Promise<void>
   spawnAgent: (args: DashboardSpawnAgentArgs) => Promise<void>
   sleepWorkspace: (args: DashboardSleepWorkspaceArgs) => Promise<void>
 }
diff --git a/src/preload/api/pty-api.ts b/src/preload/api/pty-api.ts
index d69d9060..fdb9488e 100644
--- a/src/preload/api/pty-api.ts
+++ b/src/preload/api/pty-api.ts
@@ -31,6 +31,7 @@ export type PtyApi = {
     resumeProviderSession?: AgentProviderSessionMetadata
     launchToken?: string
     launchAgent?: TuiAgent
+    issueId?: string
     startupCommandDelivery?: StartupCommandDelivery
     connectionId?: string | null
     worktreeId?: string
@@ -48,6 +49,7 @@ export type PtyApi = {
     telemetry?: { agent_kind: AgentKind; launch_source: LaunchSource; request_kind: RequestKind }
   }) => Promise<{
     id: string
+    conversationId?: string
     launchAgent?: TuiAgent
     launchConfig?: SleepingAgentLaunchConfig
     snapshot?: string
diff --git a/src/preload/api/ui-command-event-api.ts b/src/preload/api/ui-command-event-api.ts
index 6ec32503..3584dbc4 100644
--- a/src/preload/api/ui-command-event-api.ts
+++ b/src/preload/api/ui-command-event-api.ts
@@ -27,6 +27,7 @@ import type {
   TerminalTabCloseRequest,
   TerminalTabCloseResponse
 } from '../../shared/terminal-tab-close'
+import type { IssueNotificationNavigationTarget } from '../../shared/issues/notification-types'
 
 export type UiCommandEventApi = {
   get: () => Promise<PersistedUIState>
@@ -118,6 +119,9 @@ export type UiCommandEventApi = {
       defaultTabs?: WorktreeDefaultTabsLaunch
     }) => void
   ) => () => void
+  onOpenIssueNotification?: (
+    callback: (data: IssueNotificationNavigationTarget) => void
+  ) => () => void
   onCreateTerminal: (
     callback: (data: {
       requestId?: string
@@ -129,6 +133,7 @@ export type UiCommandEventApi = {
       resumeProviderSession?: AgentProviderSessionMetadata
       launchToken?: string
       launchAgent?: TuiAgent
+      issueId?: string
       viewMode?: 'terminal' | 'chat'
       title?: string
       ptyId?: string
diff --git a/src/preload/index.ts b/src/preload/index.ts
index c5bb2959..502b8343 100644
--- a/src/preload/index.ts
+++ b/src/preload/index.ts
@@ -6,6 +6,8 @@ import { glApi } from './gitlab'
 import type { AppIdentity } from '../shared/app-identity'
 import type { ComputerAwakeStatus } from '../shared/computer-awake-mode'
 import type {
+  DashboardAckAgentArgs,
+  DashboardOpenIssueArgs,
   DashboardRevealAgentArgs,
   DashboardSleepWorkspaceArgs,
   DashboardSnapshot,
@@ -112,6 +114,8 @@ import type {
 import type { GitPushTarget, WorktreeHeadIdentity } from '../shared/worktree/types'
 import type { PtyModelRestoreNeededEvent } from '../shared/pty-model-restore-marker'
 import type { PtyListedSession } from '../shared/pty-listed-session'
+import type { IssueNotificationNavigationTarget } from '../shared/issues/notification-types'
+import type { IssueFactsChangedEvent } from '../shared/issues/types'
 import type {
   PtyRendererDeliveryHealthReply,
   PtyRendererDeliveryStateReport
@@ -567,6 +571,22 @@ const api = {
     orgMemberRemove: (args) => ipcRenderer.invoke('orcaProfiles:orgMemberRemove', args)
   } satisfies PreloadApi['orcaProfiles'],
 
+  issueClientData: {
+    readCache: (args) => ipcRenderer.invoke('issues:readCache', args),
+    replaceCache: (args) => ipcRenderer.invoke('issues:replaceCache', args),
+    readPreferences: () => ipcRenderer.invoke('issues:readPreferences'),
+    writePreferences: (preferences) =>
+      ipcRenderer.invoke('issues:writePreferences', preferences),
+    onFactsChanged: (callback): (() => void) => {
+      const listener = (
+        _event: Electron.IpcRendererEvent,
+        change: IssueFactsChangedEvent
+      ): void => callback(change)
+      ipcRenderer.on('issues:factsChanged', listener)
+      return () => ipcRenderer.removeListener('issues:factsChanged', listener)
+    }
+  } satisfies PreloadApi['issueClientData'],
+
   platform: {
     get: () => ({
       platform: process.platform,
@@ -963,6 +983,7 @@ const api = {
       resumeProviderSession?: AgentProviderSessionMetadata
       launchToken?: string
       launchAgent?: TuiAgent
+      issueId?: string
       startupCommandDelivery?: StartupCommandDelivery
       connectionId?: string | null
       worktreeId?: string
@@ -979,6 +1000,7 @@ const api = {
       telemetry?: { agent_kind: AgentKind; launch_source: LaunchSource; request_kind: RequestKind }
     }): Promise<{
       id: string
+      conversationId?: string
       launchConfig?: SleepingAgentLaunchConfig
       snapshot?: string
       snapshotCols?: number
@@ -2384,12 +2406,31 @@ const api = {
       ipcRenderer.on('ui:revealDashboardAgent', listener)
       return () => ipcRenderer.removeListener('ui:revealDashboardAgent', listener)
     },
-    onAckAgent: (callback: (paneKey: string) => void): (() => void) => {
-      const listener = (_event: Electron.IpcRendererEvent, paneKey: string): void =>
-        callback(paneKey)
+    onAckAgent: (
+      callback: (
+        paneKey: string,
+        executionHostId?: DashboardAckAgentArgs['executionHostId']
+      ) => void
+    ): (() => void) => {
+      const listener = (
+        _event: Electron.IpcRendererEvent,
+        value: string | DashboardAckAgentArgs
+      ): void => {
+        if (typeof value === 'string') {
+          callback(value)
+          return
+        }
+        callback(value.paneKey, value.executionHostId)
+      }
       ipcRenderer.on('ui:ackDashboardAgent', listener)
       return () => ipcRenderer.removeListener('ui:ackDashboardAgent', listener)
     },
+    onOpenIssue: (callback: (args: DashboardOpenIssueArgs) => void): (() => void) => {
+      const listener = (_event: Electron.IpcRendererEvent, args: DashboardOpenIssueArgs): void =>
+        callback(args)
+      ipcRenderer.on('ui:openDashboardIssue', listener)
+      return () => ipcRenderer.removeListener('ui:openDashboardIssue', listener)
+    },
     onSpawnAgent: (callback: (args: DashboardSpawnAgentArgs) => void): (() => void) => {
       const listener = (_event: Electron.IpcRendererEvent, args: DashboardSpawnAgentArgs): void =>
         callback(args)
@@ -2421,8 +2462,16 @@ const api = {
     },
     revealAgent: (args: DashboardRevealAgentArgs): Promise<void> =>
       ipcRenderer.invoke('dashboardPopout:revealAgent', args),
-    ackAgent: (paneKey: string): Promise<void> =>
-      ipcRenderer.invoke('dashboardPopout:ackAgent', { paneKey }),
+    ackAgent: (
+      paneKey: string,
+      executionHostId?: DashboardAckAgentArgs['executionHostId']
+    ): Promise<void> =>
+      ipcRenderer.invoke('dashboardPopout:ackAgent', {
+        paneKey,
+        ...(executionHostId ? { executionHostId } : {})
+      }),
+    openIssue: (args: DashboardOpenIssueArgs): Promise<void> =>
+      ipcRenderer.invoke('dashboardPopout:openIssue', args),
     spawnAgent: (args: DashboardSpawnAgentArgs): Promise<void> =>
       ipcRenderer.invoke('dashboardPopout:spawnAgent', args),
     sleepWorkspace: (args: DashboardSleepWorkspaceArgs): Promise<void> =>
@@ -3869,6 +3918,17 @@ const api = {
       ipcRenderer.on('ui:activateWorktree', listener)
       return () => ipcRenderer.removeListener('ui:activateWorktree', listener)
     },
+    onOpenIssueNotification: (
+      callback: (data: IssueNotificationNavigationTarget) => void
+    ): (() => void) => {
+      const listener = (
+        _event: Electron.IpcRendererEvent,
+        data: IssueNotificationNavigationTarget
+      ): void => callback(data)
+      ipcRenderer.on('ui:openIssueNotification', listener)
+      return () =>
+        ipcRenderer.removeListener('ui:openIssueNotification', listener)
+    },
     onCreateTerminal: (
       callback: (data: {
         requestId?: string
@@ -3880,6 +3940,7 @@ const api = {
         resumeProviderSession?: AgentProviderSessionMetadata
         launchToken?: string
         launchAgent?: TuiAgent
+        issueId?: string
         viewMode?: 'terminal' | 'chat'
         title?: string
         ptyId?: string
@@ -3906,6 +3967,7 @@ const api = {
           resumeProviderSession?: AgentProviderSessionMetadata
           launchToken?: string
           launchAgent?: TuiAgent
+          issueId?: string
           viewMode?: 'terminal' | 'chat'
           title?: string
           ptyId?: string
diff --git a/src/relay/agent-hook-server.ts b/src/relay/agent-hook-server.ts
index 6a281a7e..f7d628c7 100644
--- a/src/relay/agent-hook-server.ts
+++ b/src/relay/agent-hook-server.ts
@@ -319,6 +319,7 @@ export class RelayAgentHookServer {
       promptInteractionKey: event.promptInteractionKey,
       hookEventName: event.hookEventName,
       providerPromptId: event.providerPromptId,
+      providerTurnId: event.providerTurnId,
       compactTrigger: event.compactTrigger,
       toolUseId: event.toolUseId,
       toolAgentId: event.toolAgentId,
diff --git a/src/renderer/src/App.tsx b/src/renderer/src/App.tsx
index f85eb212..9df4613d 100644
--- a/src/renderer/src/App.tsx
+++ b/src/renderer/src/App.tsx
@@ -339,6 +339,7 @@ const WorktreeCreationPanel = lazy(
   () => import('./components/worktree-creation/WorktreeCreationPanel')
 )
 const TaskPage = lazy(() => import('./components/TaskPage'))
+const IssuesPage = lazy(() => import('./components/issues/IssuesPage'))
 const AutomationsPage = lazy(() => import('./components/automations/AutomationsPage'))
 const ActivityPrototypePage = lazy(() => import('./components/activity/ActivityPrototypePage'))
 const Settings = lazy(() => import('./components/settings/Settings'))
@@ -2423,6 +2424,7 @@ function App(): React.JSX.Element {
                               {activeView === 'settings' ? <Settings /> : null}
                               {activeView === 'artifacts' ? <ArtifactsPage /> : null}
                               {activeView === 'tasks' ? <TaskPage /> : null}
+                              {activeView === 'issues' ? <IssuesPage /> : null}
                               {activeView === 'automations' ? <AutomationsPage /> : null}
                               {activeView === 'activity' ? <ActivityPrototypePage /> : null}
                               {activeView === 'space' ? <WorkspaceSpacePage /> : null}
diff --git a/src/renderer/src/components/activity/ActivityPrototypePage.tsx b/src/renderer/src/components/activity/ActivityPrototypePage.tsx
index 6157c796..02e3b71c 100644
--- a/src/renderer/src/components/activity/ActivityPrototypePage.tsx
+++ b/src/renderer/src/components/activity/ActivityPrototypePage.tsx
@@ -1,6 +1,7 @@
 /* eslint-disable max-lines -- Why: prototype keeps the real-data adapter and visual skeleton together until a refinement pass splits them into modules. */
 import React, { useCallback, useEffect, useLayoutEffect, useMemo, useRef, useState } from 'react'
 import { useShallow } from 'zustand/react/shallow'
+import { toast } from 'sonner'
 import {
   Bell,
   BellDot,
@@ -84,6 +85,21 @@ import {
   resolveActivityThreadStatusPreview
 } from '@/lib/activity-thread-display'
 import { getAgentRowPrimaryText } from '@/lib/agent-row-primary-text'
+import { useSidebarHostScopeOptions } from '../sidebar/use-sidebar-host-scope-options'
+import {
+  overlayIssueActivityThreads
+} from '@/issues/issue-activity-thread-overlay'
+import { useIssueActivityTimeline } from '@/issues/use-issue-activity-timeline'
+import {
+  createIssueActivityReadDependencies,
+  markIssueActivityRead
+} from '@/issues/issue-activity-read'
+import { issueActivityStore } from '@/issues/issue-activity-store'
+import { openIssueConversation } from '@/issues/issue-conversation-navigation'
+import { openIssueDetail } from '@/issues/issue-navigation'
+import type { IssueActivityThreadProjection } from '@/issues/issue-activity-projection'
+import { getWorktreeExecutionHostId } from '../../../../shared/execution-host'
+import { folderWorkspaceKey } from '../../../../shared/workspace-scope'
 
 type ThreadReadFilter = 'all' | 'unread'
 type ActivityGroupBy = 'status' | 'project' | 'worktree' | 'agent'
@@ -117,6 +133,7 @@ type ActivityLiveAgentSnapshot = {
 
 // Why: keyed per agent pane (tab + leaf id), not per workspace, so the list shows one row per agent; paneKey is `${tabId}:${leafId}`.
 type AgentPaneThread = {
+  threadKey: string
   paneKey: string
   paneTitle: string
   worktree: Worktree
@@ -131,6 +148,7 @@ type AgentPaneThread = {
   events: ActivityEvent[]
   migrationUnsupportedPtyId?: string
   unread: boolean
+  issueActivity?: IssueActivityThreadProjection
 }
 
 type ActivityThreadGroup = {
@@ -775,6 +793,7 @@ export function buildAgentPaneThreads(args: {
     const existing = byPaneKey.get(paneKey)
     if (!existing) {
       byPaneKey.set(paneKey, {
+        threadKey: paneKey,
         paneKey,
         paneTitle: paneTitleForEvent(event, generatedTitlesEnabled),
         worktree: event.worktree,
@@ -814,6 +833,7 @@ export function buildAgentPaneThreads(args: {
     const existing = byPaneKey.get(paneKey)
     if (!existing) {
       byPaneKey.set(paneKey, {
+        threadKey: paneKey,
         paneKey,
         paneTitle: paneTitleForEntry(liveAgent.entry, liveAgent.tab, generatedTitlesEnabled),
         worktree: liveAgent.worktree,
@@ -1221,6 +1241,7 @@ function ThreadRow({
   onSelect,
   onJump,
   onMarkUnread,
+  canMarkUnread,
   canJump,
   compactMode
 }: {
@@ -1229,6 +1250,7 @@ function ThreadRow({
   onSelect: () => void
   onJump: () => void
   onMarkUnread: () => void
+  canMarkUnread: boolean
   canJump: boolean
   compactMode: boolean
 }): React.JSX.Element {
@@ -1363,7 +1385,7 @@ function ThreadRow({
                       'Unread'
                     )}
                   />
-                ) : (
+                ) : canMarkUnread ? (
                   <Tooltip>
                     <TooltipTrigger asChild>
                       <button
@@ -1393,6 +1415,8 @@ function ThreadRow({
                       )}
                     </TooltipContent>
                   </Tooltip>
+                ) : (
+                  <Bell className="size-3 text-muted-foreground/25" aria-hidden="true" />
                 )}
               </span>
               <EventTime timestamp={thread.latestTimestamp} />
@@ -1410,8 +1434,8 @@ export default function ActivityPrototypePage(): React.JSX.Element {
   const [query, setQuery] = useState('')
   const activityFilterInputRef = useRef<HTMLInputElement | null>(null)
   const [compactMode, setCompactMode] = useState(false)
-  const [selectedPaneKey, setSelectedPaneKey] = useState<string | null>(null)
-  const [displayedPaneKey, setDisplayedPaneKey] = useState<string | null>(null)
+  const [selectedThreadKey, setSelectedThreadKey] = useState<string | null>(null)
+  const [displayedThreadKey, setDisplayedThreadKey] = useState<string | null>(null)
   const [activePortalSlotId, setActivePortalSlotId] =
     useState<ActivityTerminalPortalSlotId>('primary')
   const [primaryPortalTargetEl, setPrimaryPortalTargetEl] = useState<HTMLElement | null>(null)
@@ -1439,6 +1463,7 @@ export default function ActivityPrototypePage(): React.JSX.Element {
       tabsByWorktree: s.tabsByWorktree,
       worktreeMap: getWorktreeMapFromState(s),
       repoMap: getRepoMapFromState(s),
+      getKnownWorktreeById: s.getKnownWorktreeById,
       acknowledgedAgentsByPaneKey: s.acknowledgedAgentsByPaneKey,
       acknowledgeAgents: s.acknowledgeAgents,
       unacknowledgeAgents: s.unacknowledgeAgents,
@@ -1447,6 +1472,12 @@ export default function ActivityPrototypePage(): React.JSX.Element {
   )
   // Why: agentStatusEpoch is a dep (not used in the body) so the memo recomputes when freshness boundaries expire even without new PTY data.
   const agentStatusEpoch = useAppStore((s) => s.agentStatusEpoch)
+  const { hostOptions } = useSidebarHostScopeOptions()
+  const issueHostIds = useMemo(
+    () => hostOptions.map((host) => host.id),
+    [hostOptions]
+  )
+  const issueTimeline = useIssueActivityTimeline(issueHostIds)
 
   const { events: allEvents, liveAgentByPaneKey } = useMemo(
     () =>
@@ -1465,7 +1496,7 @@ export default function ActivityPrototypePage(): React.JSX.Element {
     [storeData, agentStatusEpoch]
   )
 
-  const allThreads = useMemo(
+  const legacyThreads = useMemo(
     () =>
       buildAgentPaneThreads({
         events: allEvents,
@@ -1474,12 +1505,66 @@ export default function ActivityPrototypePage(): React.JSX.Element {
       }),
     [allEvents, liveAgentByPaneKey, storeData.generatedTitlesEnabled]
   )
-  const selectedPaneKeyIsLive =
-    selectedPaneKey === null || allThreads.some((thread) => thread.paneKey === selectedPaneKey)
-  const effectiveSelectedPaneKey = selectedPaneKeyIsLive ? selectedPaneKey : null
-  if (!selectedPaneKeyIsLive) {
+  const allThreads = useMemo(
+    () =>
+      overlayIssueActivityThreads({
+        legacyThreads,
+        projections: issueTimeline.projections,
+        authoritativeRoutes: issueTimeline.authoritativeRoutes,
+        routeForLegacyThread: (thread) =>
+          getWorktreeExecutionHostId(
+            thread.worktree,
+            thread.repo ?? undefined
+          ),
+        resolveWorkspace: (projection) => {
+          const conversation = projection.conversation
+          if (!conversation) {
+            return null
+          }
+          const workspaceId =
+            conversation.workspaceRef.type === 'folder'
+              ? folderWorkspaceKey(
+                  conversation.workspaceRef.folderWorkspaceId
+                )
+              : conversation.workspaceRef.worktreeId
+          const worktree = storeData.getKnownWorktreeById(
+            workspaceId,
+            projection.routeExecutionHostId
+          )
+          if (!worktree) {
+            return null
+          }
+          const parsedPane = projection.paneKey
+            ? parsePaneKey(projection.paneKey)
+            : null
+          return {
+            worktree,
+            repo: storeData.repoMap.get(worktree.repoId) ?? null,
+            tab: parsedPane
+              ? (storeData.tabsByWorktree[worktree.id] ?? []).find(
+                  (tab) => tab.id === parsedPane.tabId
+                ) ?? null
+              : null
+          }
+        }
+      }),
+    [
+      issueTimeline,
+      legacyThreads,
+      storeData.getKnownWorktreeById,
+      storeData.repoMap,
+      storeData.tabsByWorktree
+    ]
+  )
+  const selectedThreadKeyIsLive =
+    selectedThreadKey === null ||
+    allThreads.some((thread) => thread.threadKey === selectedThreadKey)
+  const effectiveSelectedThreadKey = selectedThreadKeyIsLive
+    ? selectedThreadKey
+    : null
+  if (!selectedThreadKeyIsLive) {
     // Why: rows disappear when agent retention or tab state changes; clear stale selection before detail/portal rendering targets it.
-    setSelectedPaneKey(null)
+    setSelectedThreadKey(null)
   }
 
   const visibleThreads = useMemo(() => {
@@ -1489,7 +1574,7 @@ export default function ActivityPrototypePage(): React.JSX.Element {
       if (
         readFilter === 'unread' &&
         !thread.unread &&
-        thread.paneKey !== effectiveSelectedPaneKey
+        thread.threadKey !== effectiveSelectedThreadKey
       ) {
         return false
       }
@@ -1498,14 +1583,14 @@ export default function ActivityPrototypePage(): React.JSX.Element {
       }
       return activityThreadMatchesSearchQuery({ thread, searchQuery: normalizedQuery })
     })
-  }, [allThreads, readFilter, query, effectiveSelectedPaneKey])
+  }, [allThreads, readFilter, query, effectiveSelectedThreadKey])
   const visibleThreadGroups = useMemo(
     () => buildActivityThreadGroups(visibleThreads, groupBy),
     [visibleThreads, groupBy]
   )
 
-  const selectedThread = effectiveSelectedPaneKey
-    ? (allThreads.find((thread) => thread.paneKey === effectiveSelectedPaneKey) ?? null)
+  const selectedThread = effectiveSelectedThreadKey
+    ? (allThreads.find((thread) => thread.threadKey === effectiveSelectedThreadKey) ?? null)
     : null
   const selectedTabId = selectedThread?.tab.id ?? null
   // Why: repo-less terminal buckets can produce Activity rows, but the workspace Terminal tree only portals real worktrees.
@@ -1515,8 +1600,8 @@ export default function ActivityPrototypePage(): React.JSX.Element {
           (tab) => tab.id === selectedTabId
         )
       : false
-  const displayedThread = displayedPaneKey
-    ? (allThreads.find((thread) => thread.paneKey === displayedPaneKey) ?? null)
+  const displayedThread = displayedThreadKey
+    ? (allThreads.find((thread) => thread.threadKey === displayedThreadKey) ?? null)
     : null
   const displayedTabId = displayedThread?.tab.id ?? null
   const displayedHasLiveTab =
@@ -1614,17 +1699,17 @@ export default function ActivityPrototypePage(): React.JSX.Element {
       stagedPortalUnavailable
     })
     if (swap?.kind === 'clear') {
-      setDisplayedPaneKey(null)
+      setDisplayedThreadKey(null)
       return
     }
     if (swap?.kind === 'swap-staged') {
       // Why: a stale selected pane must swap to the unavailable state, not leave the previous pane visible under the new row.
       setActivePortalSlotId(inactivePortalSlotId)
-      setDisplayedPaneKey(swap.paneKey)
+      setDisplayedThreadKey(stagedThread?.threadKey ?? null)
       return
     }
     if (swap?.kind === 'settle-visible') {
-      setDisplayedPaneKey(swap.paneKey)
+      setDisplayedThreadKey(visibleThread?.threadKey ?? null)
     }
   }, [
     inactivePortalSlotId,
@@ -1665,25 +1750,61 @@ export default function ActivityPrototypePage(): React.JSX.Element {
     return () => window.removeEventListener('keydown', focusActivityFilter, { capture: true })
   }, [activePortalTargetEl, inactivePortalTargetEl])
 
-  const markThreadRead = (thread: AgentPaneThread): void => {
-    storeData.acknowledgeAgents([thread.paneKey])
-  }
+  const pendingIssueReadsRef = useRef(new Set<string>())
+  const markThreadRead = useCallback(
+    (thread: AgentPaneThread): void => {
+      const projection = thread.issueActivity
+      if (!projection) {
+        storeData.acknowledgeAgents([thread.paneKey])
+        return
+      }
+      if (
+        projection.unreadRoundRecordIds.length === 0 ||
+        pendingIssueReadsRef.current.has(thread.threadKey)
+      ) {
+        return
+      }
+      pendingIssueReadsRef.current.add(thread.threadKey)
+      void markIssueActivityRead(
+        [
+          {
+            routeExecutionHostId: projection.routeExecutionHostId,
+            roundRecordIds: projection.unreadRoundRecordIds
+          }
+        ],
+        Date.now(),
+        createIssueActivityReadDependencies(
+          issueActivityStore.getState().patchRoundsRead
+        )
+      )
+        .catch(() => {
+          toast.error('Unable to mark Activity read')
+        })
+        .finally(() => {
+          pendingIssueReadsRef.current.delete(thread.threadKey)
+        })
+    },
+    [storeData.acknowledgeAgents]
+  )
 
   const markThreadUnread = (thread: AgentPaneThread): void => {
+    if (thread.issueActivity) {
+      return
+    }
     storeData.unacknowledgeAgents([thread.paneKey])
   }
 
-  const activateThreadTerminal = (thread: AgentPaneThread): void => {
+  const activateThreadTerminal = (thread: AgentPaneThread): boolean => {
     const state = useAppStore.getState()
     const worktree = getWorktreeMapFromState(state).get(thread.worktree.id)
     if (!worktree) {
-      return
+      return false
     }
     // Why: retained-agent threads can outlive their tab; without a live tab, reorienting the workspace and focusing a dead tab id would just confuse the user.
     const liveTabs = state.tabsByWorktree[worktree.id] ?? []
     const hasLiveTab = liveTabs.some((t) => t.id === thread.tab.id)
     if (!hasLiveTab) {
-      return
+      return false
     }
     if (state.activeRepoId !== worktree.repoId) {
       state.setActiveRepo(worktree.repoId)
@@ -1698,11 +1819,28 @@ export default function ActivityPrototypePage(): React.JSX.Element {
       parsed && parsed.tabId === thread.tab.id ? parsed.leafId : null,
       { scrollToBottomIfOutputSinceLastView: true }
     )
+    return true
   }
 
   const selectThread = (thread: AgentPaneThread): void => {
-    setSelectedPaneKey(thread.paneKey)
-    activateThreadTerminal(thread)
+    setSelectedThreadKey(thread.threadKey)
+    if (activateThreadTerminal(thread)) {
+      return
+    }
+    const projection = thread.issueActivity
+    if (projection?.conversation) {
+      void openIssueConversation({
+        routeExecutionHostId: projection.routeExecutionHostId,
+        conversation: projection.conversation
+      })
+      return
+    }
+    if (projection?.issue) {
+      openIssueDetail({
+        authority: projection.authority,
+        issueId: projection.issue.id
+      })
+    }
   }
 
   useEffect(() => {
@@ -1710,23 +1848,24 @@ export default function ActivityPrototypePage(): React.JSX.Element {
       !selectedThread ||
       !selectedThread.unread ||
       stagedThread ||
-      selectedThread.paneKey !== effectiveSelectedPaneKey
+      selectedThread.threadKey !== effectiveSelectedThreadKey
     ) {
       return
     }
     const selectedThreadHasDetailOnlyView =
       !selectedHasLiveTab || selectedThread.migrationUnsupportedPtyId !== undefined
     const selectedThreadIsVisibleTerminal =
-      visibleThread?.paneKey === effectiveSelectedPaneKey && visiblePortalReady
+      visibleThread?.threadKey === effectiveSelectedThreadKey &&
+      visiblePortalReady
     if (selectedThreadHasDetailOnlyView || selectedThreadIsVisibleTerminal) {
-      storeData.acknowledgeAgents([selectedThread.paneKey])
+      markThreadRead(selectedThread)
     }
   }, [
     selectedHasLiveTab,
-    effectiveSelectedPaneKey,
+    effectiveSelectedThreadKey,
+    markThreadRead,
     selectedThread,
     stagedThread,
-    storeData,
     visiblePortalReady,
     visibleThread
   ])
@@ -1743,11 +1882,36 @@ export default function ActivityPrototypePage(): React.JSX.Element {
   const hasUnreadThreads = allThreads.some((thread) => thread.unread)
 
   const markAllThreadsRead = (): void => {
-    const unreadKeys = allThreads.filter((t) => t.unread).map((t) => t.paneKey)
-    if (unreadKeys.length === 0) {
-      return
+    const unreadThreads = allThreads.filter((thread) => thread.unread)
+    const legacyKeys = unreadThreads.flatMap((thread) =>
+      thread.issueActivity ? [] : [thread.paneKey]
+    )
+    if (legacyKeys.length > 0) {
+      storeData.acknowledgeAgents(legacyKeys)
+    }
+    const issueTargets = unreadThreads.flatMap((thread) =>
+      thread.issueActivity
+        ? [
+            {
+              routeExecutionHostId:
+                thread.issueActivity.routeExecutionHostId,
+              roundRecordIds:
+                thread.issueActivity.unreadRoundRecordIds
+            }
+          ]
+        : []
+    )
+    if (issueTargets.length > 0) {
+      void markIssueActivityRead(
+        issueTargets,
+        Date.now(),
+        createIssueActivityReadDependencies(
+          issueActivityStore.getState().patchRoundsRead
+        )
+      ).catch(() => {
+        toast.error('Unable to mark Activity read')
+      })
     }
-    storeData.acknowledgeAgents(unreadKeys)
   }
 
   // Why (page padding): no top/horizontal padding so the page reaches the window edges; the titlebar and the right pane's title row (pt-2) supply the top spacing.
@@ -1865,12 +2029,13 @@ export default function ActivityPrototypePage(): React.JSX.Element {
                 <ActivityStatusGroupHeader group={group} />
                 {group.threads.map((thread) => (
                   <ThreadRow
-                    key={thread.paneKey}
+                    key={thread.threadKey}
                     thread={thread}
-                    selected={thread.paneKey === selectedThread?.paneKey}
+                    selected={thread.threadKey === selectedThread?.threadKey}
                     onSelect={() => selectThread(thread)}
                     onJump={() => jumpToWorkspace(thread)}
                     onMarkUnread={() => markThreadUnread(thread)}
+                    canMarkUnread={!thread.issueActivity}
                     canJump={storeData.worktreeMap.has(thread.worktree.id)}
                     compactMode={compactMode}
                   />
diff --git a/src/renderer/src/components/activity/useActivityUnreadCount.test.ts b/src/renderer/src/components/activity/useActivityUnreadCount.test.ts
index 5c28a132..d831f73b 100644
--- a/src/renderer/src/components/activity/useActivityUnreadCount.test.ts
+++ b/src/renderer/src/components/activity/useActivityUnreadCount.test.ts
@@ -1,5 +1,6 @@
 import { describe, expect, it } from 'vitest'
 import type { AgentStatusEntry } from '../../../../shared/agent-status-types'
+import type { IssueActivityThreadProjection } from '../../issues/issue-activity-projection'
 import { countActivityUnread } from './useActivityUnreadCount'
 
 const PANE = 'tab-1:11111111-1111-4111-8111-111111111111'
@@ -27,6 +28,15 @@ function makeSource(entry: AgentStatusEntry, ackAt = 0) {
   }
 }
 
+function issueProjection(read: boolean): IssueActivityThreadProjection {
+  return {
+    routeExecutionHostId: 'local',
+    paneKey: PANE,
+    unread: !read,
+    unreadRoundRecordIds: read ? [] : ['round-1']
+  } as unknown as IssueActivityThreadProjection
+}
+
 describe('countActivityUnread session-boundary rows (STA-3386)', () => {
   it('does not count a session-boundary done as unread in either mode', () => {
     const source = makeSource(makeEntry({ sessionBoundary: true }))
@@ -63,4 +73,33 @@ describe('countActivityUnread session-boundary rows (STA-3386)', () => {
     const source = makeSource(makeEntry({}))
     expect(countActivityUnread(source, 'sidebar-badge')).toBe(1)
   })
+
+  it('uses Round readAt instead of pane acknowledgement for authoritative Activity', () => {
+    const source = makeSource(makeEntry({}), 0)
+
+    expect(
+      countActivityUnread(source, 'agent-events', {
+        authoritativeRoutes: new Set(['local']),
+        projections: [issueProjection(true)],
+        routeExecutionHostIdByPaneKey: { [PANE]: 'local' }
+      })
+    ).toBe(0)
+
+    expect(
+      countActivityUnread(makeSource(makeEntry({}), 3_000), 'agent-events', {
+        authoritativeRoutes: new Set(['local']),
+        projections: [issueProjection(false)],
+        routeExecutionHostIdByPaneKey: { [PANE]: 'local' }
+      })
+    ).toBe(1)
+  })
+
+  it('keeps pane acknowledgement as the fallback for unsupported hosts', () => {
+    expect(
+      countActivityUnread(makeSource(makeEntry({})), 'agent-events', {
+        authoritativeRoutes: new Set(),
+        projections: []
+      })
+    ).toBe(1)
+  })
 })
diff --git a/src/renderer/src/components/activity/useActivityUnreadCount.ts b/src/renderer/src/components/activity/useActivityUnreadCount.ts
index 91b51c44..d888bff8 100644
--- a/src/renderer/src/components/activity/useActivityUnreadCount.ts
+++ b/src/renderer/src/components/activity/useActivityUnreadCount.ts
@@ -2,9 +2,18 @@ import { useMemo } from 'react'
 import { useShallow } from 'zustand/react/shallow'
 
 import { migrationUnsupportedToAgentStatusEntry } from '@/lib/migration-unsupported-agent-entry'
+import { resolveLegacyActivityPaneRoutes } from '@/issues/legacy-activity-pane-routes'
+import type { IssueActivityTimeline } from '@/issues/use-issue-activity-timeline'
+import { useIssueActivityTimeline } from '@/issues/use-issue-activity-timeline'
 import { useAppStore } from '@/store'
 import type { AppState } from '@/store/types'
+import { getAllWorktreesFromState } from '@/store/selectors'
+import { useSidebarHostScopeOptions } from '@/components/sidebar/use-sidebar-host-scope-options'
 import type { AgentStatusEntry, AgentStatusState } from '../../../../shared/agent-status-types'
+import {
+  toSshExecutionHostId,
+  type ExecutionHostId
+} from '../../../../shared/execution-host'
 
 type ActivityUnreadCountSource = Pick<
   AppState,
@@ -17,17 +26,30 @@ type ActivityUnreadCountSource = Pick<
 
 type ActivityUnreadCountMode = 'agent-events' | 'sidebar-badge'
 
+export type IssueActivityUnreadState = Pick<
+  IssueActivityTimeline,
+  'authoritativeRoutes' | 'projections'
+> & {
+  routeExecutionHostIdByPaneKey?: Readonly<
+    Partial<Record<string, ExecutionHostId>>
+  >
+}
+
 const EMPTY_WORKTREES_BY_REPO: AppState['worktreesByRepo'] = {}
 const EMPTY_MIGRATION_UNSUPPORTED: AppState['migrationUnsupportedByPtyId'] = {}
 const EMPTY_RETAINED_AGENTS: AppState['retainedAgentsByPaneKey'] = {}
 const EMPTY_ACKNOWLEDGED_AGENTS: AppState['acknowledgedAgentsByPaneKey'] = {}
+const EMPTY_REPOS: AppState['repos'] = []
+const EMPTY_TABS_BY_WORKTREE: AppState['tabsByWorktree'] = {}
 
 const DISABLED_ACTIVITY_UNREAD_INPUTS = {
   sortEpoch: 0,
   worktreesByRepo: EMPTY_WORKTREES_BY_REPO,
   migrationUnsupportedByPtyId: EMPTY_MIGRATION_UNSUPPORTED,
+  repos: EMPTY_REPOS,
   retainedAgentsByPaneKey: EMPTY_RETAINED_AGENTS,
-  acknowledgedAgentsByPaneKey: EMPTY_ACKNOWLEDGED_AGENTS
+  acknowledgedAgentsByPaneKey: EMPTY_ACKNOWLEDGED_AGENTS,
+  tabsByWorktree: EMPTY_TABS_BY_WORKTREE
 }
 
 function isUnreadAgentState(state: AgentStatusState): boolean {
@@ -36,7 +58,8 @@ function isUnreadAgentState(state: AgentStatusState): boolean {
 
 export function countActivityUnread(
   source: ActivityUnreadCountSource,
-  mode: ActivityUnreadCountMode
+  mode: ActivityUnreadCountMode,
+  issueActivity?: IssueActivityUnreadState
 ): number {
   let count = 0
 
@@ -50,7 +73,38 @@ export function countActivityUnread(
     }
   }
 
-  const countEntry = (entry: AgentStatusEntry, ackAt: number): void => {
+  if (issueActivity) {
+    const unreadRoundIds = new Set<string>()
+    for (const projection of issueActivity.projections) {
+      if (
+        !issueActivity.authoritativeRoutes.has(
+          projection.routeExecutionHostId
+        )
+      ) {
+        continue
+      }
+      for (const roundRecordId of projection.unreadRoundRecordIds) {
+        unreadRoundIds.add(roundRecordId)
+      }
+    }
+    count += unreadRoundIds.size
+  }
+
+  const countEntry = (
+    paneKey: string,
+    entry: AgentStatusEntry,
+    ackAt: number,
+    routeOverride?: ExecutionHostId
+  ): void => {
+    const routeExecutionHostId =
+      issueActivity?.routeExecutionHostIdByPaneKey?.[paneKey] ??
+      routeOverride
+    if (
+      routeExecutionHostId &&
+      issueActivity?.authoritativeRoutes.has(routeExecutionHostId)
+    ) {
+      return
+    }
     if (mode === 'agent-events') {
       // Why: Activity feed surfaces historical done/blocked/waiting events
       // from stateHistory, so the titlebar badge must mirror that event count.
@@ -80,18 +134,35 @@ export function countActivityUnread(
   }
 
   for (const [paneKey, entry] of Object.entries(source.agentStatusByPaneKey)) {
-    countEntry(entry, source.acknowledgedAgentsByPaneKey[paneKey] ?? 0)
+    countEntry(
+      paneKey,
+      entry,
+      source.acknowledgedAgentsByPaneKey[paneKey] ?? 0
+    )
   }
   for (const [paneKey, retained] of Object.entries(source.retainedAgentsByPaneKey)) {
     if (mode === 'sidebar-badge' && retained.entry.state !== 'done') {
       continue
     }
-    countEntry(retained.entry, source.acknowledgedAgentsByPaneKey[paneKey] ?? 0)
+    countEntry(
+      paneKey,
+      retained.entry,
+      source.acknowledgedAgentsByPaneKey[paneKey] ?? 0
+    )
   }
   for (const unsupported of Object.values(source.migrationUnsupportedByPtyId)) {
     const entry = migrationUnsupportedToAgentStatusEntry(unsupported)
     if (entry) {
-      countEntry(entry, source.acknowledgedAgentsByPaneKey[entry.paneKey] ?? 0)
+      countEntry(
+        entry.paneKey,
+        entry,
+        source.acknowledgedAgentsByPaneKey[entry.paneKey] ?? 0,
+        unsupported.source === 'ssh' && entry.connectionId
+          ? toSshExecutionHostId(entry.connectionId)
+          : unsupported.source === 'local'
+            ? 'local'
+            : undefined
+      )
     }
   }
 
@@ -99,12 +170,23 @@ export function countActivityUnread(
 }
 
 export function useActivityUnreadCount(enabled: boolean, mode: ActivityUnreadCountMode): number {
+  const { hostOptions } = useSidebarHostScopeOptions()
+  const issueHostIds = useMemo(
+    () => hostOptions.map((host) => host.id),
+    [hostOptions]
+  )
+  const issueTimeline = useIssueActivityTimeline(
+    issueHostIds,
+    enabled
+  )
   const {
     sortEpoch,
     worktreesByRepo,
     migrationUnsupportedByPtyId,
+    repos,
     retainedAgentsByPaneKey,
-    acknowledgedAgentsByPaneKey
+    acknowledgedAgentsByPaneKey,
+    tabsByWorktree
   } = useAppStore(
     useShallow((state) => {
       if (!enabled) {
@@ -117,8 +199,10 @@ export function useActivityUnreadCount(enabled: boolean, mode: ActivityUnreadCou
         sortEpoch: state.sortEpoch,
         worktreesByRepo: state.worktreesByRepo,
         migrationUnsupportedByPtyId: state.migrationUnsupportedByPtyId,
+        repos: state.repos,
         retainedAgentsByPaneKey: state.retainedAgentsByPaneKey,
-        acknowledgedAgentsByPaneKey: state.acknowledgedAgentsByPaneKey
+        acknowledgedAgentsByPaneKey: state.acknowledgedAgentsByPaneKey,
+        tabsByWorktree: state.tabsByWorktree
       }
     })
   )
@@ -128,23 +212,54 @@ export function useActivityUnreadCount(enabled: boolean, mode: ActivityUnreadCou
       return 0
     }
     void sortEpoch
+    const state = useAppStore.getState()
+    const agentStatusByPaneKey = state.agentStatusByPaneKey
+    const entriesByPaneKey = {
+      ...Object.fromEntries(
+        Object.entries(retainedAgentsByPaneKey).map(
+          ([paneKey, retained]) => [
+            paneKey,
+            {
+              ...retained.entry,
+              worktreeId:
+                retained.entry.worktreeId ?? retained.worktreeId
+            }
+          ]
+        )
+      ),
+      ...agentStatusByPaneKey
+    }
+    const routeExecutionHostIdByPaneKey =
+      resolveLegacyActivityPaneRoutes({
+        entriesByPaneKey,
+        tabsByWorktree,
+        worktrees: getAllWorktreesFromState({ worktreesByRepo }),
+        repos
+      })
     return countActivityUnread(
       {
-        agentStatusByPaneKey: useAppStore.getState().agentStatusByPaneKey,
+        agentStatusByPaneKey,
         migrationUnsupportedByPtyId,
         retainedAgentsByPaneKey,
         worktreesByRepo,
         acknowledgedAgentsByPaneKey
       },
-      mode
+      mode,
+      {
+        ...issueTimeline,
+        routeExecutionHostIdByPaneKey
+      }
     )
   }, [
     acknowledgedAgentsByPaneKey,
     enabled,
+    issueTimeline,
     migrationUnsupportedByPtyId,
     mode,
+    repos,
     retainedAgentsByPaneKey,
     sortEpoch,
+    tabsByWorktree,
     worktreesByRepo
   ])
 }
diff --git a/src/renderer/src/components/dashboard-popout/AgentKanbanBoard.test.tsx b/src/renderer/src/components/dashboard-popout/AgentKanbanBoard.test.tsx
index fc3389d0..8cf3eb09 100644
--- a/src/renderer/src/components/dashboard-popout/AgentKanbanBoard.test.tsx
+++ b/src/renderer/src/components/dashboard-popout/AgentKanbanBoard.test.tsx
@@ -122,6 +122,7 @@ function renderBoard(
 }
 
 const ackAgent = vi.fn(async () => {})
+const openIssue = vi.fn(async () => {})
 
 describe('AgentKanbanBoard', () => {
   beforeEach(async () => {
@@ -132,7 +133,9 @@ describe('AgentKanbanBoard', () => {
       vi.fn(() => ({ matches: true, addEventListener: vi.fn(), removeEventListener: vi.fn() }))
     )
     // The board relays seen-acks through the dashboard preload API.
-    ;(window as unknown as { api: unknown }).api = { dashboard: { ackAgent } }
+    ;(window as unknown as { api: unknown }).api = {
+      dashboard: { ackAgent, openIssue }
+    }
   })
   afterEach(() => {
     cleanup()
@@ -569,7 +572,7 @@ describe('AgentKanbanBoard', () => {
     expect(screen.getByTestId('card').dataset.unseen).toBe('true')
 
     fireEvent.click(screen.getByTestId('card'))
-    expect(ackAgent).toHaveBeenCalledWith('pk-ack')
+    expect(ackAgent).toHaveBeenCalledWith('pk-ack', undefined)
     ackAgent.mockClear()
 
     // The ack round-trips through the main window; the next snapshot mutes it.
@@ -597,7 +600,7 @@ describe('AgentKanbanBoard', () => {
         }}
       />
     )
-    expect(ackAgent).toHaveBeenCalledWith('pk-ack')
+    expect(ackAgent).toHaveBeenCalledWith('pk-ack', undefined)
   })
 
   it('keeps an acknowledged result visible as a seen finish in the map without review state', async () => {
@@ -617,7 +620,7 @@ describe('AgentKanbanBoard', () => {
       await screen.findByRole('button', { name: /Fresh result/ }, MAP_LOAD_TIMEOUT)
     ).toBeInTheDocument()
     fireEvent.click(screen.getByRole('button', { name: /Fresh result/ }))
-    expect(ackAgent).toHaveBeenCalledWith('fresh-result')
+    expect(ackAgent).toHaveBeenCalledWith('fresh-result', undefined)
 
     view.rerender(
       <AgentKanbanBoard
diff --git a/src/renderer/src/components/dashboard-popout/AgentKanbanBoard.tsx b/src/renderer/src/components/dashboard-popout/AgentKanbanBoard.tsx
index 376f38a1..d30d69f7 100644
--- a/src/renderer/src/components/dashboard-popout/AgentKanbanBoard.tsx
+++ b/src/renderer/src/components/dashboard-popout/AgentKanbanBoard.tsx
@@ -4,6 +4,7 @@ import {
   DASHBOARD_BUCKET_ORDER,
   type DashboardBucket,
   type DashboardCard,
+  type DashboardOpenIssueArgs,
   type DashboardSleepWorkspaceArgs,
   type DashboardSnapshot,
   type DashboardSpawnAgentArgs
@@ -36,8 +37,12 @@ const AgentDashboardMapView = lazyWithRetry(
 /** Ack an agent in the pop-out window: relayed over IPC to the main renderer.
  *  ?. shields dialog-opening from dev-HMR preload skew (renderer updates hot,
  *  the preload only on app restart) — acks just no-op until restart. */
-function ackAgentViaPopoutRelay(paneKey: string): void {
-  void window.api.dashboard.ackAgent?.(paneKey)
+function ackAgentViaPopoutRelay(card: DashboardCard): void {
+  void window.api.dashboard.ackAgent?.(card.paneKey, card.executionHostId)
+}
+
+function openIssueViaPopoutRelay(args: DashboardOpenIssueArgs): void {
+  void window.api.dashboard.openIssue?.(args)
 }
 
 /** Reveal an agent from the pop-out window: raise the main window and route it
@@ -95,13 +100,15 @@ function KanbanColumn({
   cards,
   repoIconsByRepoId,
   now,
-  onOpenTerminal
+  onOpenTerminal,
+  onOpenIssue
 }: {
   bucket: DashboardBucket
   cards: DashboardCard[]
   repoIconsByRepoId: Record<string, RepoIcon | null> | undefined
   now: number
   onOpenTerminal: (card: DashboardCard) => void
+  onOpenIssue: (args: DashboardOpenIssueArgs) => void
 }): React.JSX.Element {
   return (
     // Why: attention no longer tints the whole column — the cards inside carry
@@ -128,6 +135,7 @@ function KanbanColumn({
               repoIcon={repoIconsByRepoId?.[card.repoId] ?? null}
               now={now}
               onOpenTerminal={onOpenTerminal}
+              onOpenIssue={onOpenIssue}
             />
           ))
         )}
@@ -144,7 +152,8 @@ type AgentKanbanBoardProps = {
   containerClassName?: string
   /** Marks an agent as seen. Defaults to the pop-out IPC relay; the in-window
    *  host acks the store directly. */
-  onAckAgent?: (paneKey: string) => void
+  onAckAgent?: (card: DashboardCard) => void
+  onOpenIssue?: (args: DashboardOpenIssueArgs) => void
   /** Focuses the agent's pane. Defaults to the pop-out IPC relay; the in-window
    *  host activates the worktree/pane locally and closes the overlay. */
   onRevealAgent?: (args: AgentRevealArgs) => void
@@ -176,6 +185,7 @@ export function AgentKanbanBoard({
   initialView = 'board',
   containerClassName = 'h-screen w-screen',
   onAckAgent = ackAgentViaPopoutRelay,
+  onOpenIssue = openIssueViaPopoutRelay,
   onRevealAgent = revealAgentViaPopoutRelay,
   onSpawnAgent = spawnAgentViaPopoutRelay,
   onSleepWorkspace = sleepWorkspaceViaPopoutRelay,
@@ -283,7 +293,7 @@ export function AgentKanbanBoard({
   // with unseen=false.
   const handleOpenTerminal = useCallback(
     (card: DashboardCard) => {
-      onAckAgent(card.paneKey)
+      onAckAgent(card)
       setOpenedCard(card)
     },
     [onAckAgent]
@@ -292,9 +302,9 @@ export function AgentKanbanBoard({
   // without this, an agent finishing while you watch would re-flag its card.
   useEffect(() => {
     if (dialogCard?.unseen) {
-      onAckAgent(dialogCard.paneKey)
+      onAckAgent(dialogCard)
     }
-  }, [dialogCard?.paneKey, dialogCard?.unseen, onAckAgent])
+  }, [dialogCard, onAckAgent])
 
   return (
     // Why: the pop-out is its own React root with no app-level provider, and the
@@ -401,6 +411,7 @@ export function AgentKanbanBoard({
                     repoIconsByRepoId={snapshot.repoIconsByRepoId}
                     now={now}
                     onOpenTerminal={handleOpenTerminal}
+                    onOpenIssue={onOpenIssue}
                   />
                 ))}
               </div>
diff --git a/src/renderer/src/components/dashboard-popout/AgentKanbanCard.test.tsx b/src/renderer/src/components/dashboard-popout/AgentKanbanCard.test.tsx
index d9859223..6b0f000a 100644
--- a/src/renderer/src/components/dashboard-popout/AgentKanbanCard.test.tsx
+++ b/src/renderer/src/components/dashboard-popout/AgentKanbanCard.test.tsx
@@ -161,6 +161,51 @@ describe('AgentKanbanCard', () => {
     expect(onOpenTerminal).toHaveBeenCalledTimes(1)
   })
 
+  it('opens a mapped Issue from its chip without opening the terminal', () => {
+    const onOpenTerminal = vi.fn()
+    const onOpenIssue = vi.fn()
+    const issueCard = card({
+      issueContext: {
+        status: 'mapped',
+        authority: {
+          authorityId: '11111111-1111-4111-8111-111111111111',
+          hostPartitionKey: 'local',
+          routeExecutionHostId: 'local'
+        },
+        conversationId: '22222222-2222-4222-8222-222222222222',
+        issue: {
+          id: '33333333-3333-4333-8333-333333333333',
+          title: 'Dashboard integration',
+          typeLabel: 'Feature',
+          state: 'active'
+        },
+        latestRound: null
+      }
+    })
+    render(
+      <TooltipProvider>
+        <AgentKanbanCard
+          card={issueCard}
+          now={2_000}
+          onOpenTerminal={onOpenTerminal}
+          onOpenIssue={onOpenIssue}
+        />
+      </TooltipProvider>
+    )
+
+    fireEvent.click(
+      screen.getByRole('button', { name: 'Open Issue Dashboard integration' })
+    )
+
+    expect(onOpenIssue).toHaveBeenCalledWith({
+      authority: issueCard.issueContext?.status === 'mapped'
+        ? issueCard.issueContext.authority
+        : undefined,
+      issueId: '33333333-3333-4333-8333-333333333333'
+    })
+    expect(onOpenTerminal).not.toHaveBeenCalled()
+  })
+
   it('labels one subagent accessibly and never renders a workspace-status dot', () => {
     renderCard({
       card: card({
diff --git a/src/renderer/src/components/dashboard-popout/AgentKanbanCard.tsx b/src/renderer/src/components/dashboard-popout/AgentKanbanCard.tsx
index faef58dc..02b79a86 100644
--- a/src/renderer/src/components/dashboard-popout/AgentKanbanCard.tsx
+++ b/src/renderer/src/components/dashboard-popout/AgentKanbanCard.tsx
@@ -16,7 +16,8 @@ import { Tooltip, TooltipContent, TooltipTrigger } from '@/components/ui/tooltip
 import { cn } from '@/lib/utils'
 import {
   dashboardCardDisplayState,
-  type DashboardCard
+  type DashboardCard,
+  type DashboardOpenIssueArgs
 } from '../../../../shared/dashboard-snapshot'
 import type { RepoIcon } from '../../../../shared/repo-icon'
 import { translate } from '@/i18n/i18n'
@@ -107,11 +108,40 @@ function sameCard(a: DashboardCard, b: DashboardCard): boolean {
     a.finishedAt === b.finishedAt &&
     a.stateChangedAt === b.stateChangedAt &&
     a.unseen === b.unseen &&
+    sameIssueContext(a.issueContext, b.issueContext) &&
     a.askSummary === b.askSummary &&
     a.conversationName === b.conversationName
   )
 }
 
+function sameIssueContext(
+  a: DashboardCard['issueContext'],
+  b: DashboardCard['issueContext']
+): boolean {
+  if (a === b) {
+    return true
+  }
+  if (!a || !b || a.status !== b.status) {
+    return false
+  }
+  if (a.status !== 'mapped' || b.status !== 'mapped') {
+    return true
+  }
+  return (
+    a.authority.authorityId === b.authority.authorityId &&
+    a.authority.hostPartitionKey === b.authority.hostPartitionKey &&
+    a.authority.routeExecutionHostId ===
+      b.authority.routeExecutionHostId &&
+    a.conversationId === b.conversationId &&
+    a.issue?.id === b.issue?.id &&
+    a.issue?.title === b.issue?.title &&
+    a.issue?.typeLabel === b.issue?.typeLabel &&
+    a.issue?.state === b.issue?.state &&
+    a.latestRound?.id === b.latestRound?.id &&
+    a.latestRound?.readAt === b.latestRound?.readAt
+  )
+}
+
 const REVIEW_PRESENTATION = {
   open: {
     icon: GitPullRequest,
@@ -191,6 +221,7 @@ type AgentKanbanCardProps = {
    *  card: bucket moves remount the card, and an embedded dialog would close
    *  the chat mid-conversation. */
   onOpenTerminal: (card: DashboardCard) => void
+  onOpenIssue?: (args: DashboardOpenIssueArgs) => void
 }
 
 /** One agent on the kanban board. Clicking opens the board's live terminal dialog. */
@@ -199,7 +230,8 @@ export const AgentKanbanCard = memo(
     card,
     repoIcon = null,
     now,
-    onOpenTerminal
+    onOpenTerminal,
+    onOpenIssue
   }: AgentKanbanCardProps): React.JSX.Element {
     useTranslation()
     const [subagentsOpen, setSubagentsOpen] = useState(false)
@@ -214,6 +246,14 @@ export const AgentKanbanCard = memo(
     // twice.
     const heading = card.conversationName ?? card.worktreeName
     const worktreeInFooter = card.conversationName !== undefined
+    const issue =
+      card.issueContext?.status === 'mapped'
+        ? card.issueContext.issue
+        : null
+    const issueAuthority =
+      card.issueContext?.status === 'mapped'
+        ? card.issueContext.authority
+        : null
 
     return (
       <div
@@ -313,6 +353,26 @@ export const AgentKanbanCard = memo(
           </>
         ) : null}
 
+        {issue && issueAuthority && onOpenIssue ? (
+          <button
+            type="button"
+            aria-label={translate(
+              'dashboardPopout.card.issue.open',
+              'Open Issue {{title}}',
+              { title: issue.title }
+            )}
+            onClick={() =>
+              onOpenIssue({
+                authority: issueAuthority,
+                issueId: issue.id
+              })
+            }
+            className="inline-flex max-w-full self-start rounded-full border border-border bg-muted/50 px-1.5 py-0.5 text-[10.5px] text-muted-foreground hover:bg-accent hover:text-foreground focus-visible:ring-2 focus-visible:ring-ring focus-visible:outline-none"
+          >
+            <span className="truncate">{issue.title}</span>
+          </button>
+        ) : null}
+
         <button
           type="button"
           onClick={() => onOpenTerminal(card)}
@@ -350,6 +410,7 @@ export const AgentKanbanCard = memo(
   },
   (previous, next) =>
     previous.onOpenTerminal === next.onOpenTerminal &&
+    previous.onOpenIssue === next.onOpenIssue &&
     sameCard(previous.card, next.card) &&
     sameRepoIcon(previous.repoIcon, next.repoIcon) &&
     (displayTimestamp(previous.card) <= 0 ||
diff --git a/src/renderer/src/components/dashboard/AgentDashboardDrawer.tsx b/src/renderer/src/components/dashboard/AgentDashboardDrawer.tsx
index e9e0efcc..b91795ed 100644
--- a/src/renderer/src/components/dashboard/AgentDashboardDrawer.tsx
+++ b/src/renderer/src/components/dashboard/AgentDashboardDrawer.tsx
@@ -16,6 +16,12 @@ import { AgentDashboardSettingsMenu } from './AgentDashboardSettingsMenu'
 import { launchDashboardAgent } from './launch-dashboard-agent'
 import { useLiveDashboardSnapshot } from './useLiveDashboardSnapshot'
 import { translate } from '@/i18n/i18n'
+import {
+  acknowledgeIssueDashboardCard,
+  issueDashboardAckTargetForCard
+} from '@/issues/issue-dashboard-ack'
+import { openIssueDetail } from '@/issues/issue-navigation'
+import { useIssueDashboardSnapshot } from '@/issues/use-issue-dashboard-snapshot'
 
 // Why: Escape should dismiss interactive nested overlays (e.g. the terminal
 // preview dialog) before this companion sheet, which is excluded by its own
@@ -39,12 +45,13 @@ function AgentDashboardDrawerBody({
   onClose: () => void
   onMenuOpenChange: (open: boolean) => void
 }): React.JSX.Element {
-  const snapshot = useLiveDashboardSnapshot()
+  const runtimeSnapshot = useLiveDashboardSnapshot()
+  const snapshot = useIssueDashboardSnapshot(runtimeSnapshot)
 
   // In-window ack/reveal act on the local store directly — the pop-out's IPC
   // relay is gated to the pop-out renderer and would reject calls from here.
-  const handleAckAgent = useCallback((paneKey: string) => {
-    useAppStore.getState().acknowledgeAgents([paneKey])
+  const handleAckAgent = useCallback((card: (typeof snapshot.cards)[number]) => {
+    void acknowledgeIssueDashboardCard(issueDashboardAckTargetForCard(card))
   }, [])
   const handleRevealAgent = useCallback(
     (args: AgentRevealArgs) => {
@@ -75,6 +82,7 @@ function AgentDashboardDrawerBody({
       // so the board reads as the same companion panel as the workspace board.
       containerClassName="h-full w-full bg-transparent"
       onAckAgent={handleAckAgent}
+      onOpenIssue={openIssueDetail}
       onRevealAgent={handleRevealAgent}
       onSpawnAgent={launchDashboardAgent}
       onClose={onClose}
diff --git a/src/renderer/src/components/dashboard/useDashboardPopoutBridge.ts b/src/renderer/src/components/dashboard/useDashboardPopoutBridge.ts
index 3d58b910..b1f1ef63 100644
--- a/src/renderer/src/components/dashboard/useDashboardPopoutBridge.ts
+++ b/src/renderer/src/components/dashboard/useDashboardPopoutBridge.ts
@@ -5,6 +5,14 @@ import { runSleepWorktree } from '../sidebar/sleep-worktree-flow'
 import type { RepoIcon } from '../../../../shared/repo-icon'
 import { buildDashboardSnapshot, type DashboardSnapshotState } from './build-dashboard-snapshot'
 import { launchDashboardAgent } from './launch-dashboard-agent'
+import {
+  acknowledgeIssueDashboardCard,
+  issueDashboardAckTargetForArgs
+} from '@/issues/issue-dashboard-ack'
+import { acquireIssueDashboardEvents } from '@/issues/issue-dashboard-event-lease'
+import { issueDashboardStore } from '@/issues/issue-dashboard-store'
+import { overlayIssueDashboardSnapshot } from '@/issues/issue-dashboard-overlay'
+import { openIssueDetail } from '@/issues/issue-navigation'
 
 // Why: cap snapshot rebuilds during bursts of agent-status pings. The board is a
 // glanceable surface, so ~4 updates/sec is plenty and keeps the cross-worktree
@@ -144,11 +152,23 @@ export function useDashboardPopoutBridge(enabled: boolean): void {
     if (!enabled) {
       return
     }
-    return window.api.dashboard.onAckAgent?.((paneKey) => {
-      useAppStore.getState().acknowledgeAgents([paneKey])
+    return window.api.dashboard.onAckAgent?.((paneKey, executionHostId) => {
+      void acknowledgeIssueDashboardCard(
+        issueDashboardAckTargetForArgs({
+          paneKey,
+          ...(executionHostId ? { executionHostId } : {})
+        })
+      )
     })
   }, [enabled])
 
+  useEffect(() => {
+    if (!enabled) {
+      return
+    }
+    return window.api.dashboard.onOpenIssue?.(openIssueDetail)
+  }, [enabled])
+
   useEffect(() => {
     if (!enabled) {
       return
@@ -156,6 +176,8 @@ export function useDashboardPopoutBridge(enabled: boolean): void {
     let open = false
     let disposed = false
     let unsubscribeStore: (() => void) | null = null
+    let unsubscribeIssueStore: (() => void) | null = null
+    let releaseIssueEvents: (() => void) | null = null
     let trailingTimer: ReturnType<typeof setTimeout> | null = null
     let lastPublishAt = 0
     let lastPublishedRepoIcons: Record<string, RepoIcon | null> | null = null
@@ -163,9 +185,22 @@ export function useDashboardPopoutBridge(enabled: boolean): void {
     // `withIcons` is forced whenever the pop-out could be starting from nothing —
     // it opened, or it mounted and asked. Throttled republishes omit an unchanged
     // icon map and the pop-out keeps the one it already has.
-    const publishNow = (withIcons: boolean): void => {
+    const publishNow = (
+      withIcons: boolean,
+      forceIssueRefresh = false
+    ): void => {
       lastPublishAt = Date.now()
-      const snapshot = buildDashboardSnapshot(useAppStore.getState(), lastPublishAt)
+      const runtimeSnapshot = buildDashboardSnapshot(
+        useAppStore.getState(),
+        lastPublishAt
+      )
+      void issueDashboardStore.getState().syncCards(runtimeSnapshot.cards, {
+        forceRefresh: forceIssueRefresh
+      })
+      const snapshot = overlayIssueDashboardSnapshot(
+        runtimeSnapshot,
+        issueDashboardStore.getState().partitionsByRoute
+      )
       const icons = snapshot.repoIconsByRepoId ?? {}
       if (!withIcons && repoIconsUnchanged(icons, lastPublishedRepoIcons)) {
         const { repoIconsByRepoId: _omitted, ...withoutIcons } = snapshot
@@ -210,10 +245,18 @@ export function useDashboardPopoutBridge(enabled: boolean): void {
         if (!unsubscribeStore) {
           unsubscribeStore = watchSnapshotInputs(publishThrottled)
         }
-        publishNow(true)
+        if (!unsubscribeIssueStore) {
+          unsubscribeIssueStore = issueDashboardStore.subscribe(publishThrottled)
+        }
+        releaseIssueEvents ??= acquireIssueDashboardEvents()
+        publishNow(true, true)
       } else {
         unsubscribeStore?.()
         unsubscribeStore = null
+        unsubscribeIssueStore?.()
+        unsubscribeIssueStore = null
+        releaseIssueEvents?.()
+        releaseIssueEvents = null
         if (trailingTimer) {
           clearTimeout(trailingTimer)
           trailingTimer = null
@@ -225,7 +268,7 @@ export function useDashboardPopoutBridge(enabled: boolean): void {
     // Popout mount asks for a fresh snapshot (its cached one may be stale).
     const offRequested = window.api.dashboard.onSnapshotRequested(() => {
       if (open) {
-        publishNow(true)
+        publishNow(true, true)
       }
     })
     // Recover the open state when the main window (re)mounts while a pop-out is
@@ -241,6 +284,8 @@ export function useDashboardPopoutBridge(enabled: boolean): void {
       offOpenChanged?.()
       offRequested?.()
       unsubscribeStore?.()
+      unsubscribeIssueStore?.()
+      releaseIssueEvents?.()
       if (trailingTimer) {
         clearTimeout(trailingTimer)
       }
diff --git a/src/renderer/src/components/sidebar/SidebarHeader.tsx b/src/renderer/src/components/sidebar/SidebarHeader.tsx
index f456fc25..a4cf4d2b 100644
--- a/src/renderer/src/components/sidebar/SidebarHeader.tsx
+++ b/src/renderer/src/components/sidebar/SidebarHeader.tsx
@@ -7,92 +7,116 @@ import SidebarWorkspaceOptionsMenu from './SidebarWorkspaceOptionsMenu'
 import { useShortcutLabel } from '@/hooks/useShortcutLabel'
 import { openWorkspaceCreationComposerWithTourHandoff } from '../contextual-tours/workspace-creation-tour-handoff'
 import { translate } from '@/i18n/i18n'
+import type { IssueClientPreferences } from '../../../../shared/issues/client-data'
+import { SidebarRootModeToggle } from './issues/sidebar-root-mode-toggle'
 
 type SidebarHeaderProps = {
   onWorkspaceBoardMenuOpenChange: (open: boolean) => void
+  rootMode: IssueClientPreferences['sidebarRootMode']
+  onRootModeChange: (
+    rootMode: IssueClientPreferences['sidebarRootMode']
+  ) => void
+  onCreateIssue: () => void
 }
 
 const SidebarHeader = React.memo(function SidebarHeader({
-  onWorkspaceBoardMenuOpenChange
+  onWorkspaceBoardMenuOpenChange,
+  rootMode,
+  onRootModeChange,
+  onCreateIssue
 }: SidebarHeaderProps) {
   const openModal = useAppStore((s) => s.openModal)
   const newWorktreeShortcutLabel = useShortcutLabel('workspace.create')
-  const groupBy = useAppStore((s) => s.groupBy)
   const canCreateWorkspace = useAppStore((s) => s.repos.length > 0)
-  const sidebarTitle = groupBy === 'repo' ? 'Projects' : 'Workspaces'
 
   return (
     <div className="mt-2 flex h-8 items-center justify-between px-2 gap-2">
-      <div className="flex min-w-0 items-center gap-1">
-        <span
-          className="pl-2 pr-0.5 text-xs font-semibold text-muted-foreground/80 select-none"
-          data-sidebar-section-title={groupBy === 'repo' ? 'projects' : 'workspaces'}
-        >
-          {sidebarTitle}
-        </span>
-      </div>
+      <SidebarRootModeToggle value={rootMode} onChange={onRootModeChange} />
       <div className="flex items-center gap-1.5 shrink-0">
-        <SidebarWorkspaceOptionsMenu
-          preserveWorkspaceBoardOpen
-          onMenuOpenChange={onWorkspaceBoardMenuOpenChange}
-        />
-
-        <Tooltip>
-          <TooltipTrigger asChild>
-            <Button
-              variant="ghost"
-              size="icon-xs"
-              className="text-muted-foreground"
-              aria-label={translate(
-                'auto.components.sidebar.SidebarHeader.25a95899c9',
-                'Add Project'
-              )}
-              onClick={() => openModal('add-repo')}
-            >
-              <FolderPlus className="size-3.5" strokeWidth={2.25} />
-            </Button>
-          </TooltipTrigger>
-          <TooltipContent side="bottom" sideOffset={6}>
-            {translate('auto.components.sidebar.SidebarHeader.25a95899c9', 'Add Project')}
-          </TooltipContent>
-        </Tooltip>
+        {rootMode === 'workspaces' ? (
+          <>
+            <SidebarWorkspaceOptionsMenu
+              preserveWorkspaceBoardOpen
+              onMenuOpenChange={onWorkspaceBoardMenuOpenChange}
+            />
 
-        <Tooltip>
-          <TooltipTrigger asChild>
-            <Button
-              variant="ghost"
-              size="icon-xs"
-              onClick={() => {
-                if (!canCreateWorkspace) {
-                  return
-                }
-                // Why: the parallel-work tour must click the real sidebar
-                // control so it can hand off to the workspace-creation tour.
-                openWorkspaceCreationComposerWithTourHandoff()
-              }}
-              aria-label={translate(
-                'auto.components.sidebar.SidebarHeader.92154beb7e',
-                'New workspace'
-              )}
-              disabled={!canCreateWorkspace}
-              data-contextual-tour-target="workspace-create-control"
-            >
-              <Plus className="size-3.5" strokeWidth={2.25} />
-            </Button>
-          </TooltipTrigger>
-          <TooltipContent side="right" sideOffset={6}>
-            {canCreateWorkspace
-              ? translate(
-                  'auto.components.sidebar.SidebarHeader.ca6f729da2',
-                  'New workspace ({{value0}})',
-                  { value0: newWorktreeShortcutLabel }
-                )
-              : translate(
-                  'auto.components.sidebar.SidebarHeader.5c9c7c16aa',
-                  'Add a project to create workspaces'
+            <Tooltip>
+              <TooltipTrigger asChild>
+                <Button
+                  variant="ghost"
+                  size="icon-xs"
+                  className="text-muted-foreground"
+                  aria-label={translate(
+                    'auto.components.sidebar.SidebarHeader.25a95899c9',
+                    'Add Project'
+                  )}
+                  onClick={() => openModal('add-repo')}
+                >
+                  <FolderPlus className="size-3.5" strokeWidth={2.25} />
+                </Button>
+              </TooltipTrigger>
+              <TooltipContent side="bottom" sideOffset={6}>
+                {translate(
+                  'auto.components.sidebar.SidebarHeader.25a95899c9',
+                  'Add Project'
                 )}
-          </TooltipContent>
-        </Tooltip>
+              </TooltipContent>
+            </Tooltip>
+
+            <Tooltip>
+              <TooltipTrigger asChild>
+                <Button
+                  variant="ghost"
+                  size="icon-xs"
+                  onClick={() => {
+                    if (!canCreateWorkspace) {
+                      return
+                    }
+                    // Why: the parallel-work tour must click the real sidebar
+                    // control so it can hand off to the workspace-creation tour.
+                    openWorkspaceCreationComposerWithTourHandoff()
+                  }}
+                  aria-label={translate(
+                    'auto.components.sidebar.SidebarHeader.92154beb7e',
+                    'New workspace'
+                  )}
+                  disabled={!canCreateWorkspace}
+                  data-contextual-tour-target="workspace-create-control"
+                >
+                  <Plus className="size-3.5" strokeWidth={2.25} />
+                </Button>
+              </TooltipTrigger>
+              <TooltipContent side="right" sideOffset={6}>
+                {canCreateWorkspace
+                  ? translate(
+                      'auto.components.sidebar.SidebarHeader.ca6f729da2',
+                      'New workspace ({{value0}})',
+                      { value0: newWorktreeShortcutLabel }
+                    )
+                  : translate(
+                      'auto.components.sidebar.SidebarHeader.5c9c7c16aa',
+                      'Add a project to create workspaces'
+                    )}
+              </TooltipContent>
+            </Tooltip>
+          </>
+        ) : (
+          <Tooltip>
+            <TooltipTrigger asChild>
+              <Button
+                variant="ghost"
+                size="icon-xs"
+                onClick={onCreateIssue}
+                aria-label="New Issue"
+              >
+                <Plus className="size-3.5" strokeWidth={2.25} />
+              </Button>
+            </TooltipTrigger>
+            <TooltipContent side="right" sideOffset={6}>
+              New Issue
+            </TooltipContent>
+          </Tooltip>
+        )}
       </div>
     </div>
   )
diff --git a/src/renderer/src/components/sidebar/index.tsx b/src/renderer/src/components/sidebar/index.tsx
index bed75283..4ba7b289 100644
--- a/src/renderer/src/components/sidebar/index.tsx
+++ b/src/renderer/src/components/sidebar/index.tsx
@@ -1,5 +1,6 @@
-import React, { useEffect, useMemo } from 'react'
+import React, { useEffect, useMemo, useState } from 'react'
 import { useAppStore } from '@/store'
+import { useStore } from 'zustand'
 import { TooltipProvider } from '@/components/ui/tooltip'
 import { useSidebarResize } from '@/hooks/useSidebarResize'
 import SidebarHeader from './SidebarHeader'
@@ -16,6 +17,11 @@ import { useWorkspaceBoardPanel } from './useWorkspaceBoardPanel'
 import { resolveLeftSidebarStyleVariables } from '@/lib/left-sidebar-appearance'
 import { useSystemPrefersDark } from '@/components/terminal-pane/use-system-prefers-dark'
 import { lazyWithRetry } from '@/lib/lazy-with-retry'
+import IssueSidebar from './issues/issue-sidebar'
+import CreateLocalIssueDialog from './issues/create-local-issue-dialog'
+import { issuesDomainStore } from '../../issues/issues-domain-store'
+import { openIssueDetail } from '../../issues/issue-navigation'
+import { useSidebarHostScopeOptions } from './use-sidebar-host-scope-options'
 
 const WorktreeMetaDialog = lazyWithRetry(() => import('./WorktreeMetaDialog'))
 const RemoveFolderDialog = lazyWithRetry(() => import('./RemoveFolderDialog'))
@@ -51,6 +57,16 @@ function Sidebar({
   const fetchAllWorktrees = useAppStore((s) => s.fetchAllWorktrees)
   const activeModal = useAppStore((s) => s.activeModal)
   const statusBarVisible = useAppStore((s) => s.statusBarVisible)
+  const rootMode = useStore(
+    issuesDomainStore,
+    (state) => state.preferences.sidebarRootMode
+  )
+  const selectedIssueHostId = useStore(
+    issuesDomainStore,
+    (state) => state.preferences.selectedRouteExecutionHostId
+  )
+  const { hostOptions } = useSidebarHostScopeOptions()
+  const [createIssueDialogOpen, setCreateIssueDialogOpen] = useState(false)
   const systemPrefersDark = useSystemPrefersDark()
   const leftSidebarStyle = useMemo(
     () => resolveLeftSidebarStyleVariables(settings, systemPrefersDark),
@@ -87,10 +103,14 @@ function Sidebar({
   }, [repoCount, startupWorktreeRefreshCompleted, fetchAllWorktrees])
 
   useEffect(() => {
-    if (!sidebarOpen && workspaceBoardRenderedOpen) {
+    void issuesDomainStore.getState().hydrate()
+  }, [])
+
+  useEffect(() => {
+    if ((!sidebarOpen || rootMode === 'issues') && workspaceBoardRenderedOpen) {
       closeWorkspaceBoard()
     }
-  }, [closeWorkspaceBoard, sidebarOpen, workspaceBoardRenderedOpen])
+  }, [closeWorkspaceBoard, rootMode, sidebarOpen, workspaceBoardRenderedOpen])
 
   const { containerRef, onResizeStart, isResizing } = useSidebarResize<HTMLDivElement>({
     isOpen: sidebarOpen,
@@ -106,36 +126,53 @@ function Sidebar({
     <TooltipProvider delayDuration={400}>
       <div
         ref={containerRef}
-        data-native-file-drop-target={sidebarOpen ? nativeDropTarget : undefined}
+        data-native-file-drop-target={
+          sidebarOpen && rootMode === 'workspaces'
+            ? nativeDropTarget
+            : undefined
+        }
         className="relative min-h-0 flex-shrink-0 bg-worktree-sidebar flex flex-col overflow-hidden scrollbar-sleek-parent"
         style={leftSidebarStyle}
-        {...dropHandlers}
+        {...(rootMode === 'workspaces' ? dropHandlers : {})}
       >
         {sidebarOpen && (
           <>
             {/* Fixed controls */}
             <SidebarNav />
-            <SidebarHeader onWorkspaceBoardMenuOpenChange={setWorkspaceBoardMenuOpen} />
-
-            <WorktreeList
-              scrollOffsetRef={worktreeScrollOffsetRef}
-              scrollAnchorRef={worktreeScrollAnchorRef}
-              workspaceBoardOpen={workspaceBoardOpen}
-              onWorkspaceBoardDragPreviewStart={previewWorkspaceBoardFromDrag}
-              onWorkspaceBoardDragPreviewCommit={solidifyWorkspaceBoardFromDrag}
-              onWorkspaceBoardDragPreviewCancel={cancelWorkspaceBoardDragPreview}
+            <SidebarHeader
+              onWorkspaceBoardMenuOpenChange={setWorkspaceBoardMenuOpen}
+              rootMode={rootMode}
+              onRootModeChange={(next) =>
+                issuesDomainStore.getState().setSidebarRootMode(next)
+              }
+              onCreateIssue={() => setCreateIssueDialogOpen(true)}
             />
 
-            <div className="relative shrink-0">
-              <SetupScriptPromptCard />
+            {rootMode === 'workspaces' ? (
+              <>
+                <WorktreeList
+                  scrollOffsetRef={worktreeScrollOffsetRef}
+                  scrollAnchorRef={worktreeScrollAnchorRef}
+                  workspaceBoardOpen={workspaceBoardOpen}
+                  onWorkspaceBoardDragPreviewStart={previewWorkspaceBoardFromDrag}
+                  onWorkspaceBoardDragPreviewCommit={solidifyWorkspaceBoardFromDrag}
+                  onWorkspaceBoardDragPreviewCancel={cancelWorkspaceBoardDragPreview}
+                />
+
+                <div className="relative shrink-0">
+                  <SetupScriptPromptCard />
 
-              {/* Fixed bottom toolbar */}
-              <SidebarToolbar
-                workspaceBoardOpen={workspaceBoardOpen}
-                workspaceBoardDragPreviewOpen={workspaceBoardDragPreviewOpen}
-                onWorkspaceBoardToggle={toggleWorkspaceBoard}
-              />
-            </div>
+                  {/* Fixed bottom toolbar */}
+                  <SidebarToolbar
+                    workspaceBoardOpen={workspaceBoardOpen}
+                    workspaceBoardDragPreviewOpen={workspaceBoardDragPreviewOpen}
+                    onWorkspaceBoardToggle={toggleWorkspaceBoard}
+                  />
+                </div>
+              </>
+            ) : (
+              <IssueSidebar />
+            )}
           </>
         )}
 
@@ -184,7 +221,16 @@ function Sidebar({
         {activeModal === 'confirm-orca-yaml-hooks' ? <OrcaYamlTrustDialog /> : null}
         {activeModal === 'forget-ssh-workspace' ? <ForgetSshWorkspaceDialog /> : null}
       </React.Suspense>
-      {sidebarOpen ? (
+      <CreateLocalIssueDialog
+        open={createIssueDialogOpen}
+        hostOptions={hostOptions}
+        defaultExecutionHostId={selectedIssueHostId}
+        onOpenChange={setCreateIssueDialogOpen}
+        onCreated={(authority, issueId) =>
+          openIssueDetail({ authority, issueId })
+        }
+      />
+      {sidebarOpen && rootMode === 'workspaces' ? (
         <WorkspaceKanbanDrawer
           leftSidebarStyle={leftSidebarStyle}
           open={workspaceBoardRenderedOpen}
diff --git a/src/renderer/src/components/sidebar/use-sidebar-host-scope-options.ts b/src/renderer/src/components/sidebar/use-sidebar-host-scope-options.ts
index 039289c7..e40fc291 100644
--- a/src/renderer/src/components/sidebar/use-sidebar-host-scope-options.ts
+++ b/src/renderer/src/components/sidebar/use-sidebar-host-scope-options.ts
@@ -8,6 +8,8 @@ import {
   type SidebarHostScopeOption
 } from './sidebar-host-options'
 
+const EMPTY_SSH_TARGET_LABELS = new Map<string, string>()
+
 /** Shared host-scope derivation for the sidebar scope strip and the workspace
  * options menu so both surfaces consume the same live runtime status without
  * duplicating store wiring. */
@@ -16,7 +18,9 @@ export function useSidebarHostScopeOptions(): {
   hostScopeOptions: SidebarHostScopeOption[]
 } {
   const repos = useAppStore((s) => s.repos)
-  const sshTargetLabels = useAppStore((s) => s.sshTargetLabels)
+  const sshTargetLabels = useAppStore(
+    (s) => s.sshTargetLabels ?? EMPTY_SSH_TARGET_LABELS
+  )
   const sshConnectionStates = useAppStore((s) => s.sshConnectionStates)
   const settings = useAppStore((s) => s.settings)
   const runtimeEnvironments = useAppStore((s) => s.runtimeEnvironments)
diff --git a/src/renderer/src/components/terminal-pane/pty-connection-types.ts b/src/renderer/src/components/terminal-pane/pty-connection-types.ts
index 9fef3423..74d9bae9 100644
--- a/src/renderer/src/components/terminal-pane/pty-connection-types.ts
+++ b/src/renderer/src/components/terminal-pane/pty-connection-types.ts
@@ -33,6 +33,7 @@ export type PtyConnectionDeps = {
     resumeProviderSession?: AgentProviderSessionMetadata
     launchToken?: string
     launchAgent?: TuiAgent
+    issueId?: string
     /** Explicit CLI override for host-owned agent launches; omission uses host settings. */
     agentArgsOverride?: string | null
     draftPrompt?: string
diff --git a/src/renderer/src/components/terminal-pane/pty-connection.test.ts b/src/renderer/src/components/terminal-pane/pty-connection.test.ts
index 98a26a1d..52778b93 100644
--- a/src/renderer/src/components/terminal-pane/pty-connection.test.ts
+++ b/src/renderer/src/components/terminal-pane/pty-connection.test.ts
@@ -2384,6 +2384,30 @@ describe('connectPanePty', () => {
     expect(createdTransportOptions[0]?.projectRuntime).toBeUndefined()
   })
 
+  it('passes the Issue binding from startup state into the PTY transport', async () => {
+    const { connectPanePty } = await import('./pty-connection')
+    const transport = createMockTransport()
+    transportFactoryQueue.push(transport)
+
+    connectPanePty(
+      createPane(1) as never,
+      createManager(1) as never,
+      createDeps({
+        startup: {
+          command: 'codex',
+          launchAgent: 'codex',
+          issueId: '11111111-1111-4111-8111-111111111111'
+        }
+      }) as never
+    )
+    await flushAsyncTicks()
+
+    expect(createdTransportOptions[0]).toMatchObject({
+      launchAgent: 'codex',
+      issueId: '11111111-1111-4111-8111-111111111111'
+    })
+  })
+
   it('observes live terminal GitHub PR URLs before agent completion', async () => {
     const { connectPanePty } = await import('./pty-connection')
     const transport = createMockTransport()
diff --git a/src/renderer/src/components/terminal-pane/pty-connection.ts b/src/renderer/src/components/terminal-pane/pty-connection.ts
index 14a514cf..a65782b0 100644
--- a/src/renderer/src/components/terminal-pane/pty-connection.ts
+++ b/src/renderer/src/components/terminal-pane/pty-connection.ts
@@ -3849,6 +3849,7 @@ export function connectPanePty(
     ...(agentLaunchPreferences ? { agentLaunchPreferences } : {}),
     ...(launchToken ? { launchToken } : {}),
     ...(paneStartup?.launchAgent ? { launchAgent: paneStartup.launchAgent } : {}),
+    ...(paneStartup?.issueId ? { issueId: paneStartup.issueId } : {}),
     ...(paneStartup?.telemetry ? { telemetry: paneStartup.telemetry } : {}),
     onPtyExit: onExit,
     onPtySpawn,
diff --git a/src/renderer/src/components/terminal-pane/pty-transport-types.ts b/src/renderer/src/components/terminal-pane/pty-transport-types.ts
index 57e0c9e2..25bfe315 100644
--- a/src/renderer/src/components/terminal-pane/pty-transport-types.ts
+++ b/src/renderer/src/components/terminal-pane/pty-transport-types.ts
@@ -65,6 +65,7 @@ export type LocalPtySessionMetadata = {
 
 export type PtyConnectResult = {
   id: string
+  conversationId?: string
   /** The requested session exited while it had no primary pane handler. Its
    *  buffered final data/exit were delivered, so callers must not fresh-spawn. */
   exitedBeforeAttach?: boolean
@@ -234,6 +235,7 @@ export type IpcPtyTransportOptions = {
   agentLaunchPreferences?: AgentLaunchPreferences
   launchToken?: string
   launchAgent?: TuiAgent
+  issueId?: string
   startupCommandDelivery?: StartupCommandDelivery
   connectionId?: string | null
   executionHostId?: ExecutionHostId | null
diff --git a/src/renderer/src/components/terminal-pane/pty-transport.test.ts b/src/renderer/src/components/terminal-pane/pty-transport.test.ts
index 1954b857..7edab93a 100644
--- a/src/renderer/src/components/terminal-pane/pty-transport.test.ts
+++ b/src/renderer/src/components/terminal-pane/pty-transport.test.ts
@@ -276,6 +276,25 @@ describe('createIpcPtyTransport', () => {
     expect(spawn).toHaveBeenCalledWith(expect.objectContaining({ resumeProviderSession }))
   })
 
+  it('preserves Issue binding and allocated Conversation identity across the spawn IPC', async () => {
+    const { createIpcPtyTransport } = await import('./pty-transport')
+    const spawn = window.api.pty.spawn as unknown as ReturnType<typeof vi.fn>
+    spawn.mockResolvedValueOnce({
+      id: 'pty-issue',
+      conversationId: 'conversation-issue'
+    })
+    const transport = createIpcPtyTransport({ issueId: 'issue-1' })
+
+    const result = await transport.connect({ url: '', callbacks: {} })
+
+    expect(spawn).toHaveBeenCalledWith(expect.objectContaining({ issueId: 'issue-1' }))
+    expect(result).toEqual({
+      id: 'pty-issue',
+      conversationId: 'conversation-issue'
+    })
+    transport.disconnect()
+  })
+
   it('leaves the transport silently unbound after a failed connect — sendInput drops with no write IPC (frozen-terminal repro)', async () => {
     const { createIpcPtyTransport } = await import('./pty-transport')
     const spawn = window.api.pty.spawn as unknown as ReturnType<typeof vi.fn>
diff --git a/src/renderer/src/components/terminal-pane/pty-transport.ts b/src/renderer/src/components/terminal-pane/pty-transport.ts
index 9eddfae0..9e3142ec 100644
--- a/src/renderer/src/components/terminal-pane/pty-transport.ts
+++ b/src/renderer/src/components/terminal-pane/pty-transport.ts
@@ -565,6 +565,7 @@ export function createIpcPtyTransport(opts: IpcPtyTransportOptions = {}): PtyTra
     resumeProviderSession,
     launchToken,
     launchAgent,
+    issueId,
     startupCommandDelivery,
     connectionId,
     worktreeId,
@@ -837,6 +838,7 @@ export function createIpcPtyTransport(opts: IpcPtyTransportOptions = {}): PtyTra
           ...((options.launchAgent ?? launchAgent)
             ? { launchAgent: options.launchAgent ?? launchAgent }
             : {}),
+          ...(issueId ? { issueId } : {}),
           ...((options.startupCommandDelivery ?? startupCommandDelivery)
             ? { startupCommandDelivery: options.startupCommandDelivery ?? startupCommandDelivery }
             : {}),
@@ -888,7 +890,13 @@ export function createIpcPtyTransport(opts: IpcPtyTransportOptions = {}): PtyTra
         registerPtyDataHandler(spawnResult.id)
         const exitedBeforeAttach = registerPtyExitHandler(spawnResult.id)
         if (exitedBeforeAttach) {
-          return { id: spawnResult.id, exitedBeforeAttach: true } satisfies PtyConnectResult
+          return {
+            id: spawnResult.id,
+            ...(spawnResult.conversationId
+              ? { conversationId: spawnResult.conversationId }
+              : {}),
+            exitedBeforeAttach: true
+          } satisfies PtyConnectResult
         }
         if (!connected || ptyId !== spawnResult.id) {
           return undefined
@@ -900,6 +908,9 @@ export function createIpcPtyTransport(opts: IpcPtyTransportOptions = {}): PtyTra
         if (spawnResult.isReattach || spawnResult.coldRestore || spawnResult.sessionExpired) {
           return {
             id: spawnResult.id,
+            ...(spawnResult.conversationId
+              ? { conversationId: spawnResult.conversationId }
+              : {}),
             // Why: recovery needs to distinguish an attach that ignored startup intent from a fresh spawn that ran it.
             ...(spawnResult.isReattach ? { isReattach: true } : {}),
             ...(resultLaunchAgent ? { launchAgent: resultLaunchAgent } : {}),
@@ -928,12 +939,16 @@ export function createIpcPtyTransport(opts: IpcPtyTransportOptions = {}): PtyTra
         }
         if (
           resultLaunchAgent ||
+          spawnResult.conversationId ||
           spawnResult.launchConfig ||
           spawnResult.startupCwdFallback ||
           spawnResult.agentResumeUnavailable
         ) {
           return {
             id: spawnResult.id,
+            ...(spawnResult.conversationId
+              ? { conversationId: spawnResult.conversationId }
+              : {}),
             ...(resultLaunchAgent ? { launchAgent: resultLaunchAgent } : {}),
             ...(spawnResult.launchConfig ? { launchConfig: spawnResult.launchConfig } : {}),
             ...(spawnResult.startupCwdFallback
diff --git a/src/renderer/src/components/terminal-pane/remote-runtime-pty-transport.test.ts b/src/renderer/src/components/terminal-pane/remote-runtime-pty-transport.test.ts
index 4087cf59..d4d843a4 100644
--- a/src/renderer/src/components/terminal-pane/remote-runtime-pty-transport.test.ts
+++ b/src/renderer/src/components/terminal-pane/remote-runtime-pty-transport.test.ts
@@ -4276,6 +4276,59 @@ describe('createRemoteRuntimePtyTransport', () => {
     )
   })
 
+  it('passes Issue binding through structured creation and returns the Conversation identity', async () => {
+    runtimeCall.mockImplementation(async (args: { method?: string }) => {
+      if (args.method === 'status.get') {
+        return {
+          ok: true,
+          result: {
+            runtimeProtocolVersion: 3,
+            minCompatibleRuntimeClientVersion: 2,
+            capabilities: ['agent-session.host-authority.v1']
+          }
+        }
+      }
+      if (args.method === 'terminal.createAgentSession') {
+        return {
+          ok: true,
+          result: {
+            disposition: 'created',
+            conversationId: '22222222-2222-4222-8222-222222222222',
+            terminal: {
+              handle: 'terminal-issue',
+              tabId: 'host-tab',
+              paneKey: 'host-tab:11111111-1111-4111-8111-111111111111'
+            }
+          }
+        }
+      }
+      return { ok: true, result: {} }
+    })
+    const { createRemoteRuntimePtyTransport } = await import('./remote-runtime-pty-transport')
+    const transport = createRemoteRuntimePtyTransport('env-1', {
+      worktreeId: 'wt-1',
+      tabId: 'tab-1',
+      leafId: 'pane:1',
+      launchAgent: 'codex',
+      issueId: '11111111-1111-4111-8111-111111111111'
+    })
+
+    await expect(transport.connect({ url: '', callbacks: {} })).resolves.toMatchObject({
+      conversationId: '22222222-2222-4222-8222-222222222222'
+    })
+    expect(runtimeCall).toHaveBeenCalledWith(
+      expect.objectContaining({
+        method: 'terminal.createAgentSession',
+        params: expect.objectContaining({
+          issueId: '11111111-1111-4111-8111-111111111111'
+        })
+      })
+    )
+    expect(runtimeCall).not.toHaveBeenCalledWith(
+      expect.objectContaining({ method: 'terminal.create' })
+    )
+  })
+
   it('activates pending host session mirrors instead of creating duplicate terminals', async () => {
     runtimeCall.mockImplementation((args) => {
       if (args.method === 'session.tabs.activate') {
diff --git a/src/renderer/src/components/terminal-pane/remote-runtime-pty-transport.ts b/src/renderer/src/components/terminal-pane/remote-runtime-pty-transport.ts
index 6eff59d8..a3f39dc3 100644
--- a/src/renderer/src/components/terminal-pane/remote-runtime-pty-transport.ts
+++ b/src/renderer/src/components/terminal-pane/remote-runtime-pty-transport.ts
@@ -145,6 +145,7 @@ export function createRemoteRuntimePtyTransport(
     resumeProviderSession,
     launchToken,
     launchAgent,
+    issueId,
     terminalColorQueryReplies,
     agentPrompt,
     agentPromptDelivery,
@@ -2052,6 +2053,7 @@ export function createRemoteRuntimePtyTransport(
                       {
                         worktree: toRuntimeTerminalWorktreeSelector(worktreeId),
                         agent: launchAgentToSend!,
+                        ...(issueId ? { issueId } : {}),
                         ...(agentPrompt ? { prompt: agentPrompt } : {}),
                         ...(agentPromptDelivery ? { promptDelivery: agentPromptDelivery } : {}),
                         ...(agentArgsOverride !== undefined
@@ -2079,6 +2081,7 @@ export function createRemoteRuntimePtyTransport(
                 ...(resumeProviderSessionToSend && launchAgentToSend === 'omp'
                   ? { hostAuthorityCapability: AGENT_SESSION_OMP_RESUME_PATH_RUNTIME_CAPABILITY }
                   : {}),
+                ...(issueId ? { requireHostAuthority: true } : {}),
                 legacy: legacyCreate
               })
           : await legacyCreate()
@@ -2150,6 +2153,9 @@ export function createRemoteRuntimePtyTransport(
 
         return {
           id: remotePtyId,
+          ...('conversationId' in created && created.conversationId
+            ? { conversationId: created.conversationId }
+            : {}),
           replay: '',
           ...(createdTerminal.isReattach === true ? { isReattach: true } : {})
         } satisfies PtyConnectResult
diff --git a/src/renderer/src/components/terminal-pane/use-notification-dispatch.test.ts b/src/renderer/src/components/terminal-pane/use-notification-dispatch.test.ts
index 0fa4e74d..a3a6bfe8 100644
--- a/src/renderer/src/components/terminal-pane/use-notification-dispatch.test.ts
+++ b/src/renderer/src/components/terminal-pane/use-notification-dispatch.test.ts
@@ -42,6 +42,9 @@ type MockState = {
 }
 
 const playDesktopNotificationSound = vi.hoisted(() => vi.fn())
+const loadIssueNotificationContext = vi.hoisted(() => vi.fn())
+const readCachedIssueNotificationContext = vi.hoisted(() => vi.fn())
+const getExecutionHostIdForWorktree = vi.hoisted(() => vi.fn(() => 'ssh:docs'))
 let mockState: MockState
 
 vi.mock('@/store', () => ({
@@ -54,6 +57,15 @@ vi.mock('@/lib/desktop-notification-sound', () => ({
   playDesktopNotificationSound
 }))
 
+vi.mock('@/issues/issue-notification-context', () => ({
+  loadIssueNotificationContext,
+  readCachedIssueNotificationContext
+}))
+
+vi.mock('@/lib/worktree-runtime-owner', () => ({
+  getExecutionHostIdForWorktree
+}))
+
 function makeAgentStatus(
   paneKey: string,
   overrides: Partial<AgentStatusEntry> = {}
@@ -99,6 +111,21 @@ describe('dispatchTerminalNotification', () => {
 
   beforeEach(() => {
     vi.clearAllMocks()
+    getExecutionHostIdForWorktree.mockReturnValue('ssh:docs')
+    loadIssueNotificationContext.mockResolvedValue({
+      executionHostId: 'ssh:docs',
+      conversationId: '33333333-3333-4333-8333-333333333333',
+      issueId: '22222222-2222-4222-8222-222222222222',
+      roundRecordId: '44444444-4444-4444-8444-444444444444',
+      isVisibleRoundRecord: false
+    })
+    readCachedIssueNotificationContext.mockReturnValue({
+      executionHostId: 'ssh:docs',
+      conversationId: '33333333-3333-4333-8333-333333333333',
+      issueId: '22222222-2222-4222-8222-222222222222',
+      roundRecordId: '44444444-4444-4444-8444-444444444444',
+      isVisibleRoundRecord: false
+    })
     mockState = {
       activeWorktreeId: 'wt-secondary',
       activeTabId: 'tab-1',
@@ -279,6 +306,53 @@ describe('dispatchTerminalNotification', () => {
     expect(mockState.markAgentCompletionPaneUnread).not.toHaveBeenCalled()
   })
 
+  it('dispatches waiting notifications with exact visibility and authority locator', async () => {
+    mockState.activeWorktreeId = 'wt-primary'
+    mockState.agentStatusByPaneKey[paneKey] = makeAgentStatus(paneKey, {
+      state: 'waiting',
+      stateStartedAt: 1_700_000_010_000
+    })
+    stubDocumentFocus({ visibilityState: 'visible', focused: true })
+    readCachedIssueNotificationContext.mockReturnValueOnce(null)
+
+    dispatchTerminalNotification('wt-primary', {
+      source: 'agent-needs-input',
+      terminalTitle: 'codex',
+      paneKey,
+      agentStatusSnapshot: {
+        state: 'waiting',
+        prompt: 'Approve the command',
+        agentType: 'codex',
+        stateStartedAt: 1_700_000_010_000
+      }
+    })
+
+    await vi.waitFor(() => {
+      expect(window.api.notifications.dispatch).toHaveBeenCalledWith(
+        expect.objectContaining({
+          source: 'agent-needs-input',
+          worktreeId: 'wt-primary',
+          paneKey,
+          executionHostId: 'ssh:docs',
+          conversationId: '33333333-3333-4333-8333-333333333333',
+          issueId: '22222222-2222-4222-8222-222222222222',
+          roundRecordId: '44444444-4444-4444-8444-444444444444',
+          isVisibleForegroundPane: true,
+          isVisibleRoundRecord: false
+        })
+      )
+    })
+    expect(getExecutionHostIdForWorktree).toHaveBeenCalledWith(mockState, 'wt-primary')
+    expect(loadIssueNotificationContext).toHaveBeenCalledWith({
+      executionHostId: 'ssh:docs',
+      paneKey,
+      expectedRound: {
+        kind: 'waiting',
+        occurredAt: 1_700_000_010_000
+      }
+    })
+  })
+
   it('marks a hidden tab in the focused worktree unread', () => {
     const hiddenLeafId = '33333333-3333-4333-8333-333333333333'
     const hiddenPaneKey = `tab-2:${hiddenLeafId}`
diff --git a/src/renderer/src/components/terminal-pane/use-notification-dispatch.ts b/src/renderer/src/components/terminal-pane/use-notification-dispatch.ts
index 20a0ddad..eaa94c6a 100644
--- a/src/renderer/src/components/terminal-pane/use-notification-dispatch.ts
+++ b/src/renderer/src/components/terminal-pane/use-notification-dispatch.ts
@@ -26,6 +26,12 @@ import {
   isOrcaWindowForegroundFocused,
   isVisibleForegroundPaneKey
 } from './terminal-notification-pane-visibility'
+import { getExecutionHostIdForWorktree } from '@/lib/worktree-runtime-owner'
+import {
+  loadIssueNotificationContext,
+  readCachedIssueNotificationContext,
+  type IssueNotificationContextInput
+} from '@/issues/issue-notification-context'
 
 const AGENT_NOTIFICATION_SNAPSHOT_MAX_AGE_MS = 10_000
 
@@ -53,7 +59,7 @@ function hasFreshActiveHookStatus(
 }
 
 export type TerminalNotificationEvent = {
-  source: 'terminal-bell' | 'agent-task-complete'
+  source: 'terminal-bell' | 'agent-task-complete' | 'agent-needs-input'
   terminalTitle?: string
   paneKey?: string
   agentStatusSnapshot?: AgentCompletionStatusSnapshot
@@ -77,15 +83,15 @@ export function dispatchTerminalNotification(
   // agent, any snapshot from another agent is stale pane-reuse residue and must
   // not lend its prompt/agentType or timing id to this notification.
   const explicitTitleAgentType =
-    event.source === 'agent-task-complete' && event.terminalTitle
+    isAgentNotificationSource(event.source) && event.terminalTitle
       ? resolveCommittedTitleAgentType(event.terminalTitle)
       : null
   const storedAgentStatus =
-    event.source === 'agent-task-complete' && event.paneKey
+    isAgentNotificationSource(event.source) && event.paneKey
       ? state.agentStatusByPaneKey[event.paneKey]
       : undefined
   const eventAgentStatusSnapshot =
-    event.source === 'agent-task-complete' &&
+    isAgentNotificationSource(event.source) &&
     agentSnapshotMatchesExplicitTitle(event.agentStatusSnapshot, explicitTitleAgentType)
       ? event.agentStatusSnapshot
       : undefined
@@ -96,7 +102,7 @@ export function dispatchTerminalNotification(
       ? storedAgentStatus
       : undefined
   if (
-    event.source === 'agent-task-complete' &&
+    isAgentNotificationSource(event.source) &&
     event.agentCompletionSource !== 'process-exit' &&
     !eventAgentStatusSnapshot &&
     hasFreshActiveHookStatus(storedAgentStatus, explicitTitleAgentType)
@@ -108,14 +114,14 @@ export function dispatchTerminalNotification(
   // Why: a process can die before its hook emits done; do not label the
   // resulting completion notification with that stale active state or prompt.
   const agentStatus =
-    event.source === 'agent-task-complete'
+    isAgentNotificationSource(event.source)
       ? (eventAgentStatusSnapshot ??
         (event.agentCompletionSource === 'process-exit' && freshStoredAgentStatus?.state !== 'done'
           ? undefined
           : freshStoredAgentStatus))
       : undefined
   if (
-    event.source === 'agent-task-complete' &&
+    isAgentNotificationSource(event.source) &&
     isSupersededAgentCompletionSnapshot(storedAgentStatus, eventAgentStatusSnapshot)
   ) {
     return
@@ -139,7 +145,7 @@ export function dispatchTerminalNotification(
     return
   }
 
-  if (event.source === 'agent-task-complete') {
+  if (isAgentNotificationSource(event.source)) {
     const terminalAttentionEnabled = state.settings?.experimentalTerminalAttention === true
     let tabId: string | null = null
     if (event.paneKey) {
@@ -203,7 +209,7 @@ export function dispatchTerminalNotification(
       }
     : {}
   const notificationId =
-    event.source === 'agent-task-complete'
+    isAgentNotificationSource(event.source)
       ? buildAgentNotificationId({
           worktreeId,
           paneKey: event.paneKey,
@@ -214,8 +220,11 @@ export function dispatchTerminalNotification(
         })
       : null
 
-  void window.api.notifications
-    .dispatch({
+  const isVisibleForegroundPane = event.paneKey
+    ? isVisibleForegroundPaneKey(state, worktreeId, event.paneKey)
+    : undefined
+  const executionHostId = getExecutionHostIdForWorktree(state, worktreeId)
+  const baseRequest = {
       source: event.source,
       ...(notificationId ? { notificationId } : {}),
       worktreeId,
@@ -225,8 +234,34 @@ export function dispatchTerminalNotification(
       hasMultipleActiveRepos: countReposNeedingNotificationDisambiguation(state) > 1,
       terminalTitle: event.terminalTitle,
       isActiveWorktree: state.activeWorktreeId === worktreeId,
+      ...(isVisibleForegroundPane === undefined ? {} : { isVisibleForegroundPane }),
       ...agentSnapshot
-    })
+    }
+  const contextInput: IssueNotificationContextInput | null =
+    isAgentNotificationSource(event.source) && event.paneKey
+      ? {
+          executionHostId,
+          paneKey: event.paneKey,
+          ...(agentStatus?.stateStartedAt === undefined
+            ? {}
+            : {
+                expectedRound: {
+                  kind: event.source === 'agent-needs-input' ? 'waiting' : 'completion',
+                  occurredAt: agentStatus.stateStartedAt
+                }
+              })
+        }
+      : null
+  const cachedContext = contextInput
+    ? readCachedIssueNotificationContext(contextInput)
+    : { executionHostId }
+  const dispatchResult = cachedContext
+    ? window.api.notifications.dispatch({ ...baseRequest, ...cachedContext })
+    : loadIssueNotificationContext(contextInput!)
+        .catch(() => ({ executionHostId }))
+        .then((context) => window.api.notifications.dispatch({ ...baseRequest, ...context }))
+
+  void dispatchResult
     .then((result) => {
       if (result.delivered) {
         void playDesktopNotificationSound(customSoundId, customSoundVolume)
@@ -244,6 +279,12 @@ export function dispatchTerminalNotification(
     })
 }
 
+function isAgentNotificationSource(
+  source: TerminalNotificationEvent['source']
+): source is 'agent-task-complete' | 'agent-needs-input' {
+  return source === 'agent-task-complete' || source === 'agent-needs-input'
+}
+
 export function useNotificationDispatch(
   worktreeId: string
 ): (event: TerminalNotificationEvent) => void {
diff --git a/src/renderer/src/components/terminal-pane/use-terminal-pane-lifecycle.ts b/src/renderer/src/components/terminal-pane/use-terminal-pane-lifecycle.ts
index 3dc04d3c..74e1faea 100644
--- a/src/renderer/src/components/terminal-pane/use-terminal-pane-lifecycle.ts
+++ b/src/renderer/src/components/terminal-pane/use-terminal-pane-lifecycle.ts
@@ -243,6 +243,7 @@ type UseTerminalPaneLifecycleDeps = {
     resumeProviderSession?: AgentProviderSessionMetadata
     launchToken?: string
     launchAgent?: TuiAgent
+    issueId?: string
     draftPrompt?: string
     /** Initial prompt-start status for agents that lack native prompt hooks. */
     initialAgentStatus?: { agent: TuiAgent; prompt: string }
diff --git a/src/renderer/src/hooks/agent-hook-completion-notifications.test.ts b/src/renderer/src/hooks/agent-hook-completion-notifications.test.ts
index 4803731f..bcdaae70 100644
--- a/src/renderer/src/hooks/agent-hook-completion-notifications.test.ts
+++ b/src/renderer/src/hooks/agent-hook-completion-notifications.test.ts
@@ -609,7 +609,7 @@ describe('agent hook completion notifications', () => {
     expect(dispatchTerminalNotification).toHaveBeenCalledWith(
       'wt-1',
       expect.objectContaining({
-        source: 'agent-task-complete',
+        source: 'agent-needs-input',
         paneKey,
         agentStatusSnapshot: expect.objectContaining({
           state: 'waiting',
@@ -637,7 +637,7 @@ describe('agent hook completion notifications', () => {
     expect(dispatchTerminalNotification).toHaveBeenCalledWith(
       'wt-1',
       expect.objectContaining({
-        source: 'agent-task-complete',
+        source: 'agent-needs-input',
         paneKey,
         terminalTitle: 'codex'
       })
diff --git a/src/renderer/src/hooks/agent-hook-completion-notifications.ts b/src/renderer/src/hooks/agent-hook-completion-notifications.ts
index 125c0994..b0522233 100644
--- a/src/renderer/src/hooks/agent-hook-completion-notifications.ts
+++ b/src/renderer/src/hooks/agent-hook-completion-notifications.ts
@@ -254,10 +254,8 @@ function createCoordinator(paneKey: string, worktreeId: string): AgentCompletion
       if (!isAgentTaskCompleteTrackingEnabled() || paneKeysRequiringFreshWorking.has(paneKey)) {
         return
       }
-      // Why: native notification settings still label this channel as "agent
-      // task complete"; the snapshot state makes the banner read "needs input".
       dispatchTerminalNotification(worktreeId, {
-        source: 'agent-task-complete',
+        source: 'agent-needs-input',
         terminalTitle: title,
         paneKey,
         suppressOsNotification: !isAgentTaskCompleteNotificationEnabled(),
diff --git a/src/renderer/src/hooks/useIpcEvents.ts b/src/renderer/src/hooks/useIpcEvents.ts
index b91a1372..460ee662 100644
--- a/src/renderer/src/hooks/useIpcEvents.ts
+++ b/src/renderer/src/hooks/useIpcEvents.ts
@@ -110,6 +110,8 @@ import {
 } from './runtime-project-refresh-scheduler'
 import { createRuntimeClientEventsSync } from './runtime-client-events-sync'
 import { detectLanguage } from '@/lib/language-detect'
+import { dispatchIssueFactsChanged } from '@/issues/issue-facts-changed-event'
+import { subscribeLocalIssueFacts } from '@/issues/local-issue-facts-subscription'
 import { makePaneKey, parsePaneKey } from '../../../shared/stable-pane-id'
 import { collectLeafIdsInOrder } from '@/components/terminal-pane/layout-serialization'
 import { track } from '@/lib/telemetry'
@@ -117,6 +119,7 @@ import { singlePaneLayoutSnapshot } from '@/store/slices/terminal-helpers'
 import { buildWorkspaceSessionPayload } from '@/lib/workspace-session'
 import { persistWorkspaceSessionByHost } from '@/lib/workspace-session-host-persistence'
 import { verifyTerminalRevealIdentity } from '@/lib/terminal-reveal-identity'
+import { openIssueNotification } from '@/issues/issue-notification-navigation'
 import { getLinearIssueWorkspaceName } from '../../../shared/workspace-name'
 import type { RuntimeClientEvent } from '../../../shared/runtime-client-events'
 import { applyHostWorktreeTerminalSleepState } from '@/components/terminal-pane/pty-shutdown-exit-deferral'
@@ -616,6 +619,7 @@ function getWorktreeRuntimeEnvironmentId(worktreeId: string | null | undefined):
 export function useIpcEvents(): void {
   useEffect(() => {
     const unsubs: (() => void)[] = []
+    unsubs.push(subscribeLocalIssueFacts(window.api.issueClientData))
     const reconnectAuthorityByTarget = new Map<string, DirectSshAuthority>()
     const authorityReconciliationDeadlines = new Set<{
       timer: ReturnType<typeof setTimeout>
@@ -1015,6 +1019,13 @@ export function useIpcEvents(): void {
           })
         return
       }
+      if (event.type === 'issueFactsChanged') {
+        dispatchIssueFactsChanged({
+          ...event,
+          routeExecutionHostId: toRuntimeExecutionHostId(environmentId)
+        })
+        return
+      }
       void ensureRuntimeEventRepoKnown(environmentId, event.repoId)
         .then(() => activateNotifiedWorktree(event, { allowRuntimeEnvironment: true }))
         .catch((error) => {
@@ -1198,6 +1209,14 @@ export function useIpcEvents(): void {
       })
     )
 
+    if (window.api.ui.onOpenIssueNotification) {
+      unsubs.push(
+        window.api.ui.onOpenIssueNotification((target) => {
+          void openIssueNotification(target).catch(() => undefined)
+        })
+      )
+    }
+
     // Why: a tray "Settings…" click can fire before this attaches; consume any queued intent (?. guards stale preload).
     void window.api.ui
       .consumePendingOpenSettings?.()
@@ -1472,6 +1491,7 @@ export function useIpcEvents(): void {
           resumeProviderSession,
           launchToken,
           launchAgent,
+          issueId,
           viewMode,
           title,
           ptyId,
@@ -1648,7 +1668,8 @@ export function useIpcEvents(): void {
                 ...(launchConfig ? { launchConfig } : {}),
                 ...(resumeProviderSession ? { resumeProviderSession } : {}),
                 ...(launchToken ? { launchToken } : {}),
-                ...(launchAgent ? { launchAgent } : {})
+                ...(launchAgent ? { launchAgent } : {}),
+                ...(issueId ? { issueId } : {})
               })
             }
             if (ptyId && terminalPresentation === 'background') {
diff --git a/src/renderer/src/lib/launch-agent-in-new-tab-web-runtime.test.ts b/src/renderer/src/lib/launch-agent-in-new-tab-web-runtime.test.ts
index 3ab3b997..c947090d 100644
--- a/src/renderer/src/lib/launch-agent-in-new-tab-web-runtime.test.ts
+++ b/src/renderer/src/lib/launch-agent-in-new-tab-web-runtime.test.ts
@@ -82,7 +82,8 @@ describe('launchAgentInNewTab paired web runtime', () => {
     const result = launchAgentInNewTab({
       agent: 'claude',
       worktreeId: 'wt-1',
-      groupId: 'group-1'
+      groupId: 'group-1',
+      issueId: '11111111-1111-4111-8111-111111111111'
     })
 
     expect(result).toEqual(expect.objectContaining({ tabId: null, pasteDraftAfterLaunch: false }))
@@ -93,6 +94,7 @@ describe('launchAgentInNewTab paired web runtime', () => {
       activate: true,
       agentSessionKind: 'fresh',
       agent: 'claude',
+      issueId: '11111111-1111-4111-8111-111111111111',
       viewMode: 'terminal'
     })
     expect(mocks.createTab).not.toHaveBeenCalled()
diff --git a/src/renderer/src/lib/launch-agent-in-new-tab.test.ts b/src/renderer/src/lib/launch-agent-in-new-tab.test.ts
index 8515379d..b2a49525 100644
--- a/src/renderer/src/lib/launch-agent-in-new-tab.test.ts
+++ b/src/renderer/src/lib/launch-agent-in-new-tab.test.ts
@@ -186,6 +186,24 @@ describe('launchAgentInNewTab', () => {
     })
   })
 
+  it('keeps an Issue binding on the local managed-agent startup', async () => {
+    const { launchAgentInNewTab } = await import('./launch-agent-in-new-tab')
+
+    launchAgentInNewTab({
+      agent: 'codex',
+      worktreeId: 'wt-1',
+      issueId: '11111111-1111-4111-8111-111111111111'
+    })
+
+    expect(mockQueueTabStartupCommand).toHaveBeenCalledWith(
+      'tab-1',
+      expect.objectContaining({
+        launchAgent: 'codex',
+        issueId: '11111111-1111-4111-8111-111111111111'
+      })
+    )
+  })
+
   it('keeps Floating Workspace authority on native Windows beside an active WSL project', async () => {
     store.projects = [
       {
diff --git a/src/renderer/src/lib/launch-agent-in-new-tab.ts b/src/renderer/src/lib/launch-agent-in-new-tab.ts
index d46cb530..4d4aefe0 100644
--- a/src/renderer/src/lib/launch-agent-in-new-tab.ts
+++ b/src/renderer/src/lib/launch-agent-in-new-tab.ts
@@ -33,6 +33,7 @@ import { seedNativeChatAppliedSessionOptions } from '@/components/native-chat/na
 export type LaunchAgentInNewTabArgs = {
   agent: TuiAgent
   worktreeId: string
+  issueId?: string
   /** Tab group the user launched from; keeps split-group launches in that pane instead of the active group. */
   groupId?: string
   /** Optional initial prompt; delivery depends on `promptDelivery` and the agent's prompt mode. */
@@ -73,6 +74,7 @@ export function launchAgentInNewTab(args: LaunchAgentInNewTabArgs): LaunchAgentI
   const {
     agent,
     worktreeId,
+    issueId,
     groupId,
     prompt,
     agentArgs,
@@ -150,6 +152,7 @@ export function launchAgentInNewTab(args: LaunchAgentInNewTabArgs): LaunchAgentI
       agent,
       worktreeId,
       environmentId: runtimeEnvironmentId,
+      issueId,
       groupId,
       cwd: initialCwd,
       startupPlan,
@@ -190,6 +193,7 @@ export function launchAgentInNewTab(args: LaunchAgentInNewTabArgs): LaunchAgentI
     ...(startupPlan.env ? { env: startupPlan.env } : {}),
     launchConfig: startupPlan.launchConfig,
     launchAgent: agent,
+    ...(issueId ? { issueId } : {}),
     ...(agentArgs !== undefined ? { agentArgsOverride: agentArgs } : {}),
     ...(startupPlan.sessionOptions ? { sessionOptions: startupPlan.sessionOptions } : {}),
     ...(startupPlan.startupCommandDelivery
diff --git a/src/renderer/src/lib/launch-agent-web-host-tab.ts b/src/renderer/src/lib/launch-agent-web-host-tab.ts
index 528798d4..4f0f0125 100644
--- a/src/renderer/src/lib/launch-agent-web-host-tab.ts
+++ b/src/renderer/src/lib/launch-agent-web-host-tab.ts
@@ -35,6 +35,7 @@ function removeStaleLocalAgentTabsForWebHostLaunch(worktreeId: string): void {
 export function launchAgentInWebHostTab(args: {
   agent: TuiAgent
   worktreeId: string
+  issueId?: string
   environmentId: string | null
   groupId?: string
   cwd?: string | null
@@ -50,6 +51,7 @@ export function launchAgentInWebHostTab(args: {
   const {
     agent,
     worktreeId,
+    issueId,
     environmentId,
     groupId,
     cwd,
@@ -69,6 +71,7 @@ export function launchAgentInWebHostTab(args: {
   removeStaleLocalAgentTabsForWebHostLaunch(worktreeId)
   const launch = {
     worktreeId,
+    ...(issueId ? { issueId } : {}),
     environmentId,
     targetGroupId: groupId,
     activate: true,
diff --git a/src/renderer/src/lib/right-sidebar-visibility.ts b/src/renderer/src/lib/right-sidebar-visibility.ts
index 08ab538d..c6563f95 100644
--- a/src/renderer/src/lib/right-sidebar-visibility.ts
+++ b/src/renderer/src/lib/right-sidebar-visibility.ts
@@ -6,6 +6,7 @@ type ActiveView = AppState['activeView']
 const RIGHT_SIDEBAR_SUPPRESSED_VIEWS = new Set<ActiveView>([
   'settings',
   'tasks',
+  'issues',
   'activity',
   'automations',
   'space',
diff --git a/src/renderer/src/lib/titlebar-worktree-history-controls.ts b/src/renderer/src/lib/titlebar-worktree-history-controls.ts
index 1e35e2da..a3e35707 100644
--- a/src/renderer/src/lib/titlebar-worktree-history-controls.ts
+++ b/src/renderer/src/lib/titlebar-worktree-history-controls.ts
@@ -1,5 +1,10 @@
 import type { UISlice } from '@/store/slices/ui'
 
 export function shouldShowWorktreeHistoryControls(activeView: UISlice['activeView']): boolean {
-  return activeView === 'terminal' || activeView === 'tasks' || activeView === 'automations'
+  return (
+    activeView === 'terminal' ||
+    activeView === 'tasks' ||
+    activeView === 'issues' ||
+    activeView === 'automations'
+  )
 }
diff --git a/src/renderer/src/lib/worktree-activation.ts b/src/renderer/src/lib/worktree-activation.ts
index cd2f1bd1..68d93c20 100644
--- a/src/renderer/src/lib/worktree-activation.ts
+++ b/src/renderer/src/lib/worktree-activation.ts
@@ -59,6 +59,7 @@ import { seedNativeChatAppliedSessionOptions } from '@/components/native-chat/na
 import type { SessionOptionValue } from '../../../shared/native-chat-session-options'
 import type { ExecutionHostId } from '../../../shared/execution-host'
 import { findFolderWorkspaceOwner } from './folder-workspace-runtime-owner'
+import { replayIssueDetailHistoryEntry } from '@/issues/issue-navigation'
 
 /** Telemetry threaded from the launch site to `pty:spawn`; main fires `agent_started`
  *  only after the spawn succeeds. See telemetry-plan.md§Agent launch semantics. */
@@ -72,6 +73,7 @@ export type WorktreeStartupPayload = {
   resumeProviderSession?: AgentProviderSessionMetadata
   launchToken?: string
   launchAgent?: TuiAgent
+  issueId?: string
   draftPrompt?: string
   /**
    * The unsent launch context, for the initial view-mode decision ONLY.
@@ -754,6 +756,10 @@ setWorktreeNavActivator(activateAndRevealWorkspace)
 
 // Why: page entries replay via setActiveView (not open*Page) so back/forward doesn't mutate previousViewBefore* or duplicate history (see navigateToIndex).
 setWorktreeNavViewActivator((entry) => {
+  if (typeof entry === 'object' && entry.kind === 'issue-detail') {
+    replayIssueDetailHistoryEntry(entry)
+    return
+  }
   if (entry === 'automations') {
     useAppStore.getState().setActiveView(entry)
     return
diff --git a/src/renderer/src/runtime/remote-agent-session-launch.test.ts b/src/renderer/src/runtime/remote-agent-session-launch.test.ts
index 0fb6399d..93ab9da9 100644
--- a/src/renderer/src/runtime/remote-agent-session-launch.test.ts
+++ b/src/renderer/src/runtime/remote-agent-session-launch.test.ts
@@ -56,6 +56,23 @@ describe('remote agent-session launch routing', () => {
     expect(hostAuthority).not.toHaveBeenCalled()
   })
 
+  it('fails closed for an Issue-bound launch when the capability is absent', async () => {
+    const hostAuthority = vi.fn().mockResolvedValue('structured')
+    const legacy = vi.fn().mockResolvedValue('legacy')
+    mocks.supportsCapability.mockResolvedValue(false)
+
+    await expect(
+      runRemoteAgentSessionLaunch({
+        environmentId: 'env-1',
+        hostAuthority,
+        requireHostAuthority: true,
+        legacy
+      })
+    ).rejects.toThrow('Issue-bound agent launch requires a newer Orca host')
+    expect(legacy).not.toHaveBeenCalled()
+    expect(hostAuthority).not.toHaveBeenCalled()
+  })
+
   it('keeps legacy behavior when a read-only capability probe fails', async () => {
     const hostAuthority = vi.fn().mockResolvedValue('structured')
     const legacy = vi.fn().mockResolvedValue('legacy')
@@ -68,6 +85,23 @@ describe('remote agent-session launch routing', () => {
     expect(hostAuthority).not.toHaveBeenCalled()
   })
 
+  it('fails closed for an Issue-bound launch when capability probing fails', async () => {
+    const hostAuthority = vi.fn().mockResolvedValue('structured')
+    const legacy = vi.fn().mockResolvedValue('legacy')
+    mocks.supportsCapability.mockRejectedValue(new Error('status temporarily unavailable'))
+
+    await expect(
+      runRemoteAgentSessionLaunch({
+        environmentId: 'env-1',
+        hostAuthority,
+        requireHostAuthority: true,
+        legacy
+      })
+    ).rejects.toThrow('Issue-bound agent launch requires a newer Orca host')
+    expect(legacy).not.toHaveBeenCalled()
+    expect(hostAuthority).not.toHaveBeenCalled()
+  })
+
   it('does not bypass an incompatible runtime protocol', async () => {
     const compatibilityError = Object.assign(new Error('runtime incompatible'), {
       code: 'runtime_compat_block'
@@ -119,6 +153,26 @@ describe('remote agent-session launch routing', () => {
     expect(legacy).toHaveBeenCalledOnce()
   })
 
+  it('does not downgrade an Issue-bound launch after a structured rejection', async () => {
+    const legacy = vi.fn().mockResolvedValue('legacy')
+    mocks.supportsCapability.mockResolvedValue(true)
+    const legacyRequired = new RuntimeRpcCallError({
+      id: 'request-1',
+      ok: false,
+      error: { code: 'agent_session_legacy_required', message: 'legacy required' }
+    })
+
+    await expect(
+      runRemoteAgentSessionLaunch({
+        environmentId: 'env-1',
+        hostAuthority: vi.fn().mockRejectedValue(legacyRequired),
+        requireHostAuthority: true,
+        legacy
+      })
+    ).rejects.toBe(legacyRequired)
+    expect(legacy).not.toHaveBeenCalled()
+  })
+
   it('uses legacy when a replaced old host does not recognize the structured method', async () => {
     const legacy = vi.fn().mockResolvedValue('legacy')
     mocks.supportsCapability.mockResolvedValue(true)
diff --git a/src/renderer/src/runtime/remote-agent-session-launch.ts b/src/renderer/src/runtime/remote-agent-session-launch.ts
index b00c6cbb..8b67fed2 100644
--- a/src/renderer/src/runtime/remote-agent-session-launch.ts
+++ b/src/renderer/src/runtime/remote-agent-session-launch.ts
@@ -3,13 +3,20 @@ import type { RuntimeCapability } from '../../../shared/protocol-version'
 import { RuntimeRpcCallError, runtimeEnvironmentSupportsCapability } from './runtime-rpc-client'
 import { isRuntimeCompatBlockError } from './runtime-protocol-compat'
 
+const ISSUE_BOUND_HOST_AUTHORITY_REQUIRED =
+  'Issue-bound agent launch requires a newer Orca host'
+
 export async function runRemoteAgentSessionLaunch<TResult>(args: {
   environmentId: string
   hostAuthority?: () => Promise<TResult>
   hostAuthorityCapability?: RuntimeCapability
+  requireHostAuthority?: boolean
   legacy: (options: { skipCompatibilityCheck: boolean }) => Promise<TResult>
 }): Promise<TResult> {
   if (!args.hostAuthority) {
+    if (args.requireHostAuthority) {
+      throw new Error(ISSUE_BOUND_HOST_AUTHORITY_REQUIRED)
+    }
     return await args.legacy({ skipCompatibilityCheck: false })
   }
   let supported: boolean
@@ -22,6 +29,9 @@ export async function runRemoteAgentSessionLaunch<TResult>(args: {
     if (isRuntimeCompatBlockError(error)) {
       throw error
     }
+    if (args.requireHostAuthority) {
+      throw new Error(ISSUE_BOUND_HOST_AUTHORITY_REQUIRED)
+    }
     // Why: a failed read-only probe has not launched anything, so preserving
     // the legacy path cannot duplicate an agent and keeps transient upgrades neutral.
     return await args.legacy({ skipCompatibilityCheck: true })
@@ -29,6 +39,9 @@ export async function runRemoteAgentSessionLaunch<TResult>(args: {
   // Why: choose before invoking either path; an ambiguous structured outcome
   // must never trigger a legacy retry that could spawn a duplicate.
   if (!supported) {
+    if (args.requireHostAuthority) {
+      throw new Error(ISSUE_BOUND_HOST_AUTHORITY_REQUIRED)
+    }
     return await args.legacy({ skipCompatibilityCheck: true })
   }
   try {
@@ -38,6 +51,9 @@ export async function runRemoteAgentSessionLaunch<TResult>(args: {
       error instanceof RuntimeRpcCallError &&
       (error.code === 'agent_session_legacy_required' || error.code === 'method_not_found')
     ) {
+      if (args.requireHostAuthority) {
+        throw error
+      }
       // Why: both responses prove no structured side effect began: the new host rejected an old
       // lower owner before dispatch, or an old host never recognized the method.
       return await args.legacy({ skipCompatibilityCheck: true })
diff --git a/src/renderer/src/runtime/runtime-client-events.ts b/src/renderer/src/runtime/runtime-client-events.ts
index 26311663..037397fe 100644
--- a/src/renderer/src/runtime/runtime-client-events.ts
+++ b/src/renderer/src/runtime/runtime-client-events.ts
@@ -88,6 +88,7 @@ function isRuntimeClientEvent(
     message.type === 'worktreesChanged' ||
     message.type === 'nativeChatLaunchDraftResolved' ||
     message.type === 'terminalSideEffects' ||
+    message.type === 'issueFactsChanged' ||
     message.type === 'sshStateChanged' ||
     message.type === 'linearLinkedIssueUpdated' ||
     message.type === 'activateWorktree' ||
diff --git a/src/renderer/src/runtime/web-runtime-session.test.ts b/src/renderer/src/runtime/web-runtime-session.test.ts
index 115edd0c..c4f66574 100644
--- a/src/renderer/src/runtime/web-runtime-session.test.ts
+++ b/src/renderer/src/runtime/web-runtime-session.test.ts
@@ -1714,6 +1714,7 @@ describe('createWebRuntimeSessionTerminal', () => {
           agentEnv: { CODEX_PROFILE: 'captured' }
         },
         launchAgent: 'codex',
+        issueId: '11111111-1111-4111-8111-111111111111',
         prompt: 'linked issue context',
         promptDelivery: 'draft',
         agentArgs: '--model gpt-5 --profile captured',
@@ -1731,6 +1732,7 @@ describe('createWebRuntimeSessionTerminal', () => {
         clientOperationId: expect.stringMatching(/^\d{13}-[0-9a-f]{32}$/),
         worktree: `id:${WORKTREE_ID}`,
         agent: 'codex',
+        issueId: '11111111-1111-4111-8111-111111111111',
         prompt: 'linked issue context',
         promptDelivery: 'draft',
         agentArgs: '--model gpt-5 --profile captured',
@@ -1767,6 +1769,54 @@ describe('createWebRuntimeSessionTerminal', () => {
     )
   })
 
+  it('fails closed instead of dropping an Issue binding on an old host', async () => {
+    const runtimeCall = vi.fn(async (request: { method: string }) => {
+      if (request.method === 'status.get') {
+        return {
+          id: 'status',
+          ok: true,
+          result: {
+            runtimeId: 'old-runtime',
+            graphStatus: 'ready',
+            runtimeProtocolVersion: 3,
+            minCompatibleRuntimeClientVersion: 2,
+            capabilities: []
+          }
+        }
+      }
+      if (request.method === 'session.tabs.createTerminal') {
+        return {
+          id: 'legacy-create',
+          ok: true,
+          result: {
+            tab: { id: 'legacy-tab-1' },
+            publicationEpoch: 'epoch-1',
+            snapshotVersion: 1
+          }
+        }
+      }
+      return { id: 'list', ok: true, result: makeSnapshot() }
+    })
+    vi.stubGlobal('window', {
+      api: { runtimeEnvironments: { call: runtimeCall } }
+    })
+
+    await expect(
+      createWebRuntimeSessionTerminal({
+        worktreeId: WORKTREE_ID,
+        launchAgent: 'codex',
+        issueId: '11111111-1111-4111-8111-111111111111'
+      })
+    ).resolves.toEqual({
+      status: 'failed',
+      message: 'Issue-bound agent launch requires a newer Orca host'
+    })
+
+    expect(runtimeCall).not.toHaveBeenCalledWith(
+      expect.objectContaining({ method: 'session.tabs.createTerminal' })
+    )
+  })
+
   it('keeps exact legacy ordering when structured creation cannot express afterTabId', async () => {
     const runtimeCall = vi
       .fn()
diff --git a/src/renderer/src/runtime/web-runtime-session.ts b/src/renderer/src/runtime/web-runtime-session.ts
index 0f4bae03..11642490 100644
--- a/src/renderer/src/runtime/web-runtime-session.ts
+++ b/src/renderer/src/runtime/web-runtime-session.ts
@@ -157,6 +157,7 @@ function matchesWebSessionIntentOwner(owner: WebSessionIntentOwner): boolean {
 
 type CreateWebRuntimeSessionTerminalArgs = {
   worktreeId: string
+  issueId?: string
   environmentId?: string | null
   afterTabId?: string
   targetGroupId?: string
@@ -319,6 +320,7 @@ async function createWebRuntimeSessionTerminalResult(
                       {
                         worktree: toRuntimeWorktreeSelector(args.worktreeId),
                         agent,
+                        ...(args.issueId ? { issueId: args.issueId } : {}),
                         ...(args.prompt ? { prompt: args.prompt } : {}),
                         ...(args.promptDelivery ? { promptDelivery: args.promptDelivery } : {}),
                         ...(agentArgsOverride !== undefined
@@ -342,6 +344,7 @@ async function createWebRuntimeSessionTerminalResult(
       }>({
         environmentId,
         ...(hostAuthority ? { hostAuthority } : {}),
+        ...(args.issueId ? { requireHostAuthority: true } : {}),
         ...(args.agentSessionKind === 'resume' && agent === 'omp'
           ? { hostAuthorityCapability: AGENT_SESSION_OMP_RESUME_PATH_RUNTIME_CAPABILITY }
           : {}),
diff --git a/src/renderer/src/store/slices/terminals.ts b/src/renderer/src/store/slices/terminals.ts
index cb7537df..920a392a 100644
--- a/src/renderer/src/store/slices/terminals.ts
+++ b/src/renderer/src/store/slices/terminals.ts
@@ -570,6 +570,7 @@ export type TerminalSlice = {
       resumeProviderSession?: AgentProviderSessionMetadata
       launchToken?: string
       launchAgent?: TuiAgent
+      issueId?: string
       /** Explicit CLI override for host-owned agent launches; omission uses host settings. */
       agentArgsOverride?: string | null
       draftPrompt?: string
@@ -752,6 +753,7 @@ export type TerminalSlice = {
       resumeProviderSession?: AgentProviderSessionMetadata
       launchToken?: string
       launchAgent?: TuiAgent
+      issueId?: string
       agentArgsOverride?: string | null
       draftPrompt?: string
       sessionOptions?: Record<string, SessionOptionValue>
@@ -772,6 +774,7 @@ export type TerminalSlice = {
     resumeProviderSession?: AgentProviderSessionMetadata
     launchToken?: string
     launchAgent?: TuiAgent
+    issueId?: string
     agentArgsOverride?: string | null
     draftPrompt?: string
     sessionOptions?: Record<string, SessionOptionValue>
diff --git a/src/renderer/src/store/slices/ui.ts b/src/renderer/src/store/slices/ui.ts
index 896e9bce..bbd87864 100644
--- a/src/renderer/src/store/slices/ui.ts
+++ b/src/renderer/src/store/slices/ui.ts
@@ -613,6 +613,7 @@ export type UISlice = {
   previousViewBeforeTasks:
     | 'terminal'
     | 'settings'
+    | 'issues'
     | 'activity'
     | 'automations'
     | 'space'
@@ -621,6 +622,7 @@ export type UISlice = {
   previousViewBeforeSettings:
     | 'terminal'
     | 'tasks'
+    | 'issues'
     | 'activity'
     | 'automations'
     | 'space'
@@ -630,6 +632,7 @@ export type UISlice = {
     | 'terminal'
     | 'settings'
     | 'tasks'
+    | 'issues'
     | 'automations'
     | 'space'
     | 'artifacts'
@@ -638,6 +641,7 @@ export type UISlice = {
     | 'terminal'
     | 'settings'
     | 'tasks'
+    | 'issues'
     | 'activity'
     | 'space'
     | 'artifacts'
@@ -646,6 +650,7 @@ export type UISlice = {
     | 'terminal'
     | 'settings'
     | 'tasks'
+    | 'issues'
     | 'activity'
     | 'automations'
     | 'artifacts'
@@ -654,6 +659,7 @@ export type UISlice = {
     | 'terminal'
     | 'settings'
     | 'tasks'
+    | 'issues'
     | 'activity'
     | 'automations'
     | 'space'
@@ -662,10 +668,20 @@ export type UISlice = {
     | 'terminal'
     | 'settings'
     | 'tasks'
+    | 'issues'
     | 'activity'
     | 'automations'
     | 'space'
     | 'mobile'
+  previousViewBeforeIssues:
+    | 'terminal'
+    | 'settings'
+    | 'tasks'
+    | 'activity'
+    | 'automations'
+    | 'space'
+    | 'artifacts'
+    | 'mobile'
   setActiveView: (view: UISlice['activeView']) => void
   taskPageData: {
     preselectedRepoId?: string
@@ -728,6 +744,8 @@ export type UISlice = {
     options?: { recordTasksInteraction?: boolean }
   ) => void
   closeTaskPage: () => void
+  openIssuesPage: () => void
+  closeIssuesPage: () => void
   openActivityPage: () => void
   closeActivityPage: () => void
   selectedAutomationId: string | null
@@ -1241,6 +1259,7 @@ export const createUISlice: StateCreator<AppState, [], [], UISlice> = (set, get)
   previousViewBeforeSpace: 'terminal',
   previousViewBeforeMobile: 'terminal',
   previousViewBeforeArtifacts: 'terminal',
+  previousViewBeforeIssues: 'terminal',
   setActiveView: (view) => set({ activeView: view }),
   taskPageData: {},
   taskResumeState: undefined,
@@ -1424,6 +1443,24 @@ export const createUISlice: StateCreator<AppState, [], [], UISlice> = (set, get)
         worktreeNavHistoryIndex: nextHistoryIndex
       }
     }),
+  openIssuesPage: () =>
+    set((state) => ({
+      activeView: 'issues',
+      previousViewBeforeIssues:
+        state.activeView === 'issues' ? state.previousViewBeforeIssues : state.activeView
+    })),
+  closeIssuesPage: () =>
+    set((state) => {
+      const currentEntry = state.worktreeNavHistory[state.worktreeNavHistoryIndex]
+      const previousIndex =
+        typeof currentEntry === 'object' && currentEntry.kind === 'issue-detail'
+          ? findPrevLiveWorktreeHistoryIndex(state)
+          : null
+      return {
+        activeView: state.previousViewBeforeIssues,
+        worktreeNavHistoryIndex: previousIndex ?? state.worktreeNavHistoryIndex
+      }
+    }),
   openActivityPage: () => {
     if (get().settings?.experimentalActivity !== true) {
       return
diff --git a/src/renderer/src/store/slices/worktree-nav-history.ts b/src/renderer/src/store/slices/worktree-nav-history.ts
index f59485d9..77d79c5a 100644
--- a/src/renderer/src/store/slices/worktree-nav-history.ts
+++ b/src/renderer/src/store/slices/worktree-nav-history.ts
@@ -5,6 +5,8 @@ import type { GitHubWorkItem } from '../../../../shared/github/work-item-types'
 import type { JiraIssue } from '../../../../shared/jira-types'
 import type { LinearIssue } from '../../../../shared/linear/issue-types'
 import type { GitLabWorkItem } from '../../../../shared/gitlab-types'
+import type { ExecutionHostId } from '../../../../shared/execution-host'
+import type { AuthorityHostPartitionKey } from '../../../../shared/issues/types'
 import {
   getTaskSourceCacheScope,
   type TaskSourceContext
@@ -42,9 +44,17 @@ export type WorktreeNavHistoryTaskDetailEntry =
       issue: JiraIssue
       sourceContext?: TaskSourceContext | null
     }
+export type WorktreeNavHistoryIssueDetailEntry = {
+  kind: 'issue-detail'
+  authorityId: string
+  hostPartitionKey: AuthorityHostPartitionKey
+  routeExecutionHostId: ExecutionHostId
+  issueId: string
+}
 export type WorktreeNavHistoryViewEntry =
   | WorktreeNavHistorySimpleViewEntry
   | WorktreeNavHistoryTaskDetailEntry
+  | WorktreeNavHistoryIssueDetailEntry
 export type WorktreeNavHistoryEntry = (string & {}) | WorktreeNavHistoryViewEntry
 
 export type WorktreeNavHistorySlice = {
@@ -90,6 +100,9 @@ function getHistoryEntryKey(entry: WorktreeNavHistoryEntry): string {
   if (typeof entry === 'string') {
     return entry === 'tasks' || entry === 'automations' ? `view:${entry}` : `worktree:${entry}`
   }
+  if (entry.kind === 'issue-detail') {
+    return `view:issue-detail:${entry.authorityId}:${entry.hostPartitionKey}:${entry.issueId}`
+  }
   if (entry.source === 'github') {
     const sourceScope =
       entry.sourceContext?.provider === 'github'
diff --git a/src/shared/agent-hook-listener.ts b/src/shared/agent-hook-listener.ts
index 82c9bfc9..97fdb5d3 100644
--- a/src/shared/agent-hook-listener.ts
+++ b/src/shared/agent-hook-listener.ts
@@ -344,6 +344,8 @@ export type AgentHookEventPayload = {
   hookEventName?: string
   /** Claude's provider-owned user-prompt UUID. */
   providerPromptId?: string
+  /** Provider-owned turn identity when the hook exposes one. */
+  providerTurnId?: string
   /** Active Claude compact generation, keyed by provider prompt identity. */
   compactTrigger?: 'manual' | 'auto'
   /** Claude tool-use identifier when the hook source exposes one. */
@@ -4289,6 +4291,10 @@ export function normalizeHookPayload(
       : extractAgentProviderSession(source, hookPayloadRecord)
   const providerPromptId =
     source === 'claude' ? normalizeClaudePromptId(hookPayloadRecord.prompt_id) : undefined
+  const providerTurnId =
+    source === 'codex'
+      ? readFirstString(hookPayloadRecord, ['turn_id', 'turnId'])
+      : undefined
   const compactTrigger =
     source === 'claude' &&
     (eventName === 'PreCompact' || eventName === 'PostCompact') &&
@@ -4495,6 +4501,7 @@ export function normalizeHookPayload(
         promptInteractionKey,
         hookEventName: typeof eventName === 'string' ? eventName : undefined,
         providerPromptId,
+        providerTurnId,
         compactTrigger,
         toolUseId: readFirstString(hookPayloadRecord, ['tool_use_id', 'toolUseId']),
         toolAgentId: readFirstString(hookPayloadRecord, ['agent_id', 'agentId']),
diff --git a/src/shared/agent-hook-relay.ts b/src/shared/agent-hook-relay.ts
index f14db403..5ca7fa78 100644
--- a/src/shared/agent-hook-relay.ts
+++ b/src/shared/agent-hook-relay.ts
@@ -87,6 +87,8 @@ export type AgentHookRelayEnvelope = {
   hookEventName?: string
   /** Claude's provider-owned user-prompt UUID. */
   providerPromptId?: string
+  /** Provider-owned turn identity when the hook exposes one. */
+  providerTurnId?: string
   /** Active Claude compact generation, keyed by provider prompt identity. */
   compactTrigger?: 'manual' | 'auto'
   /** Claude tool execution id, when the source hook provides one. */
diff --git a/src/shared/agent-session-host-authority.ts b/src/shared/agent-session-host-authority.ts
index ec9f5776..d415db55 100644
--- a/src/shared/agent-session-host-authority.ts
+++ b/src/shared/agent-session-host-authority.ts
@@ -117,12 +117,14 @@ export type RuntimeEnsureAgentSessionRequest =
 export type RuntimeEnsureAgentSessionResult = {
   terminal: RuntimeTerminalCreate
   disposition: 'created' | 'adopted'
+  conversationId?: string
 }
 
 export type RuntimeCreateAgentSessionRequest = {
   clientOperationId: string
   worktree: string
   agent: TuiAgent
+  issueId?: string
   prompt?: string
   promptDelivery?: AgentPromptDelivery
   /** Explicit client override. Omission keeps launch defaults host-owned. */
@@ -137,6 +139,7 @@ export type RuntimeCreateAgentSessionRequest = {
 export type RuntimeCreateAgentSessionResult = {
   terminal: RuntimeTerminalCreate
   disposition: 'created' | 'replayed'
+  conversationId?: string
 }
 
 export type RuntimeAgentSessionRpcCaller = {
diff --git a/src/shared/dashboard-snapshot.ts b/src/shared/dashboard-snapshot.ts
index a3a888d8..c4b9170a 100644
--- a/src/shared/dashboard-snapshot.ts
+++ b/src/shared/dashboard-snapshot.ts
@@ -1,5 +1,9 @@
 import type { AgentType } from './agent-status-types'
 import type { ExecutionHostId } from './execution-host'
+import type {
+  IssueAuthorityDescriptor,
+  IssueDisplayProjection
+} from './issues/types'
 import type { RepoIcon } from './repo-icon'
 import type { TuiAgent } from './tui-agent'
 
@@ -57,6 +61,16 @@ export type DashboardCardSubagent = {
 export type DashboardCardHostKind = 'local' | 'ssh' | 'wsl' | 'remote'
 export type DashboardCardWorkspaceKind = 'worktree' | 'folder'
 
+export type DashboardCardIssueContext =
+  | {
+      status: 'mapped'
+      authority: IssueAuthorityDescriptor
+      conversationId: string
+      issue: IssueDisplayProjection | null
+      latestRound: { id: string; readAt: number | null } | null
+    }
+  | { status: 'unmapped' | 'unsupported' | 'unavailable' }
+
 export type DashboardWorkspace = {
   repoId: string
   worktreeId: string
@@ -132,6 +146,8 @@ export type DashboardCard = {
    *  user last acknowledged it (visited its tab / opened its dashboard dialog).
    *  Derived from the app-wide ack map so both surfaces mute in lockstep. */
   unseen: boolean
+  /** Optional Issues projection. Missing means the snapshot predates Issues. */
+  issueContext?: DashboardCardIssueContext
   /** Short summary of the pending question when bucket === 'attention'. */
   askSummary?: string
   /** The tab's conversation name, resolved exactly as the sidebar's agent rows
@@ -214,6 +230,16 @@ export type DashboardRevealAgentArgs = {
   leafId: string | null
 }
 
+export type DashboardAckAgentArgs = {
+  paneKey: string
+  executionHostId?: ExecutionHostId
+}
+
+export type DashboardOpenIssueArgs = {
+  authority: IssueAuthorityDescriptor
+  issueId: string
+}
+
 export type DashboardSpawnAgentArgs = {
   worktreeId: string
   agent: TuiAgent
diff --git a/src/shared/notification-settings-types.ts b/src/shared/notification-settings-types.ts
index fe4ef740..79ca9086 100644
--- a/src/shared/notification-settings-types.ts
+++ b/src/shared/notification-settings-types.ts
@@ -1,4 +1,5 @@
 import type { AgentStatusState, AgentType } from './agent-status-types'
+import type { ExecutionHostId } from './execution-host'
 
 export type NotificationSettings = {
   enabled: boolean
@@ -21,7 +22,11 @@ export type NotificationSettings = {
   customSoundVolume: number
 }
 
-export type NotificationEventSource = 'agent-task-complete' | 'terminal-bell' | 'test'
+export type NotificationEventSource =
+  | 'agent-task-complete'
+  | 'agent-needs-input'
+  | 'terminal-bell'
+  | 'test'
 
 export type NotificationDispatchRequest = {
   source: NotificationEventSource
@@ -36,6 +41,13 @@ export type NotificationDispatchRequest = {
   hasMultipleActiveRepos?: boolean
   terminalTitle?: string
   isActiveWorktree?: boolean
+  /** Exact foreground checks from the renderer; old clients omit both and use worktree fallback. */
+  isVisibleForegroundPane?: boolean
+  isVisibleRoundRecord?: boolean
+  executionHostId?: ExecutionHostId
+  conversationId?: string
+  issueId?: string
+  roundRecordId?: string
   agentType?: AgentType
   agentState?: AgentStatusState
   agentPrompt?: string
diff --git a/src/shared/protocol-version.ts b/src/shared/protocol-version.ts
index a2b20a8b..3677a9cb 100644
--- a/src/shared/protocol-version.ts
+++ b/src/shared/protocol-version.ts
@@ -1,4 +1,5 @@
 import { REMOTE_SERVER_UPDATE_CAPABILITY } from './remote-server-update'
+import { ORCA_ISSUES_RUNTIME_CAPABILITY } from './issues/constants'
 
 // Why: declares the Orca runtime RPC compatibility contract. Desktop,
 // headless server, CLI, and mobile builds may drift in app version, but
@@ -92,6 +93,7 @@ export const AGENT_SESSION_OMP_RESUME_PATH_RUNTIME_CAPABILITY =
 export const FILE_MUTATION_OWNERSHIP_RUNTIME_CAPABILITY = 'files.mutation-ownership.v1' as const
 export const FILE_MUTATION_OWNERSHIP_UPDATE_REQUIRED_MESSAGE =
   'Remote file changes require a newer Orca server. Update the HUB and try again.'
+export { ORCA_ISSUES_RUNTIME_CAPABILITY } from './issues/constants'
 
 export const RUNTIME_CAPABILITIES = [
   'runtime.status.compat.v1',
@@ -128,7 +130,8 @@ export const RUNTIME_CAPABILITIES = [
   AGENT_SESSION_OMP_RESUME_PATH_RUNTIME_CAPABILITY,
   FILE_MUTATION_OWNERSHIP_RUNTIME_CAPABILITY,
   ACCOUNT_IMPORT_RUNTIME_CAPABILITY,
-  CODEX_RESET_CREDIT_RUNTIME_CAPABILITY
+  CODEX_RESET_CREDIT_RUNTIME_CAPABILITY,
+  ORCA_ISSUES_RUNTIME_CAPABILITY
 ] as const
 
 export type RuntimeCapability = (typeof RUNTIME_CAPABILITIES)[number] | (string & {})
diff --git a/src/shared/runtime-client-events.ts b/src/shared/runtime-client-events.ts
index c849f2f6..04e0c45d 100644
--- a/src/shared/runtime-client-events.ts
+++ b/src/shared/runtime-client-events.ts
@@ -7,12 +7,14 @@ import type {
 import type { SshConnectionState } from './ssh-types'
 import type { TerminalSideEffectBatch } from './terminal-side-effect-facts'
 import type { RuntimeNativeChatLaunchDraftResolution } from './runtime-types'
+import type { IssueFactsChangedEvent } from './issues/types'
 
 export type RuntimeClientEvent =
   | { type: 'reposChanged' }
   | { type: 'worktreesChanged'; repoId: string }
   | ({ type: 'nativeChatLaunchDraftResolved' } & RuntimeNativeChatLaunchDraftResolution)
   | { type: 'terminalSideEffects'; batch: TerminalSideEffectBatch }
+  | IssueFactsChangedEvent
   // Why: SSH connections live on the runtime host; paired clients have no IPC
   // channel for ssh:state-changed, so without this event their reconnect
   // overlays never learn the host connected (STA-1468).
diff --git a/src/shared/runtime-types.ts b/src/shared/runtime-types.ts
index d6b257ca..c9d34ac5 100644
--- a/src/shared/runtime-types.ts
+++ b/src/shared/runtime-types.ts
@@ -641,6 +641,7 @@ type RuntimeTerminalCreateBaseRequestPayload = {
   resumeProviderSession?: AgentProviderSessionMetadata
   launchToken?: string
   launchAgent?: TuiAgent
+  issueId?: string
   viewMode?: 'terminal' | 'chat'
   startupCommandDelivery?: StartupCommandDelivery
   title?: string
@@ -666,6 +667,7 @@ export type RuntimeTerminalCreateRequestPayload =
 
 export type RuntimeTerminalCreate = {
   handle: string
+  conversationId?: string
   tabId?: string
   paneKey?: string | null
   ptyId?: string | null
diff --git a/src/shared/top-level-view.ts b/src/shared/top-level-view.ts
index 479c1611..65a0c1af 100644
--- a/src/shared/top-level-view.ts
+++ b/src/shared/top-level-view.ts
@@ -6,6 +6,7 @@ const TOP_LEVEL_VIEW_LOOKUP: Record<TopLevelView, true> = {
   terminal: true,
   settings: true,
   tasks: true,
+  issues: true,
   activity: true,
   automations: true,
   space: true,
diff --git a/src/shared/ui-chrome-types.ts b/src/shared/ui-chrome-types.ts
index 80c78d55..d53c746a 100644
--- a/src/shared/ui-chrome-types.ts
+++ b/src/shared/ui-chrome-types.ts
@@ -110,6 +110,7 @@ export type TopLevelView =
   | 'terminal'
   | 'settings'
   | 'tasks'
+  | 'issues'
   | 'activity'
   | 'automations'
   | 'space'
diff --git a/src/shared/worktree/launch-types.ts b/src/shared/worktree/launch-types.ts
index 5fc9f18d..b625cbca 100644
--- a/src/shared/worktree/launch-types.ts
+++ b/src/shared/worktree/launch-types.ts
@@ -19,6 +19,7 @@ export type WorktreeStartupLaunch = {
   launchConfig?: SleepingAgentLaunchConfig
   launchToken?: string
   launchAgent?: TuiAgent
+  issueId?: string
   viewMode?: 'terminal' | 'chat'
   startupCommandDelivery?: StartupCommandDelivery
   telemetry?: { agent_kind: AgentKind; launch_source: LaunchSource; request_kind: RequestKind }
