---
title: 2026-09-15 取消路径位置拦截验证
document_type: test-run
status: completed
created: 2026-09-15
updated: 2026-09-15
---

# 2026-09-15 取消路径位置拦截验证

引用的测试规格见 [远程工作区绝对路径打开功能测试](../cases/远程工作区绝对路径打开功能测试.md)。本文只记录这一轮的实际执行，不改写 Case 正文。

## 1. 环境与版本

| 项 | 值 |
| --- | --- |
| 执行时间 | 2026-09-15 |
| 执行人 | Claude（会话内自动化） |
| 分支 / worktree | `feat/absolute-path-open-any-path` @ `/Users/bytedance/dev/orca-absolute-path-any` |
| 基线 | 从 `fork/integration` (`d3ef847c1`) 切出 |
| 客户端 | macOS（darwin-arm64），Electron 无头启动，`ORCA_BACKGROUND_LAUNCH=1`，不抢焦点 |
| 单元测试 | `pnpm exec vitest run --config config/vitest.config.ts src/renderer/src/components/tab-entry-remote-path src/renderer/src/components/tab-bar` |
| 真机验证 | `.docs/absolute-path-any-ui-validation/2026-09-15/scripts/run-validation.sh` |

## 2. 覆盖与结果

| Test Case | 关联需求 | 方式 | 结果 |
| --- | --- | --- | --- |
| TC-501～TC-506、TC-509～TC-512 | REQ-301～REQ-305 | 单元 | PASS（回归，未改动） |
| TC-507 | REQ-306 | 单元 | PASS |
| TC-508 | REQ-306 | 单元 | PASS（断言改为「照发请求并回显主机错误」） |
| TC-517 | REQ-306 | 单元 | PASS |
| TC-518 | REQ-306、REQ-305 | 单元 | PASS |
| TC-519 | REQ-306 | 单元 | PASS |
| TC-520 | REQ-306、REQ-301 | 端到端（真机） | PASS |
| TC-513～TC-515 | REQ-302～REQ-304 | 端到端（SSH） | 本轮未执行（本轮未触碰 SSH 分支，见 §4） |
| TC-516 | REQ-301 | 端到端（真机） | 由 TC-520 覆盖同一断言 |

单元合计：63 个文件 529 个用例通过。

## 3. 真机执行记录

TC-520 在真实应用里驱动「+」弹窗，一条在 worktree 外、一条在 worktree 内：

| 输入 | 弹窗给出的行 | 回车后的 tab |
| --- | --- | --- |
| `/var/folders/.../orca-outside-ED5TbO/outside-note.md`（worktree 外） | `Open file · outside-note.md` | `relativePath` 为完整路径 |
| `<worktree>/README.md`（worktree 内） | `Open file · README.md` | `relativePath` 为 `README.md` |

同时断言两条状态行文案都没有出现：改动前的「This remote workspace can only open files inside its worktree.」与「Absolute paths are unavailable until the workspace host is ready.」。

逐步 JSON 日志与两张截图在 `.docs/absolute-path-any-ui-validation/2026-09-15/evidence/`。

## 4. 未覆盖与原因

- TC-513～TC-515（SSH 远端绝对路径）本轮未重跑。本轮改动只涉及分类阶段的路径位置判定与 runtime 环境 id 的目录校验，未触碰 SSH 分支；SSH 分支的证据仍为 2026-09-11 那一轮。
- 用户机器上「本地工作区被判成远程 runtime」的原始现场未能在真机复现：该现场依赖一个实际不存在的环境 id 出现在 store 中，成因未定位（见 journal 未解决问题）。本轮对该情形的覆盖为 TC-517～TC-519 的单元证据。

## 5. 门禁

`pnpm tc`、`oxlint`、`check:architecture-policies`、`check:fork-features`、`check:fork-docs`、三项 `verify:localization-*` 全部通过。`check:code-quality:changed` 报 319 项，与本分支基线逐项相同（改动前后同为 319），本轮未新增。
