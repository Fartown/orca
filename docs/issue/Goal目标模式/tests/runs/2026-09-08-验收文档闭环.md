---
title: Goal 验收文档闭环验证
document_type: test-run
status: reviewing
created: 2026-09-08
updated: 2026-09-08
---

# Goal 验收文档闭环验证

## 1. 执行信息

- 执行时间：2026-09-08。
- 环境：macOS，本地独立 worktree；全部命令带 `ORCA_BACKGROUND_LAUNCH=1`。真实守卫样例使用独立目录与 Goal Home。
- 版本：`feat/goal-acceptance-document`，基线 `9899e7148023dae0fb5fab201e70944a384fd23f` 加本轮未提交变更。
- 执行人：Codex。
- 范围：REQ-121、TC-344～TC-346；既有 Goal 与 CLI 回归。

## 2. 执行结果

| Case ID | 实际结果 | 结论 | 证据 | 遗留问题 |
| --- | --- | --- | --- | --- |
| TC-344 | 隐藏 Electron 中点击生成、预览、修改、开始；实际创建收据 applied，record/spec、goals.get 与 judge-criteria.md 逐字一致；实际驱动调用守卫时包含完整最终文档，界面显示 FAIL 和具体缺口，执行进程 stdin 实际收到失败反馈；真实 Codex 生成与判词独立补证 | PASS | ui-checkpoints.json、01～08 截图、生成与裁判日志 | UI 使用固定 CLI fixture，真实 Codex 调用为宿主/CLI 层证据 |
| TC-345 | 实际 UI 验证目标变化阻止开始、失败/取消保留输入；实际进程取消清理守卫树；复现并修复取消确认前 ready 返回被误采纳的竞态 | PASS | ui-checkpoints.json、取消前后回归日志、real-process-cancellation.json | 无该场景未解决问题 |
| TC-346 | 普通文件夹中的真实守卫生成成功；宿主测试拒绝过期和远程终端；163 项旧 CLI 回归通过 | PASS（自动化和守卫层） | Goal 与 CLI 测试日志、真实守卫生成 | 不宣称真实远程环境测试通过 |

其他检查：`pnpm tc`、应用与驱动构建、四项本地化检查、fork-features、fork-docs 通过。改动范围质量检查 0 新问题，`check:architecture-policies --base HEAD` 通过。

## 3. 证据索引

证据根目录：`.docs/goal-acceptance-document-ui-validation/2026-09-08/`（本地 ignored）。

| 证据 | 类型 | 对应 Case | 说明 |
| --- | --- | --- | --- |
| `evidence/goal-tests.log` / `cli-tests.log` | 日志 | 全部 | 95 / 163 项通过 |
| `evidence/real-codex-generation.json` | 文档与结果 | TC-344、TC-346 | 真实读取的来源、验收项和方法；未修改样例源码 |
| `evidence/real-codex-judge.log` / `real-codex-judge-exit.json` | 判词及退出码 | TC-344 | 同一文档真实验收；预期 FAIL，实际退出 1，包含具体缺口 |
| `evidence/real-process-cancellation.json` | 真实进程验证 | TC-345 | 假守卫和子进程都忽略 SIGTERM，取消后实际进程树退出，返回 cancelled 且无文档 |
| `evidence/ui-checkpoints.json`、`screenshots/01-initial.png` 至 `08-created-document.png` | 实际 UI / 截图 | TC-344、TC-345 | 8 项真实 UI 检查点通过 |
| `evidence/ui-restart-chinese.json`、09/10 截图 | 重启与中文 | TC-344 | 重启文档一致；中文文案与按钮边界正常 |
| `evidence/driver-loop/ui-complete-loop-proof.json`、`ui-driver-feedback.json`、12-complete-fixture-loop.png | 完整运行闭环 | TC-344 | 完整通过运行 ui-run-1788877770216：实际 driver/judge 调用收到完整最终文档；界面显示失败；原执行进程 stdin 收到 FAIL、具体缺口及用户补充条目 |
| `evidence/architecture-baseline.json` | 归因 | 架构检查 | 340 项全分支差异均已存在于基线 HEAD，涉及文件与本轮改动无交集 |
| `README.md` | 说明 | 全部 | 验证范围、未执行项和 Electron 隔离约束 |

## 4. 缺陷与遗留问题

| 问题 | 程度 | 对应 Case | 处理状态 |
| --- | --- | --- | --- |
| 通用 electron Skill 缺失 | 已解决验证阻碍 | TC-344、TC-345 | 用户授权复用现有 Playwright Electron fixture，已执行后台验证 |
| 取消确认前采纳迟到文档 | 已修复 | TC-345 | before-fix 复现失败，after-fix 6 项组件测试通过，实际 UI 取消通过 |
| 长工作区路径撑宽表单，生成按钮超出视口 | 已修复 | TC-344 | fieldset/选择器最小宽度及路径截断修复；实际 clientWidth=scrollWidth=639，按钮可正常点击 |
| 英文开始按钮文字溢出 | 已修复 | TC-344 | 按钮自适应宽度，实际文字边界在按钮内；中文亦通过 |
| 隔离 profile 路径过长，runtime/daemon socket 无法监听 | 测试环境已解决 | TC-344 | 短路径独立 profile 正常发布真实 metadata 并运行；证据仍归档在 .docs |
| 测试执行进程未启用 TTY raw mode，长提示未进入 stdin | 测试替身已修正 | TC-344 | 按真实 TUI 启用 raw mode 后，原进程实际收到完整失败反馈；未修改产品终端路径 |
| 默认全分支架构门禁报 340 项已有范围差异 | 基线问题 | 架构检查 | 已证实与本轮改动无交集；没有扩大 allowlist 或调整策略掩盖 |

## 5. 本轮结论

代码、95 项 Goal 测试、163 项 CLI 回归、真实守卫调用及隐藏 Electron 文档交付流程已有证据。实际 UI 8 项基础检查点、中文界面、重启回读，以及新增的守卫失败显示和终端反馈两项检查点均通过，发现的三项产品问题已修复。UI 链路使用固定 CLI 测试替身，实际 host、driver、judge、RPC、PTY 与存储均为本轮构建；真实 Codex 另以独立宿主/CLI 样例验证。全分支架构门禁仍有 340 项既有差异，因此 Journal 保留 testing，不把本轮功能验证等同于全仓库门禁通过或已发布。未操作用户 App、历史 Goal 或原 Claude 会话。
