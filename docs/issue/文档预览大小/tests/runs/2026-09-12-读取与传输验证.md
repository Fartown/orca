---
title: 文档预览 20 MiB 读取与传输验证
document_type: test-run
status: completed
created: 2026-09-12
updated: 2026-09-12
issue: 文档预览大小
---

# 文档预览 20 MiB 读取与传输验证

- 执行时间：2026-09-12，Asia/Hong_Kong。
- 环境：macOS，本机 Node 24.18.0；测试使用 ORCA_BACKGROUND_LAUNCH=1；未启动或替换用户 App。
- 代码版本：feat/document-preview-size，基线 a84d9eb04ac4804f113200024cf9598fe3680f2d，加本轮未提交修改。
- 执行人：Codex。
- 覆盖范围：REQ-401/402；用例参见 [TC-401/402/403](../cases/预览大小边界.md)。
- 最终执行：09:31:55，10 个测试文件、83 个测试全部通过，耗时 6.22 秒；最后一次 pnpm tc 与 17 个变更源码文件的三类代码质量检查均 exit 0。

## 实际结果

| 范围 | 结果 | 证据与边界 |
| --- | --- | --- |
| TC-401 文件边界 | PASS | 本地及 runtime-owned 预览读取 512 KiB+1、20 MiB 完整返回；20 MiB+1 拒绝；校验全部字节及末尾标记 |
| TC-401 SSH 传输 | PASS | 真实 FsHandler、dispatcher、mux、codec 经内存管道传回 20 MiB；单帧小于既有 control lane 预算；不是实际 SSH 服务器部署 |
| TC-401 runtime 传输 | PASS | 预览 reader → RpcDispatcher/真实参数 schema → 文件句柄读取 → 受限 JSON 序列化 → reader 重组；每次响应小于 512 KiB；不是实际 paired WebSocket/网络测试 |
| TC-402 预算保持 | PASS | 普通文件和旧客户端保留限制；JSON 超预算仍拒绝；既有授权和二进制测试通过 |
| TC-403 分块完整性 | PASS | 中文/emoji 跨块无损，空文件、二进制和旧主机完整响应保留；短块与版本改变拒绝 |
| 类型与局部门禁 | PASS | pnpm tc；changed-code quality 三类检查无新增问题；oxlint；fork-features；fork-docs；document-preview-size 架构策略；max-lines ratchet |
| 本地化 | PASS | localization-catalog、localization-runtime-catalog、localization-extraction 通过；已有翻译缺项不由本轮改动产生 |
| 全仓架构门禁 | FAIL（基线已有） | columns:、tests/e2e/worktree-switch-first-paint.spec.ts 不在上游接缝白名单；对基线 SHA 与 origin/main merge-base 的只读差异核对，两文件在本轮开始前已存在差异 |
| RPC 生成目录 | FAIL（基线已有） | 生成器差异全为 goals / conversations / issues 目录缺项，非本轮 chunk schema；已撤回本轮生成动作带来的无关变化，未混入其他功能 |
| Electron UI / 安装包 | 未执行 | 没有启动日常 App、真实远端或浏览器；不能把读取测试等同界面验收 |

## 复现命令

```sh
ORCA_BACKGROUND_LAUNCH=1 pnpm exec vitest run --config config/vitest.config.ts src/main/document-preview-size src/shared/doc-preview-file-access.test.ts src/main/browser/doc-preview-file-reader.test.ts src/main/browser/doc-preview-protocol.test.ts src/main/runtime/orca-runtime-files-preview-budget.test.ts src/main/runtime/rpc/methods/files-doc-preview.test.ts src/main/providers/ssh-filesystem-doc-preview.test.ts src/relay/fs-list-files-large-response.integration.test.ts
pnpm tc
ORCA_CODE_QUALITY_BASE=a84d9eb04ac4804f113200024cf9598fe3680f2d pnpm check:code-quality:changed
pnpm check:fork-features
pnpm check:fork-docs
pnpm check:architecture-policies --policy document-preview-size
```

## 结论与遗留问题

代码层读取/传输主流程通过，20 MiB 并非仅修改常量：SSH 已避开单帧限制，runtime 已按授权分块读取。新客户端连接旧主机保持旧行为，完整 20 MiB 需要执行端也更新。未提交、未合入、未打包、未安装；整体交付与真实 UI 验收尚未完成。
