---
title: 文档整理定向验证
document_type: test-run
status: completed
executed: 2026-09-05
---

# 文档整理定向验证（拆分前历史快照）

> 本文保存 2026-09-05 08:44 的实际测试及随后第一次归并校验；其中“16 份文档、3 个决策、2 份 Test Run”描述拆分前结构，不代表当前三个独立 issue。仅迁移证据，没有重新执行命令。当前入口见 [三个需求索引](../../issue/README.md)。

## 1. 环境与版本

- 执行人：Codex。
- 工作区：本地 `/Users/bytedance/dev/orca`。
- HEAD：`0b1e92fa9a783a978f3f43ac8f4eea1c5c55a3f9`。
- 代码版本补充：包含整理开始前已暂存的标题来源拆分实现，不能仅用 HEAD 复现标题测试。
- Node：v24.18.0；Vitest：v4.1.11。
- 原有 staged diff SHA-256：`4574289899a73ae1ef86d12fd5adf55a75fa6acea2efe5bcd3e4061e3df123da`。

## 2. 实际执行

命令：

```sh
pnpm exec vitest run --config config/vitest.config.ts src/main/artifacts/artifact-cloud-config.test.ts src/shared/session-display-title.test.ts
```

执行时间：2026-09-05 08:44:22（Asia/Hong_Kong）；退出码 0。

```text
Test Files  2 passed (2)
     Tests  14 passed (14)
  Duration  139ms
```

| 测试文件                                           | 实际结果         | 覆盖与限制                                                                                                                                                                                       |
| -------------------------------------------------- | ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `src/main/artifacts/artifact-cloud-config.test.ts` | PASS，9 个测试项 | 默认、loopback、私网、公网限制、path、伪私网域名、网段边界、非法 octet、auth override。对应 TC-401～TC-408 的已有断言；TC-405 只覆盖 path，TC-408 只覆盖 override 判定，不冒充整个扩展规格已跑完 |
| `src/shared/session-display-title.test.ts`         | PASS，5 个测试项 | 标题来源优先级、live 优于身份兜底、Provider 兜底降级、人工名与兜底同字串、trim/全空。只验证纯函数，不证明四处 UI、异步刷新、迁移或重启链路                                                       |

TC-409 配置优先级扩展组合未单独执行。Goal 用例本轮未执行；Issues 完整回归、类型检查、构建、真实 Electron App、SSH、paired runtime、Windows 与 Linux 均未执行。

## 3. 文档与工作区校验

| 检查                   | 实际结果                                                                                       |
| ---------------------- | ---------------------------------------------------------------------------------------------- |
| task-leader 阶段校验   | PASS；16 份文档、48 个 REQ、3 个决策、2 份 Test Run；未使用表示产品完成的 strict 门禁          |
| 表格与标题用例补充校验 | PASS；278 个 TC 连续、唯一，包含 Issues 230、Goal 39、产物 9；48 个 REQ 都有技术映射和测试关联 |
| 优先级                 | Issues 76/117/37，Goal 19/18/2，产物 6/3/0（依次 P0/P1/P2）                                    |
| 新 issue Markdown 链接 | 全部本地链接存在，含源码、历史截图与备份路径                                                   |
| 原稿恢复验证           | tar 解压到独立临时目录，58 个条目的 SHA-256 全部与整理前清单一致                               |
| 原暂存区               | staged diff SHA-256 与整理前完全相同                                                           |
| 功能源码               | 没有新增 unstaged 非文档代码改动；额外配置变更仅新 issue 的 `.gitignore` 例外                  |
| `git diff --check`     | PASS                                                                                           |

通用 task-leader 脚本只识别标题形式的 TC，原 Issues 的 230 条表格用例通过独立校验器补全，不把脚本输出的 48 个标题 TC 误当总数。补充校验脚本为 [validate-consolidation.mjs](../../../tmp/tasks/2026-09-05-local-doc-consolidation/validate-consolidation.mjs)。

## 4. 结论边界

本轮定向测试支持 URL 策略和标题优先级的代码描述。文档整理不改变运行代码；不能用 14 条通过宣称三个需求验收完成。历史真实 App 结果独立保存在 [2026-08-30 执行记录](../../issue/Issues看板与会话/tests/runs/2026-08-30-Issues历史App验证.md)，当前待回归范围见各测试规格。
