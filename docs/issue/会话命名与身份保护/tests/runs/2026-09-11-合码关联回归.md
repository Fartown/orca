---
title: 会话命名合码关联回归
document_type: test-run
status: completed
created: 2026-09-11
updated: 2026-09-11
issue: 会话命名与身份保护
---

# 会话命名合码关联回归

## 范围与复现

- 用户要求提交并合入 `fork/integration`；关联 REQ-025/026/028 与 [Provider优先命名验收](../cases/Provider优先命名验收.md)。本轮不改变需求或删除消费者。
- 基线为已推送的 `8d6a4e5c3454ca995e05a79f0fdd86ff076ae54d`；PR 为 [Fartown/orca#5](https://github.com/Fartown/orca/pull/5)，目标已包含 `831bde8f7`。
- 复核旧 CI 后，在当前基线执行五个关联测试文件：52 条中 47 PASS、5 FAIL；旧注册检查没有包含这五个文件，不能用此前注册回归全绿替代完整 CI。
- 本地环境：macOS、fnm Node 24.18.0；测试均设置 `ORCA_BACKGROUND_LAUNCH=1`。未启动、替换或关闭用户 App。

## 失败归类与修复

| 失败 | 直接证据与原因 | 修复及保留边界 |
| --- | --- | --- |
| 结构化会话计时 | Host feed 明确传入 `stateStartedAt=100`，同一结构化会话补齐 Provider ID 后，命名层改写为 200 并追加虚假换会话历史 | 既有 `structuredHostOwned` 路径尊重 host 的 turn 时钟；普通终端 A→B 的计时与历史保护保留 |
| React Doctor | `useSessionNameIndex` 的 `[requests]` Effect 依赖每次渲染新建的数组 | 直接从既有语义 key 建立 watch，删除冗余 ref/Effect；相同内容不重订阅，host/SID/path 变化与卸载正确清理 |
| 热更新扫描断言 | 50 次 live 写入前已因首次建立 binding 枚举 retained/sleeping 各一次；最终计数 2 并非热更新扫描 | 分开断言初始化为 2、热更新新增枚举为 0；不调整产品扫描实现、不放宽热更新要求 |
| 快捷命令标题断言 | 旧测试仍要求不生成任务候选，与已有任务优先契约冲突 | 明确保存任务候选；无 agent 身份的终端仍显示命令标签，有 agent 身份时显示任务名 |
| Relay 精确响应断言 | 实际响应已有独立 `nameEvidence` 与 `generatedTitle`，旧期望只列 legacy `title` | 精确断言全部字段与 `unavailable`，保持不调用 broad scanner；未更改 wire 或 relay 产品代码 |
| 身份调用点清单 | 命名通知消费者新增两处引用，dispatch 多一处调用，未归类 | 补精确路径/次数的 action-consumer 登记；未禁用清单检查 |

## 验证记录

- 首轮修复后 6 文件 53 条有 1 条失败：测试仍把无 agent 身份的终端当成 agent 标签。补上非 agent 与显式 agent 两种输入后通过；产品排序未改。
- 26 文件 152/152 PASS：包括完整 `src/renderer/src/session-names`、上述五个关联测试、新增订阅生命周期与结构化计时历史断言。原始报告：`.docs/session-name-refactor-ui-validation/2026-09-11/ci-regression.json`。
- 与 fork 镜像 `Fartown/main` 对齐的 React Doctor 检查 exit 0，无阻断 error；既有 warnings 保留。architecture-policies、fork-features 和定向 oxlint 通过。
- 首轮完整类型检查发现新增测试中 host 字面量被拓宽为 string；使用已有 `SessionNameRequest` 类型约束测试输入后，完整 `pnpm tc` 复跑通过。
- 新增五文件关联回归到注册 checks；新订阅测试、既有结构化桥接测试加入登记。只增四个测试文件的精确 seam/scope，不增加产品接缝或放宽阈值。
- 全部 9 组注册回归通过，分别为 246、52、96、116、14、15、124、64、23 条（集合有重叠，不相加）；定向普通/type-aware lint、fork-docs、max-lines ratchet 与四项本地化门禁通过。
- 本记录完成的是本地关联回归，当前修复的远端 CI 待提交推送后执行；不构成全量真实 UI 验收，最终交付状态以 PR checks 和合并记录为准。

## 未解决事项

- 既有 Dashboard 非活动 pane 候选缺口及 Claude 活跃窗口启发式边界不混入本次 CI 修复；不宣称完整命名需求已完成。
- 原集成工作树未提交内容、具名 stash 与安装 App 保留。尚未完成 PR 合并时不得标记已合入。
