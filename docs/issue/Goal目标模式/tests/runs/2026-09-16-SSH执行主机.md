---
title: Goal SSH 执行主机验证
document_type: test-run
status: reviewing
created: 2026-09-16
updated: 2026-09-16
---

# Goal SSH 执行主机验证

## 1. 执行信息

- 需求：REQ-101、REQ-102、REQ-104、REQ-110、REQ-111、REQ-122；TC-357～TC-361。
- 工作区：`orca-goal-remote-host`，分支 `feat/goal-remote-host`，起点 `d3ef847c1b33`。
- 隔离：隐藏 Mac Electron、独立 profile/Goal Home、独立 loopback sshd/密钥/HOME；`ORCA_BACKGROUND_LAUNCH=1`。未改用户正在运行的 App、SSH relay 或 provider 会话。
- 原始证据：`.docs/goal-remote-host-ui-validation/2026-09-16/`（ignored），含脚本、截图、构建指纹和逐次观测。

## 2. 原缺陷与实测修复

原基线 `evidence/baseline-1789524205448/`：SSH 终端真实可用，但 Goal 工作区为空、生成禁用、文案固定“在本机执行”。失败来自 Goal 本机限制，不是 SSH 不可用。

首个修复包 `evidence/fixed-1789525055977/` 又复现提前续轮：working hook 可能先于 terminal.send 的完成回包，原分类器未认定新轮 hook，再把 12 秒无输出当结束；worker 还在工作就收到第二次注入，最终预算耗尽。不能把这轮记成通过，也不能未经证据归咎于 SSH 缓存重放。

修复后 SSH 只用 hook 确认结束；working 始终等待，缺 hook 或传输失联保持 unverifiable，重连不重启运行。离线详情标出旧快照及不可验证，控制按钮不可用。

## 3. 隐藏 App 与真实 SSH

最终完整流程 `evidence/fixed-1789525994804/`，**12/12 通过**。构建树 SHA-256 `cb85a931ee527e9a45e28c5140d356604da2ad647076d3f5ff068e233feda89b`，1427 个构建文件；逐项截图、provider ledger、每秒 RPC/hook 观测均在该目录。

| 检查 | 实际结果 |
| --- | --- |
| R1 SSH 目标选择 | 无执行会话也可选工作区并生成 |
| R2 关闭与重开 | 生成继续，同一草稿恢复进度和真实活动 |
| R3 后台完成 | 草稿列表找回，文档存于远端 Goal Home |
| R4 查看文档 | 真实远程路径，已有 Markdown tab 完整渲染，externalSshTargetId 匹配 |
| R5 执行注入 | 远端 worker 在远端 cwd/HOME 收到任务及 claim 路径 |
| R6 断联 | provider 仍活着；UI 标旧快照/不可验证，没有宣称退出 |
| R7 重连与守卫 | 同一 Goal，仅 1 轮注入，执行中额外提交为 0；远端运行 `node verify.mjs` 后独立 judge 返回 PASS，UI 显示“已独立验证” |
| R8 失败反馈 | 远端生成器的受控失败可见 |
| R9 重试 | 新生成成功，同一草稿重新可审阅 |
| F1 非 Git SSH folder | 无执行会话也能生成 |
| F2 folder 文档 | 文件在远端生成并在原生 Markdown tab 渲染 |
| F3 同路径不同主机 | 注册相同物理路径到本机后，不展示远端 Goal 或草稿 |

本机 provider sentinel 未接到错误的生成/执行请求。最终窗口 `visible=false/focused=false`；测试 App、profile、SSH root 和密钥已清理，`remainingPids=[]`。中途一次关闭 Sheet 动画未结束即点重开造成脚本失败，补等待后通过；保留失败证据，不算产品修复。

### 最新集成复验

合入集成分支 `1f4e75213` 后，针对新的 hook 缓存和 Goal fence，按 `a98a0b07b` 精确快照重新构建并完整执行同样 12 项，**12/12 通过**。证据 `evidence/integrated-1789527031283/`，构建树 SHA-256 `e97aa05208a80250ecaf535bd6981c0679a6aa97a4327ae7da73d0dae70f8e52`。远端 worker/独立 judge 各一次，首轮验收完成，运行中额外提交为 0；folder 和主机隔离再次通过，清理后 `remainingPids=[]`。

完整矩阵截图发现两处旧文案误写“本机安装 CLI”和“保存到本地”，修正后以 `bdd56870e` 的四处文本变更补测生成状态文案与远程 Markdown，3/3 通过（`evidence/copy-1789527295994/`）。最终包 SHA-256 `f408d0c2bb71c7b98ca429be76dc511e561bdd871cdc218846ca7c3bbb729335`；逐文件差异仅 renderer，main/preload/CLI/relay/driver 未变。两轮页面/控制台错误为 0，窗口均隐藏且未获焦，清理无残留。

合并冲突仅涉及 Journal 两条开发记录、生成索引及 hook 缓存重构；保留双方开发记录，缓存采用上游修复并保留 Goal 只读入口。提交钩子引入的一处无关上游测试格式化已恢复。没有更改上游状态机来迁就 Goal。

## 4. 自动化与纯 Node 宿主

- 完整 feature 测试：28 文件、176/176 通过；合入 `1f4e75213` 后连同 hook/fence 接缝回归 31 文件、200/200 通过（`evidence/feature-tests-integration.log`），无跳过。
- CLI 完整回归 166/166 通过，`evidence/cli-tests-final.log`。合入集成没有改变 CLI 状态机。
- 增量代码质量、类型断言、type-aware、React Doctor、设计系统检查均 0 新问题，`evidence/code-quality-integration.log`；架构、fork-features/fork-docs、本地化目录/运行时目录/提取/覆盖门禁通过。
- `pnpm tc`：退出码 0，`evidence/typecheck-integration.log`。
- SSH hook/断联分类专项：退出码 0，`evidence/ssh-round-regression.log`。
- relay 与 orcad 构建通过；orcad bundle 不含 electron 或 node:sqlite 导入。`evidence/build-relay-hook-confirmation.log`、`evidence/build-orcad-final.log`。
- 纯 Node orcad：真实 RPC status ready、保存文档在独立 Goal Home、停止重启后同一草稿修订与正文保持，3 项通过。`evidence/orcad-smoke/result.json` 和 `runtime.log`；两次 daemon 自检均为真实 PTY spawn healthy，测试进程退出后无残留。
- orcad smoke 用测试 preload 模拟“未安装桌面浏览器”，仅拦截已安装 App 的路径探测；Goal、RPC、存储、daemon 均为真实构建。首次脚本在 ready 消息前发 SIGTERM 的失败属于脚本时序，已改为等 `orca_server_ready` 后再测重启。

## 5. 验证边界

SSH 是同一台 Mac 上的真实 SSH/SFTP/relay，不是不同物理机器或 Linux。provider 为确定性的独立进程：实际读取远端需求、写文件、发 hook、执行检查和返回 verdict，没有调用真实模型。配对 orcad 覆盖真实服务注册/存储/重启，未做完整 paired UI 执行。Windows、WSL、标准 release 安装升级未在本轮验收；用户运行中的安装包未替换。
