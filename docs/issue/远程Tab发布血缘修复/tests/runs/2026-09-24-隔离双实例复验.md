---
title: "2026-09-24 隔离双实例复验"
document_type: test-run
status: completed
created: 2026-09-24
updated: 2026-09-24
---

# 2026-09-24 隔离双实例复验

## 1. 执行信息

- 执行时间：2026-09-24 10:26–10:36，Asia/Shanghai；逐步 UTC 时间见证据 JSON。
- 测试环境：macOS；从源码构建的 Electron 43.7.0；两个独立 App 通过本机 paired runtime 连接。复用仓库 Playwright/Electron fixture，以客户端真实 DOM 点击和键盘输入验证。
- 候选版本：`45f19aad8f177a99453505ab6a34b00f229184c8`，隔离工作树 `session-tabs-isolated-validation`。
- 完整旧版：`1f4e752132e8f3e57f7b87f1e4133b60b8236139`，另一隔离工作树 `session-tabs-isolated-old`。
- 执行人：Codex。覆盖 TC-707 的关闭、新 Tab 同步与 shell 消息子集，补充旧版对照、输出丢失阳性对照和新 host / 旧 client 兼容验证。**TC-707 整体仍未完成。**
- 隔离措施：每次运行使用独立 HOME、userData、临时 Git 仓库与配对凭据；`ORCA_BACKGROUND_LAUNCH=1`，窗口 hidden / unfocused；未操作正式 Orca。临时关闭 macOS login-shell 环境探测，避免隔离 HOME 下 mise 初始化阻塞。
- 受控前提：在关闭请求到达 host 的边界，删除 victim 的 runtime Tab map 条目，构造“快照尚存、父 Tab 已不在 map”的状态以进入实际 headless close。随后仍由客户端关闭按钮调用产品链路。此测试没有证明该前提在现场如何自然形成。
- 观测插桩：只在一次性构建产物导出 main composition root；TS 产品源码未变。仅 `old-close-mutation` 额外在调用边界恢复旧 epoch 写法；`old-full` 使用完整旧版的自然实现。

## 2. 执行结果

表内 PASS 指该对照实验的预期成立，不表示旧版正常，也不表示 TC-707 全部验收通过。

| Case ID / 场景 | 实际结果 | 结论 | 证据 | 缺陷或遗留问题 |
| --- | --- | --- | --- | --- |
| TC-707 子集 / fixed | close 后 renderer epoch 不变、version 8→9；新 Tab 可见；5 次界面发送均回显；无重连横幅 | PASS（子集） | `fixed.json`、`fixed-client.png` | 非真实 agent/hooks |
| TC-707 负对照 / old-close-mutation | renderer→headless、version 6→7；新 Tab 不可见；既有终端 5 次发送仍回显；无重连横幅 | PASS（复现同步缺陷） | `old-close-mutation.json` | 未复现发送后重连 |
| TC-707 负对照 / old-full | 完整旧版自然生成 headless epoch、version 7→8；新 Tab 不可见；5 次发送仍回显；无重连横幅 | PASS（复现同步缺陷） | `old-full.json` | 同步缺陷不等于本次条件下必然重连 |
| TC-707 观测阳性对照 / fixed-positive-control | 正常 5 次发送无横幅；故意丢弃输出后 watchdog 触发 recovering 横幅；恢复后本次和下一次消息均回显 | PASS（观测有效） | `fixed-positive-control.json` | 此注入不是现场根因证据 |
| TC-707 兼容子集 / fixed-host-old-client | 新 host 配旧 client；epoch 保留、version 7→8；新 Tab 可见；5 次发送回显；无横幅 | PASS（子集） | `fixed-host-old-client.json` | 不代表全部混合版本兼容性 |
| TC-707 完整现场验收 | 真实 agent 连续消息、hooks、mini SSH 条件及拖动/置顶等操作未完整执行 | BLOCKED（验收证据不足） | 上述原始证据 | 不得登记为全量 PASS |

## 3. 证据索引

证据根目录：`.docs/session-tabs-isolated-ui-validation/2026-09-24/`（git 忽略；位于本隔离工作树）。

| 证据 | 类型 | 对应 Case | 说明 |
| --- | --- | --- | --- |
| `README.md` | 说明 | TC-707 | 结论、可复跑命令与限制 |
| `scripts/epoch-message.spec.ts`、`scripts/playwright.config.ts` | 脚本 | TC-707 | 真实界面输入、xterm 回显、MutationObserver 捕获短暂横幅 |
| `evidence/results-summary.json` | JSON | TC-707 | 五个有效场景汇总 |
| `evidence/<场景>.json`、`<场景>-run.log`、`<场景>-client.png` | JSON / 日志 / 截图 | TC-707 | host/client 隔离信息、close-path、快照、回显与界面；截图在阳性注入之前采集 |
| `evidence/<场景>-host-daemon.log` | 日志 | TC-707 | survivor 的 session-attached 计数均为 0，包括确实出现横幅的阳性对照 |
| `build/*instrumentation.json`、`*e2e-build.log` | 构建记录 | TC-707 | 插桩前后 SHA256 与源码构建日志 |
| `evidence/setup-selector-failure.json`、`setup-close-ack-race.json` | 失败记录 | TC-707 | 前两次 harness 调整：本地化 selector、等待 host 关闭确认；未计入五次有效结果 |
| `evidence/cleanup.json` | 清理记录 | TC-707 | 14 个临时 repo/worktree 已清理；隔离 profile 和 App 进程均无残留 |

## 4. 缺陷与遗留问题

| 问题 | 严重程度 | 对应 Case | 处理状态 |
| --- | --- | --- | --- |
| 用户报告的每次发送后重连没有在完整旧版复现，不能归因于 epoch 或宣布修复 | P0 验收缺口 | TC-707 / Q-702 | open |
| daemon session-attached 为 0 不能证明客户端未重连；阳性对照直接否定该替代指标 | P1 测试判据缺口 | TC-707 | 已在本轮改用 DOM 全程观测；用例说明已同步 |
| 浏览器 Tab 退役、其他六个写路径尚无本轮真实 App 验收 | P1 覆盖缺口 | REQ-702 | open；原单测证据不能替代 UI 验收 |
| loopback 双实例不能代表 mini SSH 的网络、agent 与 hooks 环境 | P1 环境边界 | TC-707 | open |

## 5. 本轮结论

- 候选修复保留现任 epoch 的方向正确；真实客户端正反对照证实关闭路径的 Tab 同步改善，且新 host 能服务旧 client。
- 本轮没有产品源码变更，没有提交、合并或部署。需求与 Journal 保持 testing，REQ-704 的发送后重连因果环节仍未完成。
- 阳性对照捕获到同款“正在重新连接到远程运行时”横幅并恢复；daemon session-attached 仍为 0。因此 Kimi 的无 daemon 重挂判据不足以验收无客户端重连。
- 未接受“剩余项可免测”的风险；本测试记录 completed 只表示本轮执行与归档结束。
- 后续定位应在隔离环境加入现场同款 agent/hooks，围绕命令响应探测、PTY sequence、输出订阅与 transport 关闭事件采证；只有复现原始触发条件后，才能决定是否需要独立的重连修复。不得直接在正式 App 试错。

- 归档校验：fork-docs、fork-features、architecture-policies、task-leader 阶段校验和 git diff --check 均通过。证据：`evidence/document-checks.json`。未重复全量 typecheck；本轮无产品源码修改。
