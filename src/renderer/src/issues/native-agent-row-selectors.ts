// Why: the Issue row wraps the untouched Workspace row and can only tell a row click from its nested controls by DOM.
export const NATIVE_AGENT_ROW_CLASS = 'worktree-agent-row-hover'
export const NATIVE_AGENT_ROW_SEND_TARGET_ATTRIBUTE = 'data-agent-send-target'
